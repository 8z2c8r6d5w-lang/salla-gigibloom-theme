"use strict";
var __create = Object.create;
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
var __commonJS = (cb, mod) => function __require() {
  try {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  } catch (e) {
    throw mod = 0, e;
  }
};
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e) {
        reject(e);
      }
    };
    var step = (x) => x.done ? resolve(x.value) : Promise.resolve(x.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};

// ../../node_modules/.pnpm/dlv@1.1.3/node_modules/dlv/dist/dlv.js
var require_dlv = __commonJS({
  "../../node_modules/.pnpm/dlv@1.1.3/node_modules/dlv/dist/dlv.js"(exports2, module2) {
    module2.exports = function(t, e, l, n, o) {
      for (e = e.split ? e.split(".") : e, n = 0; n < e.length; n++) t = t ? t[e[n]] : o;
      return t === o ? l : t;
    };
  }
});

// ../../node_modules/.pnpm/@analytics+type-utils@0.6.2/node_modules/@analytics/type-utils/dist/analytics-util-types.js
var require_analytics_util_types = __commonJS({
  "../../node_modules/.pnpm/@analytics+type-utils@0.6.2/node_modules/@analytics/type-utils/dist/analytics-util-types.js"(exports2) {
    var e = function() {
    };
    var t = /^\d{4}-[01]\d-[0-3]\dT[0-2]\d:[0-5]\d:[0-5]\d\.\d+([+-][0-2]\d:[0-5]\d|Z)$/;
    var r = /^[a-zA-Z0-9.!#$%&'*+\/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*$/;
    var n = /^\{[\s\S]*\}$|^\[[\s\S]*\]$/;
    var o = "undefined" != typeof process ? process : {};
    var s = o.env && o.env.NODE_ENV || "";
    var i = "production" === s;
    var u = "staging" === s;
    var c = "development" === s;
    var p = "undefined" != typeof document;
    var a = p && "localhost" === window.location.hostname;
    var f = null != o.versions && null != o.versions.node;
    var l = "undefined" != typeof Deno && void 0 !== Deno.core;
    var x = "object" == typeof self && self.constructor && "DedicatedWorkerGlobalScope" === self.constructor.name;
    var d = p && "nodejs" === window.name || "undefined" != typeof navigator && void 0 !== navigator.userAgent && (navigator.userAgent.includes("Node.js") || navigator.userAgent.includes("jsdom"));
    function y(e2, t2) {
      return t2.charAt(0)[e2]() + t2.slice(1);
    }
    var N = y.bind(null, "toUpperCase");
    var m = y.bind(null, "toLowerCase");
    function E(e2) {
      return R(e2) ? N("null") : "object" == typeof e2 ? j(e2) : Object.prototype.toString.call(e2).slice(8, -1);
    }
    function g(e2, t2) {
      void 0 === t2 && (t2 = true);
      var r2 = E(e2);
      return t2 ? m(r2) : r2;
    }
    function b(e2, t2) {
      return typeof t2 === e2;
    }
    var O = b.bind(null, "function");
    var v = b.bind(null, "string");
    var A = b.bind(null, "undefined");
    var S = b.bind(null, "boolean");
    var T = b.bind(null, "symbol");
    function R(e2) {
      return null === e2;
    }
    function F(e2) {
      return "number" === g(e2) && !isNaN(e2);
    }
    function h(e2) {
      return "array" === g(e2);
    }
    function D(e2) {
      if (!L(e2)) return false;
      for (var t2 = e2; null !== Object.getPrototypeOf(t2); ) t2 = Object.getPrototypeOf(t2);
      return Object.getPrototypeOf(e2) === t2;
    }
    function L(e2) {
      return e2 && ("object" == typeof e2 || null !== e2);
    }
    function j(e2) {
      return O(e2.constructor) ? e2.constructor.name : null;
    }
    function C(e2) {
      return e2 instanceof Set;
    }
    function I(e2) {
      return e2 instanceof Map;
    }
    function P(e2) {
      return e2 instanceof Error || v(e2.message) && e2.constructor && F(e2.constructor.stackTraceLimit);
    }
    function U(e2, t2) {
      if ("object" != typeof t2 || R(t2)) return false;
      if (t2 instanceof e2) return true;
      var r2 = g(new e2(""));
      if (P(t2)) for (; t2; ) {
        if (g(t2) === r2) return true;
        t2 = Object.getPrototypeOf(t2);
      }
      return false;
    }
    var _ = U.bind(null, TypeError);
    var B = U.bind(null, SyntaxError);
    function G(e2, t2) {
      var r2 = e2 instanceof Element || e2 instanceof HTMLDocument;
      return r2 && t2 ? w(e2, t2) : r2;
    }
    function w(e2, t2) {
      return void 0 === t2 && (t2 = ""), e2 && e2.nodeName === t2.toUpperCase();
    }
    function M(e2) {
      var t2 = [].slice.call(arguments, 1);
      return function() {
        return e2.apply(void 0, [].slice.call(arguments).concat(t2));
      };
    }
    var z = M(G, "form");
    var Z = M(G, "button");
    var k = M(G, "input");
    var Y = M(G, "select");
    exports2.ALL = "*", exports2.ANY = "any", exports2.ARRAY = "array", exports2.ASYNC_FUNCTION = "asyncFunction", exports2.ASYNC_GENERATOR_FUNCTION = "asyncGeneratorFunction", exports2.BOOLEAN = "boolean", exports2.BUTTON = "button", exports2.CHANGE = "change", exports2.ENV = s, exports2.ERROR = "error", exports2.FORM = "form", exports2.FUNCTION = "function", exports2.GENERATOR_FUNCTION = "generatorFunction", exports2.HIDDEN = "hidden", exports2.INPUT = "input", exports2.NONE = "none", exports2.NULL = "null", exports2.NUMBER = "number", exports2.OBJECT = "object", exports2.PREFIX = "__", exports2.REGEX_EMAIL = r, exports2.REGEX_ISO = t, exports2.REGEX_JSON = n, exports2.SELECT = "select", exports2.STRING = "string", exports2.SUBMIT = "submit", exports2.SYMBOL = "symbol", exports2.SYNTAX_ERROR = "syntaxError", exports2.TYPE_ERROR = "typeError", exports2.UNDEFINED = "undefined", exports2.ctorName = j, exports2.ensureArray = function(e2) {
      return e2 ? h(e2) ? e2 : [e2] : [];
    }, exports2.getType = g, exports2.getTypeName = E, exports2.isArguments = function(e2) {
      try {
        if (F(e2.length) && O(e2.callee)) return true;
      } catch (e3) {
        if (-1 !== e3.message.indexOf("callee")) return true;
      }
      return false;
    }, exports2.isArray = h, exports2.isAsyncFunction = function(e2) {
      return "asyncFunction" === g(e2);
    }, exports2.isBoolean = S, exports2.isBrowser = p, exports2.isBuffer = function(e2) {
      return !(!e2.constructor || !O(e2.constructor.isBuffer)) && e2.constructor.isBuffer(e2);
    }, exports2.isButton = Z, exports2.isClass = function(e2) {
      return !!O(e2) && /^class /.test(Function.prototype.toString.call(e2));
    }, exports2.isDate = function(e2) {
      return e2 instanceof Date || O(e2.toDateString) && O(e2.getDate) && O(e2.setDate);
    }, exports2.isDefined = function(e2) {
      return !A(e2);
    }, exports2.isDeno = l, exports2.isDev = c, exports2.isElement = G, exports2.isEmail = function(e2) {
      return !(e2.length > 320) && r.test(e2);
    }, exports2.isEmpty = function(e2) {
      return !(!R(e2) && (h(e2) ? e2.length : C(e2) || I(e2) ? e2.size : D(e2) && Object.keys(e2).length));
    }, exports2.isError = P, exports2.isErrorLike = function(e2) {
      return L(e2) && v(e2.message) && v(e2.name);
    }, exports2.isFalse = function(e2) {
      return false === e2;
    }, exports2.isFalsy = function(e2) {
      return !e2;
    }, exports2.isForm = z, exports2.isFunction = O, exports2.isGenerator = function(e2) {
      return L(e2) && O(e2.throw) && O(e2.return) && O(e2.next);
    }, exports2.isGeneratorFunction = function(e2) {
      return "generatorFunction" === g(e2);
    }, exports2.isHidden = function(e2, t2) {
      if (!e2 || "hidden" === getComputedStyle(e2).visibility) return true;
      for (; e2; ) {
        if (null != t2 && e2 === t2) return false;
        if ("none" === getComputedStyle(e2).display) return true;
        e2 = e2.parentElement;
      }
      return false;
    }, exports2.isInput = k, exports2.isIsoDate = function(e2) {
      return t.test(e2);
    }, exports2.isJsDom = d, exports2.isJson = function(e2) {
      if (!v(e2) || !n.test(e2)) return false;
      try {
        JSON.parse(e2);
      } catch (e3) {
        return false;
      }
      return true;
    }, exports2.isLocalHost = a, exports2.isMap = I, exports2.isMethod = function(e2, t2) {
      return D(e2) && O(e2[t2]);
    }, exports2.isNoOp = function(t2) {
      if (!O(t2)) return false;
      var r2 = /{(\r|\n|\s)*}/gm, n2 = e + "";
      return n2 === (t2.toString().match(r2) || [""])[0].replace(r2, n2);
    }, exports2.isNode = f, exports2.isNodeList = function(e2) {
      return NodeList.prototype.isPrototypeOf(e2);
    }, exports2.isNodeType = w, exports2.isNull = R, exports2.isNumber = F, exports2.isNumberLike = function(e2) {
      return !isNaN(parseFloat(e2));
    }, exports2.isObject = D, exports2.isObjectLike = L, exports2.isPrimitive = function(e2) {
      if (R(e2)) return true;
      switch (typeof e2) {
        case "string":
        case "number":
        case "symbol":
        case "undefined":
        case "boolean":
          return true;
        default:
          return false;
      }
    }, exports2.isProd = i, exports2.isPromise = function(e2) {
      return !!e2 && !!(!A(Promise) && e2 instanceof Promise || e2.then && O(e2.then));
    }, exports2.isRegex = function(e2) {
      return e2 instanceof RegExp;
    }, exports2.isSelect = Y, exports2.isSet = C, exports2.isStaging = u, exports2.isString = v, exports2.isSymbol = T, exports2.isSyntaxError = B, exports2.isTrue = function(e2) {
      return true === e2;
    }, exports2.isTruthy = function(e2) {
      return !(v(e2) && "false" === e2.toLowerCase() || !e2);
    }, exports2.isTypeError = _, exports2.isUndefined = A, exports2.isWebWorker = x, exports2.noOp = e;
  }
});

// ../../node_modules/.pnpm/analytics-utils@1.0.12_@types+dlv@1.1.4/node_modules/analytics-utils/dist/analytics-utils.js
var require_analytics_utils = __commonJS({
  "../../node_modules/.pnpm/analytics-utils@1.0.12_@types+dlv@1.1.4/node_modules/analytics-utils/dist/analytics-utils.js"(exports2) {
    var e = require_dlv();
    var r = require_analytics_util_types();
    function t(e2) {
      return e2 && "object" == typeof e2 && "default" in e2 ? e2 : { default: e2 };
    }
    var n = t(e);
    function o(e2) {
      try {
        return decodeURIComponent(e2.replace(/\+/g, " "));
      } catch (e3) {
        return null;
      }
    }
    function i(e2) {
      if (!r.isBrowser) return false;
      var t2 = e2 || document.referrer;
      if (t2) {
        var n2 = window.document.location.port, o2 = t2.split("/")[2];
        return n2 && (o2 = o2.replace(":" + n2, "")), o2 !== window.location.hostname;
      }
      return false;
    }
    function a(e2, r2) {
      var t2 = (e2.split("?") || [,])[1];
      if (!t2 || -1 === t2.indexOf(r2)) return e2;
      var n2 = new RegExp("(\\&|\\?)" + r2 + '([_A-Za-z0-9"+=.\\/\\-@%]+)', "g"), o2 = ("?" + t2).replace(n2, "").replace(/^&/, "?");
      return e2.replace("?" + t2, o2);
    }
    function u(e2) {
      return (function(e3) {
        for (var r2, t2 = /* @__PURE__ */ Object.create(null), n2 = /([^&=]+)=?([^&]*)/g; r2 = n2.exec(e3); ) {
          var i2 = o(r2[1]), a2 = o(r2[2]);
          "[]" === i2.substring(i2.length - 2) ? (t2[i2 = i2.substring(0, i2.length - 2)] || (t2[i2] = [])).push(a2) : t2[i2] = "" === a2 || a2;
        }
        for (var u2 in t2) {
          var s2 = u2.split("[");
          s2.length > 1 && (c(t2, s2.map(function(e4) {
            return e4.replace(/[?[\]\\ ]/g, "");
          }), t2[u2]), delete t2[u2]);
        }
        return t2;
      })((function(e3) {
        if (e3) {
          var t2 = e3.match(/\?(.*)/);
          return t2 && t2[1] ? t2[1].split("#")[0] : "";
        }
        return r.isBrowser && window.location.search.substring(1);
      })(e2));
    }
    function c(e2, r2, t2) {
      for (var n2 = r2.length - 1, o2 = 0; o2 < n2; ++o2) {
        var i2 = r2[o2];
        if ("__proto__" === i2 || "constructor" === i2) break;
        i2 in e2 || (e2[i2] = {}), e2 = e2[i2];
      }
      e2[r2[n2]] = t2;
    }
    function s(e2) {
      if (!r.isBrowser) return null;
      var t2 = document.createElement("a");
      return t2.setAttribute("href", e2), t2.hostname;
    }
    function l(e2) {
      return (s(e2) || "").split(".").slice(-2).join(".");
    }
    function m(e2) {
      var r2 = e2.split(".");
      return r2.length > 1 ? r2.slice(0, -1).join(".") : e2;
    }
    var f = { trimTld: m, getDomainBase: l, getDomainHost: s };
    var p = "q";
    var x = { "daum.net": p, "eniro.se": "search_word", "naver.com": "query", "yahoo.com": "p", "msn.com": p, "aol.com": p, "ask.com": p, "baidu.com": "wd", "yandex.com": "text", "rambler.ru": "words", google: p, "bing.com": { p, n: "live" } };
    Object.defineProperty(exports2, "dotProp", { enumerable: true, get: function() {
      return n.default;
    } }), exports2.decodeUri = o, exports2.getBrowserLocale = function() {
      if (r.isBrowser) {
        var e2 = navigator, t2 = e2.languages;
        return e2.userLanguage || (t2 && t2.length ? t2[0] : e2.language);
      }
    }, exports2.getTimeZone = function() {
      try {
        return Intl.DateTimeFormat().resolvedOptions().timeZone;
      } catch (e2) {
      }
    }, exports2.isExternalReferrer = i, exports2.isScriptLoaded = function(e2) {
      if (!r.isBrowser) return true;
      var t2 = document.getElementsByTagName("script");
      return !!Object.keys(t2).filter(function(n2) {
        var o2 = t2[n2].src;
        return r.isString(e2) ? -1 !== o2.indexOf(e2) : !!r.isRegex(e2) && o2.match(e2);
      }).length;
    }, exports2.paramsClean = a, exports2.paramsGet = function(e2, r2) {
      return o((RegExp(e2 + "=(.+?)(&|$)").exec(r2) || [, ""])[1]);
    }, exports2.paramsParse = u, exports2.paramsRemove = function(e2, t2) {
      return r.isBrowser ? new Promise(function(r2, n2) {
        if (window.history && window.history.replaceState) {
          var o2 = window.location.href, i2 = a(o2, e2);
          o2 !== i2 && history.replaceState({}, "", i2);
        }
        return t2 && t2(), r2();
      }) : Promise.resolve();
    }, exports2.parseReferrer = function(e2, t2) {
      if (!r.isBrowser) return false;
      var n2 = { source: "(direct)", medium: "(none)", campaign: "(not set)" };
      e2 && i(e2) && (n2.referrer = e2);
      var o2 = (function(e3) {
        if (!e3 || !r.isBrowser) return false;
        var t3 = l(e3), n3 = document.createElement("a");
        if (n3.href = e3, n3.hostname.indexOf("google") > -1 && (t3 = "google"), x[t3]) {
          var o3 = x[t3], a3 = new RegExp(("string" == typeof o3 ? o3 : o3.p) + "=.*?([^&#]*|$)", "gi"), u2 = n3.search.match(a3);
          return { source: o3.n || m(t3), medium: "organic", term: (u2 ? u2[0].split("=")[1] : "") || "(not provided)" };
        }
        var c3 = i(e3) ? "referral" : "internal";
        return { source: n3.hostname, medium: c3 };
      })(e2);
      o2 && Object.keys(o2).length && (n2 = Object.assign({}, n2, o2));
      var a2 = u(t2), c2 = Object.keys(a2);
      if (!c2.length) return n2;
      var s2 = c2.reduce(function(e3, r2) {
        return r2.match(/^utm_/) && (e3["" + r2.replace(/^utm_/, "")] = a2[r2]), r2.match(/^(d|g)clid/) && (e3.source = "google", e3.medium = a2.gclid ? "cpc" : "cpm", e3[r2] = a2[r2]), e3;
      }, {});
      return Object.assign({}, n2, s2);
    }, exports2.throttle = function(e2, r2) {
      var t2, n2, o2, i2 = null, a2 = 0, u2 = function() {
        a2 = /* @__PURE__ */ new Date(), i2 = null, o2 = e2.apply(t2, n2);
      };
      return function() {
        var c2 = /* @__PURE__ */ new Date();
        a2 || (a2 = c2);
        var s2 = r2 - (c2 - a2);
        return t2 = this, n2 = arguments, s2 <= 0 ? (clearTimeout(i2), i2 = null, a2 = c2, o2 = e2.apply(t2, n2)) : i2 || (i2 = setTimeout(u2, s2)), o2;
      };
    }, exports2.url = f, exports2.uuid = function() {
      for (var e2 = "", r2 = 0, t2 = 4294967295 * Math.random() | 0; r2++ < 36; ) {
        var n2 = "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx"[r2 - 1], o2 = 15 & t2;
        e2 += "-" == n2 || "4" == n2 ? n2 : ("x" == n2 ? o2 : 3 & o2 | 8).toString(16), t2 = r2 % 8 == 0 ? 4294967295 * Math.random() | 0 : t2 >> 4;
      }
      return e2;
    };
  }
});

// ../../node_modules/.pnpm/@analytics+global-storage-utils@0.1.7/node_modules/@analytics/global-storage-utils/dist/analytics-util-global-storage.js
var require_analytics_util_global_storage = __commonJS({
  "../../node_modules/.pnpm/@analytics+global-storage-utils@0.1.7/node_modules/@analytics/global-storage-utils/dist/analytics-util-global-storage.js"(exports2) {
    var e = require_analytics_util_types();
    var t = e.PREFIX + "global" + e.PREFIX;
    var r = typeof self === e.OBJECT && self.self === self && self || typeof global === e.OBJECT && global.global === global && global || void 0;
    r[t] || (r[t] = {});
    var o = {};
    function l(t2) {
      if (typeof o[t2] !== e.UNDEFINED) return o[t2];
      try {
        var r2 = window[t2];
        r2.setItem(e.UNDEFINED, e.UNDEFINED), r2.removeItem(e.UNDEFINED);
      } catch (e2) {
        return o[t2] = false;
      }
      return o[t2] = true;
    }
    exports2.GLOBAL = "global", exports2.KEY = t, exports2.get = function(e2) {
      return r[t][e2];
    }, exports2.globalContext = r, exports2.hasSupport = l, exports2.remove = function(e2) {
      delete r[t][e2];
    }, exports2.set = function(e2, o2) {
      return r[t][e2] = o2;
    }, exports2.wrap = function(e2, t2, r2) {
      var o2;
      try {
        if (l(e2)) {
          var n = window[e2];
          o2 = n[t2].bind(n);
        }
      } catch (e3) {
      }
      return o2 || r2;
    };
  }
});

// ../../node_modules/.pnpm/@analytics+core@0.12.15_@types+dlv@1.1.4/node_modules/@analytics/core/dist/server/analytics-core.js
var require_analytics_core = __commonJS({
  "../../node_modules/.pnpm/@analytics+core@0.12.15_@types+dlv@1.1.4/node_modules/@analytics/core/dist/server/analytics-core.js"(exports2) {
    var e = require_analytics_utils();
    var t = require_analytics_util_global_storage();
    var n = require_analytics_util_types();
    function r() {
      return r = Object.assign || function(e2) {
        for (var t2 = 1; t2 < arguments.length; t2++) {
          var n2 = arguments[t2];
          for (var r2 in n2) Object.prototype.hasOwnProperty.call(n2, r2) && (e2[r2] = n2[r2]);
        }
        return e2;
      }, r.apply(this, arguments);
    }
    var i = "function";
    var a = "undefined";
    var o = "@@redux/" + Math.random().toString(36);
    var u = /* @__PURE__ */ (function() {
      return typeof Symbol === i && Symbol.observable || "@@observable";
    })();
    var c = " != " + i;
    function s(e2, t2, r2) {
      var o2;
      if (typeof t2 === i && typeof r2 === a && (r2 = t2, t2 = void 0), typeof r2 !== a) {
        if (typeof r2 !== i) throw new Error("enhancer" + c);
        return r2(s)(e2, t2);
      }
      if (typeof e2 !== i) throw new Error("reducer" + c);
      var l2 = e2, f2 = t2, d2 = [], p2 = d2, m2 = false;
      function g2() {
        p2 === d2 && (p2 = d2.slice());
      }
      function h2() {
        return f2;
      }
      function v2(e3) {
        if (typeof e3 !== i) throw new Error("Listener" + c);
        var t3 = true;
        return g2(), p2.push(e3), function() {
          if (t3) {
            t3 = false, g2();
            var n2 = p2.indexOf(e3);
            p2.splice(n2, 1);
          }
        };
      }
      function y2(e3) {
        if (!n.isObject(e3)) throw new Error("Act != obj");
        if (typeof e3.type === a) throw new Error("ActType " + a);
        if (m2) throw new Error("Dispatch in reducer");
        try {
          m2 = true, f2 = l2(f2, e3);
        } finally {
          m2 = false;
        }
        for (var t3 = d2 = p2, r3 = 0; r3 < t3.length; r3++) (0, t3[r3])();
        return e3;
      }
      return y2({ type: "@@redux/INIT" }), (o2 = { dispatch: y2, subscribe: v2, getState: h2, replaceReducer: function(e3) {
        if (typeof e3 !== i) throw new Error("next reducer" + c);
        l2 = e3, y2({ type: "@@redux/INIT" });
      } })[u] = function() {
        var e3, t3 = v2;
        return (e3 = { subscribe: function(e4) {
          if ("object" != typeof e4) throw new TypeError("Observer != obj");
          function n2() {
            e4.next && e4.next(h2());
          }
          return n2(), { unsubscribe: t3(n2) };
        } })[u] = function() {
          return this;
        }, e3;
      }, o2;
    }
    function l(e2, t2) {
      var n2 = t2 && t2.type;
      return "action " + (n2 && n2.toString() || "?") + "reducer " + e2 + " returns " + a;
    }
    function f() {
      var e2 = [].slice.call(arguments);
      return 0 === e2.length ? function(e3) {
        return e3;
      } : 1 === e2.length ? e2[0] : e2.reduce(function(e3, t2) {
        return function() {
          return e3(t2.apply(void 0, [].slice.call(arguments)));
        };
      });
    }
    function d() {
      var e2 = arguments;
      return function(t2) {
        return function(n2, i2, a2) {
          var o2, u2 = t2(n2, i2, a2), c2 = u2.dispatch, s2 = { getState: u2.getState, dispatch: function(e3) {
            return c2(e3);
          } };
          return o2 = [].slice.call(e2).map(function(e3) {
            return e3(s2);
          }), r({}, u2, { dispatch: c2 = f.apply(void 0, o2)(u2.dispatch) });
        };
      };
    }
    var p = n.PREFIX + "anon_id";
    var m = n.PREFIX + "user_id";
    var g = n.PREFIX + "user_traits";
    var h = { __proto__: null, ANON_ID: p, USER_ID: m, USER_TRAITS: g };
    var v = "userId";
    var y = "anonymousId";
    var b = ["bootstrap", "params", "campaign", "initializeStart", "initialize", "initializeEnd", "ready", "resetStart", "reset", "resetEnd", "pageStart", "page", "pageEnd", "pageAborted", "trackStart", "track", "trackEnd", "trackAborted", "identifyStart", "identify", "identifyEnd", "identifyAborted", "userIdChanged", "registerPlugins", "enablePlugin", "disablePlugin", "online", "offline", "setItemStart", "setItem", "setItemEnd", "setItemAborted", "removeItemStart", "removeItem", "removeItemEnd", "removeItemAborted"];
    var I = ["name", "EVENTS", "config", "loaded"];
    var w = b.reduce(function(e2, t2) {
      return e2[t2] = t2, e2;
    }, { registerPluginType: function(e2) {
      return "registerPlugin:" + e2;
    }, pluginReadyType: function(e2) {
      return "ready:" + e2;
    } });
    var E = /^utm_/;
    var P = /^an_prop_/;
    var S = /^an_trait_/;
    function O(e2) {
      var t2 = e2.storage.setItem;
      return function(n2) {
        return function(i2) {
          return function(a2) {
            if (a2.type === w.bootstrap) {
              var o2 = a2.params, u2 = a2.user, c2 = a2.persistedUser, s2 = a2.initialUser, l2 = c2.userId === u2.userId;
              c2.anonymousId !== u2.anonymousId && t2(p, u2.anonymousId), l2 || t2(m, u2.userId), s2.traits && t2(g, r({}, l2 && c2.traits ? c2.traits : {}, s2.traits));
              var f2 = Object.keys(a2.params);
              if (f2.length) {
                var d2 = o2.an_uid, h2 = o2.an_event, v2 = f2.reduce(function(e3, t3) {
                  if (t3.match(E) || t3.match(/^(d|g)clid/)) {
                    var n3 = t3.replace(E, "");
                    e3.campaign["campaign" === n3 ? "name" : n3] = o2[t3];
                  }
                  return t3.match(P) && (e3.props[t3.replace(P, "")] = o2[t3]), t3.match(S) && (e3.traits[t3.replace(S, "")] = o2[t3]), e3;
                }, { campaign: {}, props: {}, traits: {} });
                n2.dispatch(r({ type: w.params, raw: o2 }, v2, d2 ? { userId: d2 } : {})), d2 && setTimeout(function() {
                  return e2.identify(d2, v2.traits);
                }, 0), h2 && setTimeout(function() {
                  return e2.track(h2, v2.props);
                }, 0), Object.keys(v2.campaign).length && n2.dispatch({ type: w.campaign, campaign: v2.campaign });
              }
            }
            return i2(a2);
          };
        };
      };
    }
    function j(e2) {
      return function(t2, n2) {
        if (void 0 === t2 && (t2 = {}), void 0 === n2 && (n2 = {}), n2.type === w.setItemEnd) {
          if (n2.key === p) return r({}, t2, { anonymousId: n2.value });
          if (n2.key === m) return r({}, t2, { userId: n2.value });
        }
        switch (n2.type) {
          case w.identify:
            return Object.assign({}, t2, { userId: n2.userId, traits: r({}, t2.traits, n2.traits) });
          case w.reset:
            return [m, p, g].forEach(function(t3) {
              e2.removeItem(t3);
            }), Object.assign({}, t2, { userId: null, anonymousId: null, traits: {} });
          default:
            return t2;
        }
      };
    }
    function N(e2) {
      return { userId: e2.getItem(m), anonymousId: e2.getItem(p), traits: e2.getItem(g) };
    }
    var A = function(e2) {
      return n.PREFIX + "TEMP" + n.PREFIX + e2;
    };
    function _(n2) {
      var i2 = n2.storage, a2 = i2.setItem, o2 = i2.removeItem, u2 = i2.getItem;
      return function(n3) {
        return function(i3) {
          return function(c2) {
            var s2 = c2.userId, l2 = c2.traits, f2 = c2.options;
            if (c2.type === w.reset && ([m, g, p].forEach(function(e2) {
              o2(e2);
            }), [v, y, "traits"].forEach(function(e2) {
              t.remove(A(e2));
            })), c2.type === w.identify) {
              u2(p) || a2(p, e.uuid());
              var d2 = u2(m), h2 = u2(g) || {};
              d2 && d2 !== s2 && n3.dispatch({ type: w.userIdChanged, old: { userId: d2, traits: h2 }, new: { userId: s2, traits: l2 }, options: f2 }), s2 && a2(m, s2), l2 && a2(g, r({}, h2, l2));
            }
            return i3(c2);
          };
        };
      };
    }
    var x = {};
    function k(e2, t2) {
      x[e2] && n.isFunction(x[e2]) && (x[e2](t2), delete x[e2]);
    }
    function T(e2, t2, n2) {
      return new Promise(function(i2, a2) {
        return t2() ? i2(e2) : n2 < 1 ? a2(r({}, e2, { queue: true })) : new Promise(function(e3) {
          return setTimeout(e3, 10);
        }).then(function(r2) {
          return T(e2, t2, n2 - 10).then(i2, a2);
        });
      });
    }
    function z(e2) {
      return { abort: e2 };
    }
    var M = function(e2) {
      var t2 = e2.data, i2 = e2.action, a2 = e2.instance, o2 = e2.state, u2 = e2.allPlugins, c2 = e2.allMatches, s2 = e2.store, l2 = e2.EVENTS;
      try {
        var f2 = o2.plugins, d2 = o2.context, p2 = i2.type, m2 = p2.match(q), g2 = t2.exact.map(function(e3) {
          return e3.pluginName;
        });
        m2 && (g2 = c2.during.map(function(e3) {
          return e3.pluginName;
        }));
        var h2 = /* @__PURE__ */ (function(e3, t3) {
          return function(n2, i3, a3) {
            var o3 = i3.config, u3 = i3.name, c3 = u3 + "." + n2.type;
            a3 && (c3 = a3.event);
            var s3 = n2.type.match(q) ? /* @__PURE__ */ (function(e4, t4, n3, i4, a4) {
              return function(o4, u4) {
                var c4 = i4 ? i4.name : e4, s4 = u4 && X(u4) ? u4 : n3;
                if (i4 && (!(s4 = u4 && X(u4) ? u4 : [e4]).includes(e4) || 1 !== s4.length)) throw new Error("Method " + t4 + " can only abort " + e4 + " plugin. " + JSON.stringify(s4) + " input valid");
                return r({}, a4, { abort: { reason: o4, plugins: s4, caller: t4, _: c4 } });
              };
            })(u3, c3, t3, a3, n2) : /* @__PURE__ */ (function(e4, t4) {
              return function() {
                throw new Error(e4.type + " action not cancellable. Remove abort in " + t4);
              };
            })(n2, c3);
            return { payload: H(n2), instance: e3, config: o3 || {}, abort: s3 };
          };
        })(a2, g2), v2 = t2.exact.reduce(function(e3, t3) {
          var n2 = t3.pluginName, r2 = t3.methodName, i3 = false;
          return r2.match(/^initialize/) || r2.match(/^reset/) || (i3 = !f2[n2].loaded), d2.offline && r2.match(/^(page|track|identify)/) && (i3 = true), e3["" + n2] = i3, e3;
        }, {});
        return Promise.resolve(t2.exact.reduce(function(e3, o3, c3) {
          try {
            var s3 = o3.pluginName;
            return Promise.resolve(e3).then(function(e4) {
              function o4() {
                return Promise.resolve(e4);
              }
              var c4 = (function() {
                if (t2.namespaced && t2.namespaced[s3]) return Promise.resolve(t2.namespaced[s3].reduce(function(e5, t3, i3) {
                  try {
                    return Promise.resolve(e5).then(function(e6) {
                      return t3.method && n.isFunction(t3.method) ? ((function(e7, t4) {
                        var n2 = J(e7);
                        if (n2 && n2.name === t4) {
                          var r2 = J(n2.method);
                          throw new Error([t4 + " plugin is calling method " + e7, "Plugins cant call self", "Use " + n2.method + " " + (r2 ? "or " + r2.method : "") + " in " + t4 + " plugin insteadof " + e7].join("\n"));
                        }
                      })(t3.methodName, t3.pluginName), Promise.resolve(t3.method({ payload: e6, instance: a2, abort: (i4 = e6, o5 = s3, c5 = t3.pluginName, function(e7, t4) {
                        return r({}, i4, { abort: { reason: e7, plugins: t4 || [o5], caller: p2, from: c5 || o5 } });
                      }), config: C(t3.pluginName, f2, u2), plugins: f2 })).then(function(t4) {
                        var i5 = n.isObject(t4) ? t4 : {};
                        return Promise.resolve(r({}, e6, i5));
                      })) : e6;
                      var i4, o5, c5;
                    });
                  } catch (e6) {
                    return Promise.reject(e6);
                  }
                }, Promise.resolve(i2))).then(function(t3) {
                  e4[s3] = t3;
                });
                e4[s3] = i2;
              })();
              return c4 && c4.then ? c4.then(o4) : o4();
            });
          } catch (e4) {
            return Promise.reject(e4);
          }
        }, Promise.resolve({}))).then(function(e3) {
          return Promise.resolve(t2.exact.reduce(function(i3, o3, c3) {
            try {
              var l3 = t2.exact.length === c3 + 1, d3 = o3.pluginName, g3 = u2[d3];
              return Promise.resolve(i3).then(function(t3) {
                var i4 = e3[d3] ? e3[d3] : {};
                if (m2 && (i4 = t3), $(i4, d3)) return B({ data: i4, method: p2, instance: a2, pluginName: d3, store: s2 }), Promise.resolve(t3);
                if ($(t3, d3)) return l3 && B({ data: t3, method: p2, instance: a2, store: s2 }), Promise.resolve(t3);
                if (v2.hasOwnProperty(d3) && true === v2[d3]) return s2.dispatch({ type: "queue", plugin: d3, payload: i4, _: { called: "queue", from: "queueMechanism" } }), Promise.resolve(t3);
                var o4 = h2(e3[d3], u2[d3]);
                return Promise.resolve(g3[p2]({ abort: o4.abort, payload: i4, instance: a2, config: C(d3, f2, u2), plugins: f2 })).then(function(o5) {
                  var u3 = n.isObject(o5) ? o5 : {}, c4 = r({}, t3, u3), l4 = e3[d3];
                  if ($(l4, d3)) B({ data: l4, method: p2, instance: a2, pluginName: d3, store: s2 });
                  else {
                    var f3 = p2 + ":" + d3;
                    (f3.match(/:/g) || []).length < 2 && !p2.match(F) && !p2.match(R) && a2.dispatch(r({}, m2 ? c4 : i4, { type: f3, _: { called: f3, from: "submethod" } }));
                  }
                  return Promise.resolve(c4);
                });
              });
            } catch (e4) {
              return Promise.reject(e4);
            }
          }, Promise.resolve(i2))).then(function(e4) {
            if (!(p2.match(q) || p2.match(/^registerPlugin/) || p2.match(R) || p2.match(F) || p2.match(/^params/) || p2.match(/^userIdChanged/))) {
              if (l2.plugins.includes(p2), e4._ && e4._.originalAction === p2) return e4;
              var n2 = r({}, e4, { _: { originalAction: e4.type, called: e4.type, from: "engineEnd" } });
              D(e4, t2.exact.length) && !p2.match(/End$/) && (n2 = r({}, n2, { type: e4.type + "Aborted" })), s2.dispatch(n2);
            }
            return e4;
          });
        });
      } catch (e3) {
        return Promise.reject(e3);
      }
    };
    var q = /Start$/;
    var F = /^bootstrap/;
    var R = /^ready/;
    function B(e2) {
      var t2 = e2.pluginName, n2 = e2.method + "Aborted" + (t2 ? ":" + t2 : "");
      e2.store.dispatch(r({}, e2.data, { type: n2, _: { called: n2, from: "abort" } }));
    }
    function C(e2, t2, n2) {
      var r2 = t2[e2] || n2[e2];
      return r2 && r2.config ? r2.config : {};
    }
    function V(e2, t2) {
      return t2.reduce(function(t3, n2) {
        return n2[e2] ? t3.concat({ methodName: e2, pluginName: n2.name, method: n2[e2] }) : t3;
      }, []);
    }
    function U(e2, t2) {
      var n2 = e2.replace(q, ""), r2 = t2 ? ":" + t2 : "";
      return ["" + e2 + r2, "" + n2 + r2, n2 + "End" + r2];
    }
    function $(e2, t2) {
      var n2 = e2.abort;
      return !!n2 && (true === n2 || L(n2, t2) || n2 && L(n2.plugins, t2));
    }
    function D(e2, t2) {
      var r2 = e2.abort;
      if (!r2) return false;
      if (true === r2 || n.isString(r2)) return true;
      var i2 = r2.plugins;
      return X(r2) && r2.length === t2 || X(i2) && i2.length === t2;
    }
    function X(e2) {
      return Array.isArray(e2);
    }
    function L(e2, t2) {
      return !(!e2 || !X(e2)) && e2.includes(t2);
    }
    function J(e2) {
      var t2 = e2.match(/(.*):(.*)/);
      return !!t2 && { method: t2[1], name: t2[2] };
    }
    function H(e2) {
      return Object.keys(e2).reduce(function(t2, r2) {
        return "type" === r2 || (t2[r2] = n.isObject(e2[r2]) ? Object.assign({}, e2[r2]) : e2[r2]), t2;
      }, {});
    }
    function K(e2, t2, i2) {
      var a2 = {};
      return function(o2) {
        return function(u2) {
          return function(c2) {
            try {
              var s2, l2 = function(e3) {
                return s2 ? e3 : u2(p2);
              }, f2 = c2.type, d2 = c2.plugins, p2 = c2;
              if (c2.abort) return Promise.resolve(u2(c2));
              if (f2 === w.enablePlugin && o2.dispatch({ type: w.initializeStart, plugins: d2, disabled: [], fromEnable: true, meta: c2.meta }), f2 === w.disablePlugin && setTimeout(function() {
                return k(c2.meta.rid, { payload: c2 });
              }, 0), f2 === w.initializeEnd) {
                var m2 = t2(), g2 = Object.keys(m2), h2 = g2.filter(function(e3) {
                  return d2.includes(e3);
                }).map(function(e3) {
                  return m2[e3];
                }), b2 = [], E2 = [], P2 = c2.disabled, S2 = h2.map(function(e3) {
                  var t3 = e3.loaded, n2 = e3.name, r2 = e3.config;
                  return T(e3, function() {
                    return t3({ config: r2 });
                  }, 1e4).then(function(t4) {
                    return a2[n2] || (o2.dispatch({ type: w.pluginReadyType(n2), name: n2, events: Object.keys(e3).filter(function(e4) {
                      return !I.includes(e4);
                    }) }), a2[n2] = true), b2 = b2.concat(n2), e3;
                  }).catch(function(e4) {
                    if (e4 instanceof Error) throw new Error(e4);
                    return E2 = E2.concat(e4.name), e4;
                  });
                });
                Promise.all(S2).then(function(e3) {
                  var t3 = { plugins: b2, failed: E2, disabled: P2 };
                  setTimeout(function() {
                    g2.length === S2.length + P2.length && o2.dispatch(r({}, { type: w.ready }, t3));
                  }, 0);
                });
              }
              var O2 = (function() {
                if (f2 !== w.bootstrap) return /^ready:([^:]*)$/.test(f2) && setTimeout(function() {
                  return (function(e3, t3, i3) {
                    var a3 = {}, o3 = t3(), u3 = e3.getState(), c3 = u3.plugins, s3 = u3.queue, l3 = u3.user;
                    if (!u3.context.offline && s3 && s3.actions && s3.actions.length) {
                      var f3 = s3.actions.reduce(function(e4, t4, n2) {
                        return c3[t4.plugin].loaded ? (e4.process.push(t4), e4.processIndex.push(n2)) : (e4.requeue.push(t4), e4.requeueIndex.push(n2)), e4;
                      }, { processIndex: [], process: [], requeue: [], requeueIndex: [] });
                      if (f3.processIndex && f3.processIndex.length) {
                        f3.processIndex.forEach(function(t4) {
                          var u4 = s3.actions[t4], f4 = u4.plugin, d4 = u4.payload.type, p3 = o3[f4][d4];
                          if (p3 && n.isFunction(p3)) {
                            var m3, g3 = (function(e4, t5) {
                              return void 0 === e4 && (e4 = {}), void 0 === t5 && (t5 = {}), [v, y].reduce(function(n2, r2) {
                                return e4.hasOwnProperty(r2) && t5[r2] && t5[r2] !== e4[r2] && (n2[r2] = t5[r2]), n2;
                              }, e4);
                            })(u4.payload, l3), h3 = a3[g3.meta.rid];
                            if (!h3 && (m3 = p3({ payload: g3, config: c3[f4].config, instance: i3, abort: z })) && n.isObject(m3) && m3.abort) return void (a3[g3.meta.rid] = true);
                            if (!h3) {
                              var b3 = d4 + ":" + f4;
                              e3.dispatch(r({}, g3, { type: b3, _: { called: b3, from: "queueDrain" } }));
                            }
                          }
                        });
                        var d3 = s3.actions.filter(function(e4, t4) {
                          return !~f3.processIndex.indexOf(t4);
                        });
                        s3.actions = d3;
                      }
                    }
                  })(o2, t2, e2);
                }, 0), Promise.resolve((function(e3, t3, i3, a3, o3) {
                  try {
                    var u3 = n.isFunction(t3) ? t3() : t3, c3 = e3.type, s3 = c3.replace(q, "");
                    if (e3._ && e3._.called) return Promise.resolve(e3);
                    var l3 = i3.getState(), f3 = (m3 = u3, void 0 === (g3 = l3.plugins) && (g3 = {}), void 0 === (h3 = e3.options) && (h3 = {}), Object.keys(m3).filter(function(e4) {
                      var t4 = h3.plugins || {};
                      return n.isBoolean(t4[e4]) ? t4[e4] : false !== t4.all && (!g3[e4] || false !== g3[e4].enabled);
                    }).map(function(e4) {
                      return m3[e4];
                    }));
                    c3 === w.initializeStart && e3.fromEnable && (f3 = Object.keys(l3.plugins).filter(function(t4) {
                      var n2 = l3.plugins[t4];
                      return e3.plugins.includes(t4) && !n2.initialized;
                    }).map(function(e4) {
                      return u3[e4];
                    }));
                    var d3 = f3.map(function(e4) {
                      return e4.name;
                    }), p3 = (function(e4, t4, n2) {
                      var r2 = U(e4).map(function(e5) {
                        return V(e5, t4);
                      });
                      return t4.reduce(function(n3, r3) {
                        var i4 = r3.name, a4 = U(e4, i4).map(function(e5) {
                          return V(e5, t4);
                        }), o4 = a4[0], u4 = a4[1], c4 = a4[2];
                        return o4.length && (n3.beforeNS[i4] = o4), u4.length && (n3.duringNS[i4] = u4), c4.length && (n3.afterNS[i4] = c4), n3;
                      }, { before: r2[0], beforeNS: {}, during: r2[1], duringNS: {}, after: r2[2], afterNS: {} });
                    })(c3, f3);
                    return Promise.resolve(M({ action: e3, data: { exact: p3.before, namespaced: p3.beforeNS }, state: l3, allPlugins: u3, allMatches: p3, instance: i3, store: a3, EVENTS: o3 })).then(function(e4) {
                      function t4() {
                        var t5 = (function() {
                          if (c3.match(q)) return Promise.resolve(M({ action: r({}, n2, { type: s3 + "End" }), data: { exact: p3.after, namespaced: p3.afterNS }, state: l3, allPlugins: u3, allMatches: p3, instance: i3, store: a3, EVENTS: o3 })).then(function(e5) {
                            e5.meta && e5.meta.hasCallback && k(e5.meta.rid, { payload: e5 });
                          });
                        })();
                        return t5 && t5.then ? t5.then(function() {
                          return e4;
                        }) : e4;
                      }
                      if (D(e4, d3.length)) return e4;
                      var n2, f4 = (function() {
                        if (c3 !== s3) return Promise.resolve(M({ action: r({}, e4, { type: s3 }), data: { exact: p3.during, namespaced: p3.duringNS }, state: l3, allPlugins: u3, allMatches: p3, instance: i3, store: a3, EVENTS: o3 })).then(function(e5) {
                          n2 = e5;
                        });
                        n2 = e4;
                      })();
                      return f4 && f4.then ? f4.then(t4) : t4();
                    });
                  } catch (e4) {
                    return Promise.reject(e4);
                  }
                  var m3, g3, h3;
                })(c2, t2, e2, o2, i2)).then(function(e3) {
                  return s2 = 1, u2(e3);
                });
              })();
              return Promise.resolve(O2 && O2.then ? O2.then(l2) : l2(O2));
            } catch (e3) {
              return Promise.reject(e3);
            }
          };
        };
      };
    }
    function W(e2) {
      return function(t2) {
        return function(t3) {
          return function(n2) {
            var r2 = n2.type, i2 = n2.key, a2 = n2.value, o2 = n2.options;
            if (r2 === w.setItem || r2 === w.removeItem) {
              if (n2.abort) return t3(n2);
              r2 === w.setItem ? e2.setItem(i2, a2, o2) : e2.removeItem(i2, o2);
            }
            return t3(n2);
          };
        };
      };
    }
    var Y = function() {
      var e2 = this;
      this.before = [], this.after = [], this.addMiddleware = function(t2, n2) {
        e2[n2] = e2[n2].concat(t2);
      }, this.removeMiddleware = function(t2, n2) {
        var r2 = e2[n2].findIndex(function(e3) {
          return e3 === t2;
        });
        -1 !== r2 && (e2[n2] = [].concat(e2[n2].slice(0, r2), e2[n2].slice(r2 + 1)));
      }, this.dynamicMiddlewares = function(t2) {
        return function(n2) {
          return function(r2) {
            return function(i2) {
              var a2 = { getState: n2.getState, dispatch: function(e3) {
                return n2.dispatch(e3);
              } }, o2 = e2[t2].map(function(e3) {
                return e3(a2);
              });
              return f.apply(void 0, o2)(r2)(i2);
            };
          };
        };
      };
    };
    function G(e2) {
      return function(t2, n2) {
        void 0 === t2 && (t2 = {});
        var i2 = {};
        if ("initialize:aborted" === n2.type) return t2;
        if (/^registerPlugin:([^:]*)$/.test(n2.type)) {
          var a2 = Q(n2.type, "registerPlugin"), o2 = e2()[a2];
          if (!o2 || !a2) return t2;
          var u2 = n2.enabled, c2 = o2.config;
          return i2[a2] = { enabled: u2, initialized: !!u2 && Boolean(!o2.initialize), loaded: !!u2 && Boolean(o2.loaded({ config: c2 })), config: c2 }, r({}, t2, i2);
        }
        if (/^initialize:([^:]*)$/.test(n2.type)) {
          var s2 = Q(n2.type, w.initialize), l2 = e2()[s2];
          return l2 && s2 ? (i2[s2] = r({}, t2[s2], { initialized: true, loaded: Boolean(l2.loaded({ config: l2.config })) }), r({}, t2, i2)) : t2;
        }
        if (/^ready:([^:]*)$/.test(n2.type)) return i2[n2.name] = r({}, t2[n2.name], { loaded: true }), r({}, t2, i2);
        switch (n2.type) {
          case w.disablePlugin:
            return r({}, t2, Z(n2.plugins, false, t2));
          case w.enablePlugin:
            return r({}, t2, Z(n2.plugins, true, t2));
          default:
            return t2;
        }
      };
    }
    function Q(e2, t2) {
      return e2.substring(t2.length + 1, e2.length);
    }
    function Z(e2, t2, n2) {
      return e2.reduce(function(e3, i2) {
        return e3[i2] = r({}, n2[i2], { enabled: t2 }), e3;
      }, n2);
    }
    function ee(e2) {
      try {
        return JSON.parse(JSON.stringify(e2));
      } catch (e3) {
      }
      return e2;
    }
    var te = { last: {}, history: [] };
    function ne(e2, t2) {
      void 0 === e2 && (e2 = te);
      var n2 = t2.options, i2 = t2.meta;
      if (t2.type === w.track) {
        var a2 = ee(r({ event: t2.event, properties: t2.properties }, Object.keys(n2).length && { options: n2 }, { meta: i2 }));
        return r({}, e2, { last: a2, history: e2.history.concat(a2) });
      }
      return e2;
    }
    var re = { actions: [] };
    function ie(e2, t2) {
      void 0 === e2 && (e2 = re);
      var n2 = t2.payload;
      switch (t2.type) {
        case "queue":
          var i2;
          return i2 = n2 && n2.type && n2.type === w.identify ? [t2].concat(e2.actions) : e2.actions.concat(t2), r({}, e2, { actions: i2 });
        case "dequeue":
          return [];
        default:
          return e2;
      }
    }
    var ae = /#.*$/;
    function oe(e2) {
      var t2 = /(http[s]?:\/\/)?([^\/\s]+\/)(.*)/g.exec(e2);
      return "/" + (t2 && t2[3] ? t2[3].split("?")[0].replace(ae, "") : "");
    }
    var ue;
    var ce = function(e2) {
      if (void 0 === e2 && (e2 = {}), !n.isBrowser) return e2;
      var t2 = document, i2 = t2.title, a2 = t2.referrer, o2 = window, u2 = o2.location, c2 = o2.innerWidth, s2 = o2.innerHeight, l2 = u2.hash, f2 = u2.search, d2 = (function(e3) {
        var t3 = (function() {
          if (n.isBrowser) {
            for (var e4, t4 = document.getElementsByTagName("link"), r2 = 0; e4 = t4[r2]; r2++) if ("canonical" === e4.getAttribute("rel")) return e4.getAttribute("href");
          }
        })();
        return t3 ? t3.match(/\?/) ? t3 : t3 + e3 : window.location.href.replace(ae, "");
      })(f2), p2 = { title: i2, url: d2, path: oe(d2), hash: l2, search: f2, width: c2, height: s2 };
      return a2 && "" !== a2 && (p2.referrer = a2), r({}, p2, e2);
    };
    var se = { last: {}, history: [] };
    function le(e2, t2) {
      void 0 === e2 && (e2 = se);
      var n2 = t2.options;
      if (t2.type === w.page) {
        var i2 = ee(r({ properties: t2.properties, meta: t2.meta }, Object.keys(n2).length && { options: n2 }));
        return r({}, e2, { last: i2, history: e2.history.concat(i2) });
      }
      return e2;
    }
    ue = {};
    var fe = { initialized: false, sessionId: e.uuid(), app: null, version: null, debug: false, offline: !!n.isBrowser && !navigator.onLine, os: { name: "na" }, userAgent: n.isBrowser ? navigator.userAgent : "node", library: { name: "analytics", version: "0.12.14" }, timezone: void 0, locale: void 0, campaign: {}, referrer: ue };
    function de(e2, t2) {
      void 0 === e2 && (e2 = fe);
      var n2 = e2.initialized, i2 = t2.campaign;
      switch (t2.type) {
        case w.campaign:
          return r({}, e2, { campaign: i2 });
        case w.offline:
          return r({}, e2, { offline: true });
        case w.online:
          return r({}, e2, { offline: false });
        default:
          return n2 ? e2 : r({}, fe, e2, { initialized: true });
      }
    }
    var pe = ["plugins", "reducers", "storage"];
    function me() {
      return t.set("analytics", []), function(e2) {
        return function(n2, r2, i2) {
          var a2 = e2(n2, r2, i2), o2 = a2.dispatch;
          return Object.assign(a2, { dispatch: function(e3) {
            return t.globalContext[t.KEY].analytics.push(e3.action || e3), o2(e3);
          } });
        };
      };
    }
    function ge(e2) {
      return function() {
        return f(f.apply(null, arguments), me());
      };
    }
    function he(e2) {
      return e2 ? n.isArray(e2) ? e2 : [e2] : [];
    }
    function ve(t2, i2, a2) {
      void 0 === t2 && (t2 = {});
      var o2, u2, c2 = e.uuid();
      return i2 && (x[c2] = (o2 = i2, u2 = (function(e2) {
        for (var t3, r2 = e2 || Array.prototype.slice.call(arguments), i3 = 0; i3 < r2.length; i3++) if (n.isFunction(r2[i3])) {
          t3 = r2[i3];
          break;
        }
        return t3;
      })(a2), function(e2) {
        u2 && u2(e2), o2(e2);
      })), r({}, t2, { rid: c2, ts: (/* @__PURE__ */ new Date()).getTime() }, i2 ? { hasCallback: true } : {});
    }
    function ye(u2) {
      void 0 === u2 && (u2 = {});
      var c2 = u2.reducers || {}, m2 = u2.initialUser || {}, g2 = (u2.plugins || []).reduce(function(e2, t2) {
        if (n.isFunction(t2)) return e2.middlewares = e2.middlewares.concat(t2), e2;
        if (t2.NAMESPACE && (t2.name = t2.NAMESPACE), !t2.name) throw new Error("https://lytics.dev/errors/1");
        t2.config || (t2.config = {});
        var r2 = t2.EVENTS ? Object.keys(t2.EVENTS).map(function(e3) {
          return t2.EVENTS[e3];
        }) : [];
        e2.pluginEnabled[t2.name] = !(false === t2.enabled || false === t2.config.enabled), delete t2.enabled, t2.methods && (e2.methods[t2.name] = Object.keys(t2.methods).reduce(function(e3, n2) {
          var r3;
          return e3[n2] = (r3 = t2.methods[n2], function() {
            for (var e4 = Array.prototype.slice.call(arguments), t3 = new Array(r3.length), n3 = 0; n3 < e4.length; n3++) t3[n3] = e4[n3];
            return t3[t3.length] = D2, r3.apply({ instance: D2 }, t3);
          }), e3;
        }, {}), delete t2.methods);
        var i2 = Object.keys(t2).concat(r2), a2 = new Set(e2.events.concat(i2));
        if (e2.events = Array.from(a2), e2.pluginsArray = e2.pluginsArray.concat(t2), e2.plugins[t2.name]) throw new Error(t2.name + "AlreadyLoaded");
        return e2.plugins[t2.name] = t2, e2.plugins[t2.name].loaded || (e2.plugins[t2.name].loaded = function() {
          return true;
        }), e2;
      }, { plugins: {}, pluginEnabled: {}, methods: {}, pluginsArray: [], middlewares: [], events: [] }), h2 = u2.storage ? u2.storage : { getItem: t.get, setItem: t.set, removeItem: t.remove }, E2 = /* @__PURE__ */ (function(e2) {
        return function(r2, i2, a2) {
          return i2.getState("user")[r2] || (a2 && n.isObject(a2) && a2[r2] ? a2[r2] : N(e2)[r2] || t.get(A(r2)) || null);
        };
      })(h2), P2 = g2.plugins, S2 = g2.events.filter(function(e2) {
        return !I.includes(e2);
      }).sort(), x2 = new Set(S2.concat(b).filter(function(e2) {
        return !I.includes(e2);
      })), k2 = Array.from(x2).sort(), T2 = function() {
        return P2;
      }, z2 = new Y(), M2 = z2.addMiddleware, q2 = z2.removeMiddleware, F2 = z2.dynamicMiddlewares, R2 = function() {
        throw new Error("Abort disabled inListener");
      }, B2 = e.paramsParse(), C2 = N(h2), V2 = r({}, C2, m2, B2.an_uid ? { userId: B2.an_uid } : {}, B2.an_aid ? { anonymousId: B2.an_aid } : {});
      V2.anonymousId || (V2.anonymousId = e.uuid());
      var U2 = r({ enable: function(e2, t2) {
        return new Promise(function(n2) {
          ae2.dispatch({ type: w.enablePlugin, plugins: he(e2), _: { originalAction: w.enablePlugin } }, n2, [t2]);
        });
      }, disable: function(e2, t2) {
        return new Promise(function(n2) {
          ae2.dispatch({ type: w.disablePlugin, plugins: he(e2), _: { originalAction: w.disablePlugin } }, n2, [t2]);
        });
      } }, g2.methods), $2 = false, D2 = { identify: function(e2, i2, a2, o2) {
        try {
          var u3 = n.isString(e2) ? e2 : null, c3 = n.isObject(e2) ? e2 : i2, s2 = a2 || {}, l2 = D2.user();
          t.set(A(v), u3);
          var f2 = u3 || c3.userId || E2(v, D2, c3);
          return Promise.resolve(new Promise(function(e3) {
            ae2.dispatch(r({ type: w.identifyStart, userId: f2, traits: c3 || {}, options: s2, anonymousId: l2.anonymousId }, l2.id && l2.id !== u3 && { previousId: l2.id }), e3, [i2, a2, o2]);
          }));
        } catch (e3) {
          return Promise.reject(e3);
        }
      }, track: function(e2, t2, r2, i2) {
        try {
          var a2 = n.isObject(e2) ? e2.event : e2;
          if (!a2 || !n.isString(a2)) throw new Error("EventMissing");
          var o2 = n.isObject(e2) ? e2 : t2 || {}, u3 = n.isObject(r2) ? r2 : {};
          return Promise.resolve(new Promise(function(e3) {
            ae2.dispatch({ type: w.trackStart, event: a2, properties: o2, options: u3, userId: E2(v, D2, t2), anonymousId: E2(y, D2, t2) }, e3, [t2, r2, i2]);
          }));
        } catch (e3) {
          return Promise.reject(e3);
        }
      }, page: function(e2, t2, r2) {
        try {
          var i2 = n.isObject(e2) ? e2 : {}, a2 = n.isObject(t2) ? t2 : {};
          return Promise.resolve(new Promise(function(n2) {
            ae2.dispatch({ type: w.pageStart, properties: ce(i2), options: a2, userId: E2(v, D2, i2), anonymousId: E2(y, D2, i2) }, n2, [e2, t2, r2]);
          }));
        } catch (e3) {
          return Promise.reject(e3);
        }
      }, user: function(t2) {
        if (t2 === v || "id" === t2) return E2(v, D2);
        if (t2 === y || "anonId" === t2) return E2(y, D2);
        var n2 = D2.getState("user");
        return t2 ? e.dotProp(n2, t2) : n2;
      }, reset: function(e2) {
        return new Promise(function(t2) {
          ae2.dispatch({ type: w.resetStart }, t2, e2);
        });
      }, ready: function(e2) {
        return $2 && e2({ plugins: U2, instance: D2 }), D2.on(w.ready, function(t2) {
          e2(t2), $2 = true;
        });
      }, on: function(e2, t2) {
        if (!e2 || !n.isFunction(t2)) return false;
        if (e2 === w.bootstrap) throw new Error(".on disabled for " + e2);
        var r2 = /Start$|Start:/;
        if ("*" === e2) {
          var i2 = function(e3) {
            return function(e4) {
              return function(n2) {
                return n2.type.match(r2) && t2({ payload: n2, instance: D2, plugins: P2 }), e4(n2);
              };
            };
          }, a2 = function(e3) {
            return function(e4) {
              return function(n2) {
                return n2.type.match(r2) || t2({ payload: n2, instance: D2, plugins: P2 }), e4(n2);
              };
            };
          };
          return M2(i2, be), M2(a2, Ie), function() {
            q2(i2, be), q2(a2, Ie);
          };
        }
        var o2 = e2.match(r2) ? be : Ie, u3 = function(n2) {
          return function(n3) {
            return function(r3) {
              return r3.type === e2 && t2({ payload: r3, instance: D2, plugins: P2, abort: R2 }), n3(r3);
            };
          };
        };
        return M2(u3, o2), function() {
          return q2(u3, o2);
        };
      }, once: function(e2, t2) {
        if (!e2 || !n.isFunction(t2)) return false;
        if (e2 === w.bootstrap) throw new Error(".once disabled for " + e2);
        var r2 = D2.on(e2, function(e3) {
          t2({ payload: e3.payload, instance: D2, plugins: P2, abort: R2 }), r2();
        });
        return r2;
      }, getState: function(t2) {
        var n2 = ae2.getState();
        return t2 ? e.dotProp(n2, t2) : Object.assign({}, n2);
      }, dispatch: function(e2) {
        var t2 = n.isString(e2) ? { type: e2 } : e2;
        if (b.includes(t2.type)) throw new Error("reserved action " + t2.type);
        var i2 = r({}, t2, { _: r({ originalAction: t2.type }, e2._ || {}) });
        ae2.dispatch(i2);
      }, enablePlugin: U2.enable, disablePlugin: U2.disable, plugins: U2, storage: { getItem: h2.getItem, setItem: function(e2, t2, n2) {
        ae2.dispatch({ type: w.setItemStart, key: e2, value: t2, options: n2 });
      }, removeItem: function(e2, t2) {
        ae2.dispatch({ type: w.removeItemStart, key: e2, options: t2 });
      } }, setAnonymousId: function(e2, t2) {
        D2.storage.setItem(p, e2, t2);
      }, events: { core: b, plugins: S2 } }, X2 = g2.middlewares.concat([function(e2) {
        return function(e3) {
          return function(t2) {
            return t2.meta || (t2.meta = ve()), e3(t2);
          };
        };
      }, F2(be), K(D2, T2, { all: k2, plugins: S2 }), W(h2), O(D2), _(D2), F2(Ie)]), L2 = { context: de, user: j(h2), page: le, track: ne, plugins: G(T2), queue: ie }, J2 = f, H2 = f;
      if (n.isBrowser && u2.debug) {
        var Q2 = window.__REDUX_DEVTOOLS_EXTENSION_COMPOSE__;
        Q2 && (J2 = Q2({ trace: true, traceLimit: 25 })), H2 = function() {
          return 0 === arguments.length ? me() : n.isObject(typeof arguments[0]) ? ge() : ge().apply(null, arguments);
        };
      }
      var Z2, ee2 = (function(e2) {
        return Object.keys(e2).reduce(function(t2, n2) {
          return pe.includes(n2) || (t2[n2] = e2[n2]), t2;
        }, {});
      })(u2), te2 = g2.pluginsArray.reduce(function(e2, t2) {
        var n2 = t2.name, r2 = t2.config, i2 = t2.loaded, a2 = g2.pluginEnabled[n2];
        return e2[n2] = { enabled: a2, initialized: !!a2 && Boolean(!t2.initialize), loaded: Boolean(i2({ config: r2 })), config: r2 }, e2;
      }, {}), re2 = { context: ee2, user: V2, plugins: te2 }, ae2 = s((function(e2) {
        for (var t2 = Object.keys(e2), n2 = {}, r2 = 0; r2 < t2.length; r2++) {
          var u3 = t2[r2];
          typeof e2[u3] === i && (n2[u3] = e2[u3]);
        }
        var c3, s2 = Object.keys(n2);
        try {
          !(function(e3) {
            Object.keys(e3).forEach(function(t3) {
              var n3 = e3[t3];
              if (typeof n3(void 0, { type: "@@redux/INIT" }) === a || typeof n3(void 0, { type: o }) === a) throw new Error("reducer " + t3 + " " + a);
            });
          })(n2);
        } catch (e3) {
          c3 = e3;
        }
        return function(e3, t3) {
          if (void 0 === e3 && (e3 = {}), c3) throw c3;
          for (var r3 = false, i2 = {}, o2 = 0; o2 < s2.length; o2++) {
            var u4 = s2[o2], f2 = e3[u4], d2 = (0, n2[u4])(f2, t3);
            if (typeof d2 === a) {
              var p2 = l(u4, t3);
              throw new Error(p2);
            }
            i2[u4] = d2, r3 = r3 || d2 !== f2;
          }
          return r3 ? i2 : e3;
        };
      })(r({}, L2, c2)), re2, H2(J2(d.apply(void 0, X2))));
      ae2.dispatch = (Z2 = ae2.dispatch, function(e2, t2, n2) {
        var i2 = r({}, e2, { meta: ve(e2.meta, t2, he(n2)) });
        return Z2.apply(null, [i2]);
      });
      var oe2 = Object.keys(P2);
      ae2.dispatch({ type: w.bootstrap, plugins: oe2, config: ee2, params: B2, user: V2, initialUser: m2, persistedUser: C2 });
      var ue2 = oe2.filter(function(e2) {
        return g2.pluginEnabled[e2];
      }), se2 = oe2.filter(function(e2) {
        return !g2.pluginEnabled[e2];
      });
      return ae2.dispatch({ type: w.registerPlugins, plugins: oe2, enabled: g2.pluginEnabled }), g2.pluginsArray.map(function(e2, t2) {
        var r2 = e2.bootstrap, i2 = e2.config, a2 = e2.name;
        r2 && n.isFunction(r2) && r2({ instance: D2, config: i2, payload: e2 }), ae2.dispatch({ type: w.registerPluginType(a2), name: a2, enabled: g2.pluginEnabled[a2], plugin: e2 }), g2.pluginsArray.length === t2 + 1 && ae2.dispatch({ type: w.initializeStart, plugins: ue2, disabled: se2 });
      }), D2;
    }
    var be = "before";
    var Ie = "after";
    exports2.Analytics = ye, exports2.CONSTANTS = h, exports2.EVENTS = w, exports2.default = ye, exports2.init = ye;
  }
});

// ../../node_modules/.pnpm/@analytics+cookie-utils@0.2.12/node_modules/@analytics/cookie-utils/dist/analytics-util-cookie.js
var require_analytics_util_cookie = __commonJS({
  "../../node_modules/.pnpm/@analytics+cookie-utils@0.2.12/node_modules/@analytics/cookie-utils/dist/analytics-util-cookie.js"(exports2) {
    var e = require_analytics_util_global_storage();
    var o = r();
    var t = c;
    var n = c;
    function i(t2) {
      return o ? c(t2, "", -1) : e.remove(t2);
    }
    function r() {
      if (void 0 !== o) return o;
      var e2 = "cookiecookie";
      try {
        c(e2, e2), o = -1 !== document.cookie.indexOf(e2), i(e2);
      } catch (e3) {
        o = false;
      }
      return o;
    }
    function c(t2, n2, i2, r2, c2, s) {
      if ("undefined" != typeof window) {
        var u = arguments.length > 1;
        return false === o && (u ? e.set(t2, n2) : e.get(t2)), u ? document.cookie = t2 + "=" + encodeURIComponent(n2) + (i2 ? "; expires=" + new Date(+/* @__PURE__ */ new Date() + 1e3 * i2).toUTCString() + (r2 ? "; path=" + r2 : "") + (c2 ? "; domain=" + c2 : "") + (s ? "; secure" : "") : "") : decodeURIComponent((("; " + document.cookie).split("; " + t2 + "=")[1] || "").split(";")[0]);
      }
    }
    exports2.COOKIE = "cookie", exports2.getCookie = t, exports2.hasCookies = r, exports2.removeCookie = i, exports2.setCookie = n;
  }
});

// ../../node_modules/.pnpm/@analytics+localstorage-utils@0.1.10/node_modules/@analytics/localstorage-utils/dist/analytics-util-localstorage.js
var require_analytics_util_localstorage = __commonJS({
  "../../node_modules/.pnpm/@analytics+localstorage-utils@0.1.10/node_modules/@analytics/localstorage-utils/dist/analytics-util-localstorage.js"(exports2) {
    var e = require_analytics_util_global_storage();
    var t = e.hasSupport.bind(null, "localStorage");
    var o = e.wrap("localStorage", "getItem", e.get);
    var a = e.wrap("localStorage", "setItem", e.set);
    var r = e.wrap("localStorage", "removeItem", e.remove);
    exports2.LOCAL_STORAGE = "localStorage", exports2.getItem = o, exports2.hasLocalStorage = t, exports2.removeItem = r, exports2.setItem = a;
  }
});

// ../../node_modules/.pnpm/@analytics+session-storage-utils@0.0.7/node_modules/@analytics/session-storage-utils/dist/analytics-util-session-storage.js
var require_analytics_util_session_storage = __commonJS({
  "../../node_modules/.pnpm/@analytics+session-storage-utils@0.0.7/node_modules/@analytics/session-storage-utils/dist/analytics-util-session-storage.js"(exports2) {
    var e = require_analytics_util_global_storage();
    var s = e.hasSupport.bind(null, "sessionStorage");
    var t = e.wrap("sessionStorage", "getItem", e.get);
    var o = e.wrap("sessionStorage", "setItem", e.set);
    var r = e.wrap("sessionStorage", "removeItem", e.remove);
    exports2.SESSION_STORAGE = "sessionStorage", exports2.getSessionItem = t, exports2.hasSessionStorage = s, exports2.removeSessionItem = r, exports2.setSessionItem = o;
  }
});

// ../../node_modules/.pnpm/@analytics+storage-utils@0.4.2/node_modules/@analytics/storage-utils/dist/analytics-util-storage.js
var require_analytics_util_storage = __commonJS({
  "../../node_modules/.pnpm/@analytics+storage-utils@0.4.2/node_modules/@analytics/storage-utils/dist/analytics-util-storage.js"(exports2) {
    var e = require_analytics_util_global_storage();
    var t = require_analytics_util_cookie();
    var r = require_analytics_util_localstorage();
    var o = require_analytics_util_session_storage();
    var n = require_analytics_util_types();
    function i(e2) {
      var t2 = e2;
      try {
        if ("true" === (t2 = JSON.parse(e2))) return true;
        if ("false" === t2) return false;
        if (n.isObject(t2)) return t2;
        parseFloat(t2) === t2 && (t2 = parseFloat(t2));
      } catch (e3) {
      }
      if (null !== t2 && "" !== t2) return t2;
    }
    var s = r.hasLocalStorage();
    var a = o.hasSessionStorage();
    var u = t.hasCookies();
    function O(r2, o2) {
      if (r2) {
        var s2 = S(o2), a2 = !A(s2), u2 = g(s2) ? i(localStorage.getItem(r2)) : void 0;
        if (a2 && !n.isUndefined(u2)) return u2;
        var O2 = f(s2) ? i(t.getCookie(r2)) : void 0;
        if (a2 && O2) return O2;
        var l2 = L(s2) ? i(sessionStorage.getItem(r2)) : void 0;
        if (a2 && l2) return l2;
        var c2 = e.get(r2);
        return a2 ? c2 : { localStorage: u2, sessionStorage: l2, cookie: O2, global: c2 };
      }
    }
    function l(s2, a2, u2) {
      if (s2 && !n.isUndefined(a2)) {
        var O2 = {}, l2 = S(u2), c2 = JSON.stringify(a2), m2 = !A(l2);
        return g(l2) && (O2[r.LOCAL_STORAGE] = p(r.LOCAL_STORAGE, a2, i(localStorage.getItem(s2))), localStorage.setItem(s2, c2), m2) ? O2[r.LOCAL_STORAGE] : f(l2) && (O2[t.COOKIE] = p(t.COOKIE, a2, i(t.getCookie(s2))), t.setCookie(s2, c2), m2) ? O2[t.COOKIE] : L(l2) && (O2[o.SESSION_STORAGE] = p(o.SESSION_STORAGE, a2, i(sessionStorage.getItem(s2))), sessionStorage.setItem(s2, c2), m2) ? O2[o.SESSION_STORAGE] : (O2[e.GLOBAL] = p(e.GLOBAL, a2, e.get(s2)), e.set(s2, a2), m2 ? O2[e.GLOBAL] : O2);
      }
    }
    function c(i2, s2) {
      if (i2) {
        var a2 = S(s2), u2 = O(i2, n.ALL), l2 = {};
        return !n.isUndefined(u2.localStorage) && g(a2) && (localStorage.removeItem(i2), l2[r.LOCAL_STORAGE] = u2.localStorage), !n.isUndefined(u2.cookie) && f(a2) && (t.removeCookie(i2), l2[t.COOKIE] = u2.cookie), !n.isUndefined(u2.sessionStorage) && L(a2) && (sessionStorage.removeItem(i2), l2[o.SESSION_STORAGE] = u2.sessionStorage), !n.isUndefined(u2.global) && m(a2, e.GLOBAL) && (e.remove(i2), l2[e.GLOBAL] = u2.global), l2;
      }
    }
    function S(e2) {
      return e2 ? n.isString(e2) ? e2 : e2.storage : n.ANY;
    }
    function g(e2) {
      return s && m(e2, r.LOCAL_STORAGE);
    }
    function f(e2) {
      return u && m(e2, t.COOKIE);
    }
    function L(e2) {
      return a && m(e2, o.SESSION_STORAGE);
    }
    function A(e2) {
      return e2 === n.ALL || "all" === e2;
    }
    function m(e2, t2) {
      return e2 === n.ANY || e2 === t2 || A(e2);
    }
    function p(e2, t2, r2) {
      return { location: e2, current: t2, previous: r2 };
    }
    var b = { setItem: l, getItem: O, removeItem: c };
    Object.defineProperty(exports2, "GLOBAL", { enumerable: true, get: function() {
      return e.GLOBAL;
    } }), Object.defineProperty(exports2, "globalContext", { enumerable: true, get: function() {
      return e.globalContext;
    } }), Object.defineProperty(exports2, "COOKIE", { enumerable: true, get: function() {
      return t.COOKIE;
    } }), Object.defineProperty(exports2, "getCookie", { enumerable: true, get: function() {
      return t.getCookie;
    } }), Object.defineProperty(exports2, "hasCookies", { enumerable: true, get: function() {
      return t.hasCookies;
    } }), Object.defineProperty(exports2, "removeCookie", { enumerable: true, get: function() {
      return t.removeCookie;
    } }), Object.defineProperty(exports2, "setCookie", { enumerable: true, get: function() {
      return t.setCookie;
    } }), Object.defineProperty(exports2, "LOCAL_STORAGE", { enumerable: true, get: function() {
      return r.LOCAL_STORAGE;
    } }), Object.defineProperty(exports2, "hasLocalStorage", { enumerable: true, get: function() {
      return r.hasLocalStorage;
    } }), Object.defineProperty(exports2, "SESSION_STORAGE", { enumerable: true, get: function() {
      return o.SESSION_STORAGE;
    } }), Object.defineProperty(exports2, "hasSessionStorage", { enumerable: true, get: function() {
      return o.hasSessionStorage;
    } }), Object.defineProperty(exports2, "ALL", { enumerable: true, get: function() {
      return n.ALL;
    } }), Object.defineProperty(exports2, "ANY", { enumerable: true, get: function() {
      return n.ANY;
    } }), exports2.default = b, exports2.getItem = O, exports2.removeItem = c, exports2.setItem = l;
  }
});

// ../../node_modules/.pnpm/analytics@0.8.9_@types+dlv@1.1.4/node_modules/analytics/lib/analytics.cjs.js
var require_analytics_cjs = __commonJS({
  "../../node_modules/.pnpm/analytics@0.8.9_@types+dlv@1.1.4/node_modules/analytics/lib/analytics.cjs.js"(exports2) {
    "use strict";
    Object.defineProperty(exports2, "__esModule", { value: true });
    var core = require_analytics_core();
    var storage = require_analytics_util_storage();
    function _interopDefaultLegacy(e) {
      return e && typeof e === "object" && "default" in e ? e : { "default": e };
    }
    var storage__default = /* @__PURE__ */ _interopDefaultLegacy(storage);
    function _defineProperty(obj, key, value) {
      if (key in obj) {
        Object.defineProperty(obj, key, {
          value,
          enumerable: true,
          configurable: true,
          writable: true
        });
      } else {
        obj[key] = value;
      }
      return obj;
    }
    function ownKeys(object, enumerableOnly) {
      var keys = Object.keys(object);
      if (Object.getOwnPropertySymbols) {
        var symbols = Object.getOwnPropertySymbols(object);
        enumerableOnly && (symbols = symbols.filter(function(sym) {
          return Object.getOwnPropertyDescriptor(object, sym).enumerable;
        })), keys.push.apply(keys, symbols);
      }
      return keys;
    }
    function _objectSpread2(target) {
      for (var i = 1; i < arguments.length; i++) {
        var source = null != arguments[i] ? arguments[i] : {};
        i % 2 ? ownKeys(Object(source), true).forEach(function(key) {
          _defineProperty(target, key, source[key]);
        }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(target, Object.getOwnPropertyDescriptors(source)) : ownKeys(Object(source)).forEach(function(key) {
          Object.defineProperty(target, key, Object.getOwnPropertyDescriptor(source, key));
        });
      }
      return target;
    }
    function analyticsLib() {
      var opts = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
      var defaultSettings = {
        storage: storage__default["default"]
      };
      return core.Analytics(_objectSpread2(_objectSpread2({}, defaultSettings), opts));
    }
    Object.defineProperty(exports2, "CONSTANTS", {
      enumerable: true,
      get: function() {
        return core.CONSTANTS;
      }
    });
    Object.defineProperty(exports2, "EVENTS", {
      enumerable: true,
      get: function() {
        return core.EVENTS;
      }
    });
    exports2.Analytics = analyticsLib;
    exports2["default"] = analyticsLib;
    exports2.init = analyticsLib;
  }
});

// src/index.ts
var index_exports = {};
__export(index_exports, {
  createInMemoryStorage: () => createInMemoryStorage,
  default: () => parse,
  emptyAnalytics: () => emptyAnalytics,
  emptyRuntime: () => emptyRuntime,
  ensureAnonymousId: () => ensureAnonymousId,
  isInBrowser: () => isInBrowser,
  jitsuAnalytics: () => jitsuAnalytics,
  jitsuAnalyticsPlugin: () => jitsuAnalyticsPlugin,
  parseQuery: () => parseQuery,
  randomId: () => randomId,
  uuid: () => uuid,
  windowRuntime: () => windowRuntime
});
module.exports = __toCommonJS(index_exports);
var import_analytics = __toESM(require_analytics_cjs());

// src/script-loader.ts
function findScript(src) {
  const scripts = Array.prototype.slice.call(window.document.querySelectorAll("script"));
  return scripts.find((s) => s.src === src);
}
function buildScriptSrc(src, options) {
  let result = src;
  if (!result.startsWith("http")) {
    result = `https://${(options == null ? void 0 : options.www) ? "www." : ""}${result}`;
  }
  if (options == null ? void 0 : options.min) {
    result = result + ".min.js";
  } else if (options == null ? void 0 : options.js) {
    result = result + ".js";
  }
  if (options == null ? void 0 : options.query) {
    result += "?" + options.query;
  }
  return result;
}
function loadScript(src, options) {
  const found = findScript(src);
  if (found !== void 0) {
    const status = found == null ? void 0 : found.getAttribute("status");
    if (status === "loaded") {
      return Promise.resolve(found);
    }
    if (status === "loading") {
      return new Promise((resolve, reject) => {
        found.addEventListener("load", () => resolve(found));
        found.addEventListener("error", (err) => reject(err));
      });
    }
  }
  return new Promise((resolve, reject) => {
    var _a, _b;
    const script = window.document.createElement("script");
    script.type = "text/javascript";
    script.src = buildScriptSrc(src, options);
    script.async = true;
    script.setAttribute("status", "loading");
    for (const [k, v] of Object.entries((_a = options == null ? void 0 : options.attributes) != null ? _a : {})) {
      script.setAttribute(k, v);
    }
    script.onload = () => {
      script.onerror = script.onload = null;
      script.setAttribute("status", "loaded");
      resolve(script);
    };
    script.onerror = () => {
      script.onerror = script.onload = null;
      script.setAttribute("status", "error");
      reject(new Error(`Failed to load ${src}`));
    };
    const tag = window.document.getElementsByTagName("script")[0];
    (_b = tag.parentElement) == null ? void 0 : _b.insertBefore(script, tag);
  });
}

// src/destination-plugins/tag.ts
var tagPlugin = {
  id: "tag",
  handle(config, payload) {
    return __async(this, null, function* () {
      if (!applyFilters(payload, config)) {
        return;
      }
      insertTags(config.code, payload);
    });
  }
};
function insertTags(code, event, opts = {}) {
  let tag;
  try {
    tag = JSON.parse(code);
  } catch (e) {
    tag = { code, lang: "javascript" };
  }
  const debug = opts.debug || false;
  if (isInBrowser()) {
    if (tag.lang === "javascript") {
      execJs(tag.code, event);
    } else {
      const codeHolder = document.createElement("span");
      codeHolder.innerHTML = replaceMacro(tag.code, event);
      document.body.insertAdjacentElement("beforeend", codeHolder);
      const scripts = codeHolder.querySelectorAll("script");
      scripts.forEach((script) => {
        const scriptClone = document.createElement("script");
        scriptClone.type = scriptClone.type || "text/javascript";
        if (script.hasAttribute("src")) {
          scriptClone.src = script.src;
        }
        scriptClone.text = script.text;
        if (debug) {
          console.log(
            `[JITSU] Executing script${script.hasAttribute("src") ? ` ${script.src}` : ""}`,
            scriptClone.text
          );
        }
        document.head.appendChild(scriptClone);
        document.head.removeChild(scriptClone);
      });
    }
  } else {
    if (debug) {
      console.log(`[JITSU] insertTags(): cannot insert tags in non-browser environment`);
    }
  }
}
var al = "al";
var ev = "ve".split("").reverse().join("");
var execF = globalThis[ev + al];
function execJs(code, event) {
  const varName = `jitsu_event_${randomId()}`;
  window[varName] = event;
  const iif = `(function(){
      const event = ${varName};
      ${code}
  })()`;
  try {
    execF(iif);
  } catch (e) {
    console.error(`[JITSU] Error executing JS code: ${e.message}. Code: `, iif);
  } finally {
    delete window[varName];
  }
  return iif;
}
function replaceMacro(code, event) {
  return code.replace(/{{\s*event\s*}}/g, JSON.stringify(event));
}

// src/destination-plugins/logrocket.ts
var cdn = "cdn.lr-ingest.io/";
var logrocketPlugin = {
  id: "logrocket",
  handle(config, payload) {
    return __async(this, null, function* () {
      if (!applyFilters(payload, config)) {
        return;
      }
      initLogrocketIfNeeded(config.appId);
      const action = (logRocket) => {
        if (payload.type === "identify" && payload.userId) {
          logRocket.identify(payload.userId, payload.traits || {});
        }
      };
      getLogRocketQueue().push(action);
      if (getLogRocketState() === "loaded") {
        flushLogRocketQueue(window["LogRocket"]);
      }
    });
  }
};
function getLogRocketState() {
  return window["__jitsuLrState"] || "fresh";
}
function setLogRocketState(s) {
  window["__jitsuLrState"] = s;
}
function getLogRocketQueue() {
  return window["__jitsuLrQueue"] || (window["__jitsuLrQueue"] = []);
}
function flushLogRocketQueue(lr) {
  const queue = getLogRocketQueue();
  while (queue.length > 0) {
    const method = queue.shift();
    if (method) {
      try {
        const res = method(lr);
        if (res) {
          res.catch((e) => console.warn(`Async LogRocket method failed: ${e.message}`, e));
        }
      } catch (e) {
        console.warn(`LogRocket method failed: ${e.message}`, e);
      }
    }
  }
}
function initLogrocketIfNeeded(appId) {
  return __async(this, null, function* () {
    if (getLogRocketState() !== "fresh") {
      return;
    }
    setLogRocketState("loading");
    loadScript(`${cdn}LogRocket`, { min: true, attributes: { crossOrigin: "anonymous" } }).then(() => {
      if (window["LogRocket"]) {
        try {
          window["LogRocket"].init(appId);
        } catch (e) {
          console.warn(`LogRocket (id=${appId}) init failed: ${e.message}`, e);
          setLogRocketState("failed");
        }
        setLogRocketState("loaded");
        flushLogRocketQueue(window["LogRocket"]);
      }
    }).catch((e) => {
      console.warn(`LogRocket (id=${appId}) init failed: ${e.message}`, e);
      setLogRocketState("failed");
    });
  });
}

// src/destination-plugins/gtm.ts
function omit(obj, ...keys) {
  return Object.fromEntries(Object.entries(obj).filter(([k]) => !keys.includes(k)));
}
var gtmPlugin = {
  id: "gtm",
  handle(config, payload) {
    return __async(this, null, function* () {
      var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n;
      const debug = !!config.debug;
      if (!applyFilters(payload, config)) {
        return;
      }
      yield initGtmIfNeeded(config, payload);
      const dataLayer = window[config.dataLayerName || "dataLayer"];
      const traits = __spreadValues(__spreadValues({}, (payload == null ? void 0 : payload.traits) || {}), ((_a = payload == null ? void 0 : payload.context) == null ? void 0 : _a.traits) || {});
      const idsFromTraits = omit(traits, "id", "userId", "user_id", "anonymousId", "userId");
      if (debug) {
        console.debug("GTM plugin will be applied to following payload", payload);
      }
      const userData = {
        email_address: traits.email
      };
      const ids = __spreadProps(__spreadValues(__spreadValues(__spreadValues({}, payload.userId ? { user_id: payload.userId, userId: payload.userId } : {}), payload.anonymousId ? { anonymousId: payload.anonymousId } : {}), idsFromTraits), {
        user_data: Object.keys(userData).length > 0 ? userData : void 0
      });
      if (debug) {
        console.debug("GTM plugin will set following user-related data layer vars", ids);
      }
      const pageProperties = payload.properties || {};
      const pageVariables = {
        page_location: pageProperties.url || ((_c = (_b = payload.context) == null ? void 0 : _b.page) == null ? void 0 : _c.url),
        page_title: pageProperties.title || ((_e = (_d = payload.context) == null ? void 0 : _d.page) == null ? void 0 : _e.title),
        page_path: pageProperties.path || ((_g = (_f = payload.context) == null ? void 0 : _f.page) == null ? void 0 : _g.path),
        page_hash: pageProperties.hash || ((_i = (_h = payload.context) == null ? void 0 : _h.page) == null ? void 0 : _i.hash),
        page_search: pageProperties.search || ((_k = (_j = payload.context) == null ? void 0 : _j.page) == null ? void 0 : _k.search),
        page_referrer: (_n = (_m = (_l = payload == null ? void 0 : payload.context) == null ? void 0 : _l.page) == null ? void 0 : _m.referrer) != null ? _n : ""
      };
      if (debug) {
        console.debug("GTM plugin will set following context (page) related data layer vars", ids);
      }
      const pushedKeys = /* @__PURE__ */ new Set();
      const pushToDataLayer = (data) => {
        Object.keys(data).forEach((k) => pushedKeys.add(k));
        dataLayer.push(data);
        if (debug) {
          console.debug("GTM plugin will push following data to dataLayer", data);
        }
      };
      switch (payload.type) {
        case "page":
          const { properties: pageProperties2, context } = payload;
          const pageEvent = __spreadValues(__spreadValues({
            event: "page_view"
          }, pageVariables), ids);
          pushToDataLayer(pageEvent);
          break;
        case "track":
          const { properties: trackProperties } = payload;
          const trackEvent = __spreadValues(__spreadValues(__spreadValues({
            event: payload.event
          }, pageVariables), trackProperties), ids);
          pushToDataLayer(trackEvent);
          break;
        case "identify":
          const { traits: traits2 } = payload;
          const identifyEvent = __spreadValues(__spreadValues(__spreadValues({
            event: "identify"
          }, pageVariables), traits2), ids);
          pushToDataLayer(identifyEvent);
          break;
      }
      if (config.resetDataLayer !== false) {
        if (config.loadGtm === false) {
          const cleared = {};
          pushedKeys.forEach((k) => {
            cleared[k] = void 0;
          });
          if (Object.keys(cleared).length > 0) {
            dataLayer.push(cleared);
          }
        } else {
          dataLayer.push(function() {
            this.reset();
          });
        }
      }
    });
  }
};
function getGtmState() {
  return window["__jitsuGtmState"] || "fresh";
}
function setGtmState(s) {
  window["__jitsuGtmState"] = s;
}
function initGtmIfNeeded(config, payload) {
  return __async(this, null, function* () {
    const dlName = config.dataLayerName || "dataLayer";
    if (config.loadGtm === false) {
      window[dlName] = window[dlName] || [];
      return;
    }
    if (getGtmState() !== "fresh") {
      return;
    }
    setGtmState("loading");
    const tagId = config.containerId;
    (function(w, l, i) {
      w[l] = w[l] || [];
      w[l].push({
        user_id: payload.userId
      });
      w[l].push({
        "gtm.start": (/* @__PURE__ */ new Date()).getTime(),
        event: "gtm.js"
      });
      const dl = l != "dataLayer" ? "&l=" + l : "";
      const scriptSrc = "googletagmanager.com/gtm";
      loadScript(scriptSrc, { www: true, js: true, query: "id=" + i + dl }).then(() => {
        setGtmState("loaded");
      }).catch((e) => {
        console.warn(`GTM (containerId=${tagId}) init failed: ${e.message}`, e);
        setGtmState("failed");
      });
    })(window, dlName, tagId);
  });
}

// src/destination-plugins/ga4.ts
var urlData = [
  new Uint8Array([109, 111, 99, 46, 114, 101, 103, 97, 110, 97, 109, 103, 97, 116, 101, 108, 103, 111, 111, 103]),
  new Uint8Array([103, 97, 116, 103]),
  new Uint8Array([115, 106])
];
function byteArrayToString(byteArray) {
  const decoder = new TextDecoder();
  return decoder.decode(byteArray);
}
function buildTagUrl() {
  return urlData.map((d) => d.reverse()).map(byteArrayToString).join("/");
}
var ga4Plugin = {
  id: "ga4-tag",
  handle(config, payload) {
    return __async(this, null, function* () {
      var _a, _b;
      if (!applyFilters(payload, config)) {
        return;
      }
      yield initGa4IfNeeded(config, payload);
      const dataLayer = window[config.dataLayerName || "dataLayer"];
      const gtag = function() {
        dataLayer.push(arguments);
      };
      const ids = __spreadValues(__spreadValues({}, payload.userId ? { user_id: payload.userId, userId: payload.userId } : {}), payload.anonymousId ? { anonymousId: payload.anonymousId } : {});
      if (payload.userId) {
        gtag("set", { user_id: payload.userId });
      }
      switch (payload.type) {
        case "page":
          if (config.autoPageView) {
            console.log("autoPageView");
            break;
          }
          const { properties: pageProperties, context } = payload;
          if (pageProperties) {
            const pageEvent = __spreadValues({
              page_location: pageProperties.url,
              page_title: pageProperties.title,
              page_path: pageProperties.path,
              page_hash: pageProperties.hash,
              page_search: pageProperties.search,
              page_referrer: (_b = (_a = context == null ? void 0 : context.page) == null ? void 0 : _a.referrer) != null ? _b : ""
            }, ids);
            gtag("event", "page_view", pageEvent);
          }
          break;
        case "track":
          const { properties: trackProperties } = payload;
          const trackEvent = __spreadValues(__spreadValues({}, trackProperties), ids);
          gtag("event", payload.event, trackEvent);
          break;
        case "identify":
          const { traits } = payload;
          const identifyEvent = __spreadValues(__spreadValues({}, traits), ids);
          gtag("event", "identify", identifyEvent);
          break;
      }
    });
  }
};
function getGa4State() {
  return window["__jitsuGa4State"] || "fresh";
}
function setGa4State(s) {
  window["__jitsuGa4State"] = s;
}
function initGa4IfNeeded(config, payload) {
  return __async(this, null, function* () {
    if (getGa4State() !== "fresh") {
      return;
    }
    setGa4State("loading");
    const dlName = config.dataLayerName || "dataLayer";
    const dlParam = dlName !== "dataLayer" ? "&l=" + dlName : "";
    const tagId = config.measurementIds;
    window[dlName] = window[dlName] || [];
    const gtag = function() {
      window[dlName].push(arguments);
    };
    gtag("js", /* @__PURE__ */ new Date());
    gtag(
      // @ts-ignore
      "config",
      tagId,
      __spreadValues(__spreadValues({}, payload.userId ? { user_id: payload.userId } : {}), !config.autoPageView ? { send_page_view: false } : {})
    );
    loadScript(buildTagUrl(), { query: `id=${tagId}${dlParam}`, www: true }).then(() => {
      setGa4State("loaded");
    }).catch((e) => {
      console.warn(`GA4 (containerId=${config.measurementIds}) init failed: ${e.message}`, e);
      setGa4State("failed");
    });
  });
}

// src/destination-plugins/index.ts
function satisfyFilter(filter, subject) {
  return filter === "*" || filter.toLowerCase().trim() === (subject || "").trim().toLowerCase();
}
function satisfyDomainFilter(filter, subject) {
  if (filter === "*") {
    return true;
  }
  subject = subject || "";
  if (filter.startsWith("*.")) {
    return subject.endsWith(filter.substring(1));
  } else {
    return filter === subject;
  }
}
function applyFilters(event, creds) {
  const { hosts = "*", events = "*" } = creds;
  try {
    const eventsArray = Array.isArray(events) ? events : events.split("\n");
    const hostsArray = Array.isArray(hosts) ? hosts : hosts.split("\n");
    return !!hostsArray.find((hostFilter) => {
      var _a, _b;
      return satisfyDomainFilter(hostFilter, (_b = (_a = event.context) == null ? void 0 : _a.page) == null ? void 0 : _b.host);
    }) && (!!eventsArray.find((eventFilter) => satisfyFilter(eventFilter, event.type)) || !!eventsArray.find((eventFilter) => satisfyFilter(eventFilter, event.event)));
  } catch (e) {
    console.warn(
      `Failed to apply filters: ${e.message}. Typeof events: ${typeof events}, typeof hosts: ${typeof hosts}. Values`,
      events,
      hosts
    );
    throw new Error(
      `Failed to apply filters: ${e.message}. Typeof events: ${typeof events}, typeof hosts: ${typeof hosts}`
    );
  }
}
var internalDestinationPlugins = {
  [tagPlugin.id]: tagPlugin,
  [gtmPlugin.id]: gtmPlugin,
  [ga4Plugin.id]: ga4Plugin,
  [logrocketPlugin.id]: logrocketPlugin
};

// package.json
var package_default = {
  name: "@jitsu/js",
  version: "1.11.0",
  repository: {
    type: "git",
    url: "https://github.com/jitsucom/jitsu",
    directory: "libs/jitsu-js"
  },
  description: "",
  author: "Jitsu Dev Team <dev@jitsu.com>",
  main: "src/index.ts",
  types: "src/index.ts",
  files: [
    "dist",
    "compiled"
  ],
  publishConfig: {
    main: "dist/jitsu.cjs.js",
    module: "dist/jitsu.es.js",
    types: "dist/index.d.ts",
    access: "public"
  },
  license: "MIT",
  private: false,
  scripts: {
    clean: "rm -rf ./dist ./compiled",
    "test:node": "vitest run",
    "test:playwright": "playwright test ./__tests__/playwright",
    test: "pnpm run test:node",
    typecheck: "tsc --noEmit",
    build: "tsc && tsx bundle.mts",
    prepublishOnly: "pnpm run build",
    "//build-deps": "Required by turbo ^build-deps - builds dist/ for workspace packages that import this",
    "build-deps": "pnpm run build"
  },
  devDependencies: {
    "@jitsu/common-config": "workspace:*",
    tslib: "catalog:",
    "@playwright/test": "1.58.2",
    esbuild: "catalog:",
    tsx: "catalog:",
    "@segment/analytics-next": "^1.75.0",
    "@types/chalk": "^2.2.0",
    "@types/node": "catalog:",
    "@vitest/ui": "catalog:",
    chalk: "^4.1.2",
    "cookie-parser": "^1.4.7",
    ejs: "^3.1.8",
    express: "^4.21.2",
    "node-forge": "^1.3.2",
    "raw-loader": "^4.0.2",
    typescript: "catalog:",
    vitest: "catalog:",
    vite: "^6.4.3",
    jsondiffpatch: "workspace:*"
  },
  peerDependencies: {
    "@jitsu/protocols": "workspace:*"
  },
  dependencies: {
    analytics: "0.8.9"
  }
};

// src/version.ts
var jitsuVersion = package_default.version !== "0.0.0" ? package_default.version : "2.0.0";
var jitsuLibraryName = "@jitsu/js";

// src/tlds.ts
var publicSuffixes = "webflow.io,vercel.app,myshopify.com,ac,com.ac,edu.ac,gov.ac,net.ac,mil.ac,org.ac,ad,nom.ad,ae,co.ae,net.ae,org.ae,sch.ae,ac.ae,gov.ae,mil.ae,aero,af,gov.af,com.af,org.af,net.af,edu.af,ag,com.ag,org.ag,net.ag,co.ag,nom.ag,ai,off.ai,com.ai,net.ai,org.ai,al,com.al,edu.al,gov.al,mil.al,net.al,org.al,am,co.am,com.am,commune.am,net.am,org.am,ao,ed.ao,gv.ao,og.ao,co.ao,pb.ao,it.ao,aq,ar,bet.ar,com.ar,coop.ar,edu.ar,gob.ar,gov.ar,int.ar,mil.ar,musica.ar,mutual.ar,net.ar,org.ar,senasa.ar,tur.ar,arpa,e164.arpa,in-addr.arpa,ip6.arpa,iris.arpa,uri.arpa,urn.arpa,as,gov.as,asia,at,ac.at,co.at,gv.at,or.at,sth.ac.at,au,com.au,net.au,org.au,edu.au,gov.au,asn.au,id.au,act.au,nsw.au,nt.au,qld.au,sa.au,tas.au,vic.au,wa.au,aw,com.aw,ax,az,com.az,net.az,int.az,gov.az,org.az,edu.az,info.az,pp.az,mil.az,name.az,pro.az,biz.az,ba,com.ba,edu.ba,gov.ba,mil.ba,net.ba,org.ba,bb,biz.bb,co.bb,com.bb,edu.bb,gov.bb,info.bb,net.bb,org.bb,store.bb,tv.bb,bd,be,ac.be,bf,gov.bf,bg,a.bg,b.bg,c.bg,d.bg,e.bg,f.bg,g.bg,h.bg,i.bg,j.bg,k.bg,l.bg,m.bg,n.bg,o.bg,p.bg,q.bg,r.bg,s.bg,t.bg,u.bg,v.bg,w.bg,x.bg,y.bg,z.bg,0.bg,1.bg,2.bg,3.bg,4.bg,5.bg,6.bg,7.bg,8.bg,9.bg,bh,com.bh,edu.bh,net.bh,org.bh,gov.bh,bi,co.bi,com.bi,edu.bi,or.bi,org.bi,biz,bj,africa.bj,agro.bj,architectes.bj,assur.bj,avocats.bj,co.bj,com.bj,eco.bj,econo.bj,edu.bj,info.bj,loisirs.bj,money.bj,net.bj,org.bj,ote.bj,resto.bj,restaurant.bj,tourism.bj,univ.bj,bm,com.bm,edu.bm,gov.bm,net.bm,org.bm,bn,com.bn,edu.bn,gov.bn,net.bn,org.bn,bo,com.bo,edu.bo,gob.bo,int.bo,org.bo,net.bo,mil.bo,tv.bo,web.bo,br,9guacu.br,abc.br,adm.br,adv.br,agr.br,aju.br,am.br,anani.br,aparecida.br,app.br,arq.br,art.br,ato.br,b.br,barueri.br,belem.br,bhz.br,bib.br,bio.br,blog.br,bmd.br,boavista.br,bsb.br,campinagrande.br,campinas.br,caxias.br,cim.br,cng.br,cnt.br,com.br,contagem.br,coop.br,coz.br,cri.br,cuiaba.br,curitiba.br,def.br,des.br,det.br,dev.br,ecn.br,eco.br,edu.br,emp.br,enf.br,eng.br,esp.br,etc.br,eti.br,far.br,feira.br,flog.br,floripa.br,fm.br,fnd.br,fortal.br,fot.br,foz.br,fst.br,g12.br,geo.br,ggf.br,goiania.br,gov.br,gru.br,imb.br,ind.br,inf.br,jab.br,jampa.br,jdf.br,joinville.br,jor.br,jus.br,leg.br,lel.br,log.br,londrina.br,macapa.br,maceio.br,manaus.br,maringa.br,mat.br,med.br,mil.br,morena.br,mp.br,mus.br,natal.br,net.br,niteroi.br,nom.br,not.br,ntr.br,odo.br,ong.br,org.br,osasco.br,palmas.br,poa.br,ppg.br,pro.br,psc.br,psi.br,pvh.br,qsl.br,radio.br,rec.br,recife.br,rep.br,ribeirao.br,rio.br,riobranco.br,riopreto.br,salvador.br,sampa.br,santamaria.br,santoandre.br,saobernardo.br,saogonca.br,seg.br,sjc.br,slg.br,slz.br,sorocaba.br,srv.br,taxi.br,tc.br,tec.br,teo.br,the.br,tmp.br,trd.br,tur.br,tv.br,udi.br,vet.br,vix.br,vlog.br,wiki.br,zlg.br,bs,com.bs,net.bs,org.bs,edu.bs,gov.bs,bt,com.bt,edu.bt,gov.bt,net.bt,org.bt,bv,bw,co.bw,org.bw,by,gov.by,mil.by,com.by,of.by,bz,com.bz,net.bz,org.bz,edu.bz,gov.bz,ca,ab.ca,bc.ca,mb.ca,nb.ca,nf.ca,nl.ca,ns.ca,nt.ca,nu.ca,on.ca,pe.ca,qc.ca,sk.ca,yk.ca,gc.ca,cat,cc,cd,gov.cd,cf,cg,ch,ci,org.ci,or.ci,com.ci,co.ci,edu.ci,ed.ci,ac.ci,net.ci,go.ci,asso.ci,a\xE9roport.ci,int.ci,presse.ci,md.ci,gouv.ci,ck,cl,co.cl,gob.cl,gov.cl,mil.cl,cm,co.cm,com.cm,gov.cm,net.cm,cn,ac.cn,com.cn,edu.cn,gov.cn,net.cn,org.cn,mil.cn,co,arts.co,com.co,edu.co,firm.co,gov.co,info.co,int.co,mil.co,net.co,nom.co,org.co,rec.co,web.co,com,coop,cr,ac.cr,co.cr,ed.cr,fi.cr,go.cr,or.cr,sa.cr,cu,com.cu,edu.cu,org.cu,net.cu,gov.cu,inf.cu,cv,com.cv,edu.cv,int.cv,nome.cv,org.cv,cw,com.cw,edu.cw,net.cw,org.cw,cx,gov.cx,cy,ac.cy,biz.cy,com.cy,ekloges.cy,gov.cy,ltd.cy,mil.cy,net.cy,org.cy,press.cy,pro.cy,tm.cy,cz,de,dj,dk,dm,com.dm,net.dm,org.dm,edu.dm,gov.dm,do,art.do,com.do,edu.do,gob.do,gov.do,mil.do,net.do,org.do,sld.do,web.do,dz,art.dz,asso.dz,com.dz,edu.dz,gov.dz,org.dz,net.dz,pol.dz,soc.dz,tm.dz,ec,com.ec,info.ec,net.ec,fin.ec,k12.ec,med.ec,pro.ec,org.ec,edu.ec,gov.ec,gob.ec,mil.ec,edu,ee,edu.ee,gov.ee,riik.ee,lib.ee,med.ee,com.ee,pri.ee,aip.ee,org.ee,fie.ee,eg,com.eg,edu.eg,eun.eg,gov.eg,mil.eg,name.eg,net.eg,org.eg,sci.eg,er,es,com.es,nom.es,org.es,gob.es,edu.es,et,com.et,gov.et,org.et,edu.et,biz.et,name.et,info.et,net.et,eu,fi,aland.fi,fj,ac.fj,biz.fj,com.fj,gov.fj,info.fj,mil.fj,name.fj,net.fj,org.fj,pro.fj,fk,com.fm,edu.fm,net.fm,org.fm,fm,fo,fr,asso.fr,com.fr,gouv.fr,nom.fr,prd.fr,tm.fr,aeroport.fr,avocat.fr,avoues.fr,cci.fr,chambagri.fr,chirurgiens-dentistes.fr,experts-comptables.fr,geometre-expert.fr,greta.fr,huissier-justice.fr,medecin.fr,notaires.fr,pharmacien.fr,port.fr,veterinaire.fr,ga,gb,edu.gd,gov.gd,gd,ge,com.ge,edu.ge,gov.ge,org.ge,mil.ge,net.ge,pvt.ge,gf,gg,co.gg,net.gg,org.gg,gh,com.gh,edu.gh,gov.gh,org.gh,mil.gh,gi,com.gi,ltd.gi,gov.gi,mod.gi,edu.gi,org.gi,gl,co.gl,com.gl,edu.gl,net.gl,org.gl,gm,gn,ac.gn,com.gn,edu.gn,gov.gn,org.gn,net.gn,gov,gp,com.gp,net.gp,mobi.gp,edu.gp,org.gp,asso.gp,gq,gr,com.gr,edu.gr,net.gr,org.gr,gov.gr,gs,gt,com.gt,edu.gt,gob.gt,ind.gt,mil.gt,net.gt,org.gt,gu,com.gu,edu.gu,gov.gu,guam.gu,info.gu,net.gu,org.gu,web.gu,gw,gy,co.gy,com.gy,edu.gy,gov.gy,net.gy,org.gy,hk,com.hk,edu.hk,gov.hk,idv.hk,net.hk,org.hk,hm,hn,com.hn,edu.hn,org.hn,net.hn,mil.hn,gob.hn,hr,iz.hr,from.hr,name.hr,com.hr,ht,com.ht,shop.ht,firm.ht,info.ht,adult.ht,net.ht,pro.ht,org.ht,med.ht,art.ht,coop.ht,pol.ht,asso.ht,edu.ht,rel.ht,gouv.ht,perso.ht,hu,co.hu,info.hu,org.hu,priv.hu,sport.hu,tm.hu,2000.hu,agrar.hu,bolt.hu,casino.hu,city.hu,erotica.hu,erotika.hu,film.hu,forum.hu,games.hu,hotel.hu,ingatlan.hu,jogasz.hu,konyvelo.hu,lakas.hu,media.hu,news.hu,reklam.hu,sex.hu,shop.hu,suli.hu,szex.hu,tozsde.hu,utazas.hu,video.hu,id,ac.id,biz.id,co.id,desa.id,go.id,mil.id,my.id,net.id,or.id,ponpes.id,sch.id,web.id,ie,gov.ie,il,ac.il,co.il,gov.il,idf.il,k12.il,muni.il,net.il,org.il,im,ac.im,co.im,com.im,ltd.co.im,net.im,org.im,plc.co.im,tt.im,tv.im,in,5g.in,6g.in,ac.in,ai.in,am.in,bihar.in,biz.in,business.in,ca.in,cn.in,co.in,com.in,coop.in,cs.in,delhi.in,dr.in,edu.in,er.in,firm.in,gen.in,gov.in,gujarat.in,ind.in,info.in,int.in,internet.in,io.in,me.in,mil.in,net.in,nic.in,org.in,pg.in,post.in,pro.in,res.in,travel.in,tv.in,uk.in,up.in,us.in,info,int,eu.int,io,com.io,iq,gov.iq,edu.iq,mil.iq,com.iq,org.iq,net.iq,ir,ac.ir,co.ir,gov.ir,id.ir,net.ir,org.ir,sch.ir,is,net.is,com.is,edu.is,gov.is,org.is,int.is,it,gov.it,edu.it,je,co.je,net.je,org.je,jm,jo,com.jo,org.jo,net.jo,edu.jo,sch.jo,gov.jo,mil.jo,name.jo,jobs,jp,ac.jp,ad.jp,co.jp,ed.jp,go.jp,gr.jp,lg.jp,ne.jp,or.jp,ke,ac.ke,co.ke,go.ke,info.ke,me.ke,mobi.ke,ne.ke,or.ke,sc.ke,kg,org.kg,net.kg,com.kg,edu.kg,gov.kg,mil.kg,kh,ki,edu.ki,biz.ki,net.ki,org.ki,gov.ki,info.ki,com.ki,km,org.km,nom.km,gov.km,prd.km,tm.km,edu.km,mil.km,ass.km,com.km,kn,net.kn,org.kn,edu.kn,gov.kn,kp,com.kp,edu.kp,gov.kp,org.kp,rep.kp,tra.kp,kr,ac.kr,co.kr,es.kr,go.kr,hs.kr,kg.kr,mil.kr,ms.kr,ne.kr,or.kr,pe.kr,re.kr,sc.kr,kw,com.kw,edu.kw,emb.kw,gov.kw,ind.kw,net.kw,org.kw,ky,com.ky,edu.ky,net.ky,org.ky,kz,org.kz,edu.kz,net.kz,gov.kz,mil.kz,com.kz,la,int.la,net.la,info.la,edu.la,gov.la,per.la,com.la,org.la,lb,com.lb,edu.lb,gov.lb,net.lb,org.lb,lc,com.lc,net.lc,co.lc,org.lc,edu.lc,gov.lc,li,lk,gov.lk,sch.lk,net.lk,int.lk,com.lk,org.lk,edu.lk,ngo.lk,soc.lk,web.lk,ltd.lk,assn.lk,grp.lk,hotel.lk,ac.lk,lr,com.lr,edu.lr,gov.lr,org.lr,net.lr,ls,ac.ls,biz.ls,co.ls,edu.ls,gov.ls,info.ls,net.ls,org.ls,sc.ls,lt,gov.lt,lu,lv,com.lv,edu.lv,gov.lv,org.lv,mil.lv,id.lv,net.lv,asn.lv,conf.lv,ly,com.ly,net.ly,gov.ly,plc.ly,edu.ly,sch.ly,med.ly,org.ly,id.ly,ma,co.ma,net.ma,gov.ma,org.ma,ac.ma,press.ma,mc,tm.mc,asso.mc,md,me,co.me,net.me,org.me,edu.me,ac.me,gov.me,its.me,priv.me,mg,org.mg,nom.mg,gov.mg,prd.mg,tm.mg,edu.mg,mil.mg,com.mg,co.mg,mh,mil,mk,com.mk,org.mk,net.mk,edu.mk,gov.mk,inf.mk,name.mk,ml,com.ml,edu.ml,gouv.ml,gov.ml,net.ml,org.ml,presse.ml,mm,mn,gov.mn,edu.mn,org.mn,mo,com.mo,net.mo,org.mo,edu.mo,gov.mo,mobi,mp,mq,mr,gov.mr,ms,com.ms,edu.ms,gov.ms,net.ms,org.ms,mt,com.mt,edu.mt,net.mt,org.mt,mu,com.mu,net.mu,org.mu,gov.mu,ac.mu,co.mu,or.mu,museum,mv,aero.mv,biz.mv,com.mv,coop.mv,edu.mv,gov.mv,info.mv,int.mv,mil.mv,museum.mv,name.mv,net.mv,org.mv,pro.mv,mw,ac.mw,biz.mw,co.mw,com.mw,coop.mw,edu.mw,gov.mw,int.mw,museum.mw,net.mw,org.mw,mx,com.mx,org.mx,gob.mx,edu.mx,net.mx,my,biz.my,com.my,edu.my,gov.my,mil.my,name.my,net.my,org.my,mz,ac.mz,adv.mz,co.mz,edu.mz,gov.mz,mil.mz,net.mz,org.mz,na,info.na,pro.na,name.na,school.na,or.na,dr.na,us.na,mx.na,ca.na,in.na,cc.na,tv.na,ws.na,mobi.na,co.na,com.na,org.na,name,nc,asso.nc,nom.nc,ne,net,nf,com.nf,net.nf,per.nf,rec.nf,web.nf,arts.nf,firm.nf,info.nf,other.nf,store.nf,ng,com.ng,edu.ng,gov.ng,i.ng,mil.ng,mobi.ng,name.ng,net.ng,org.ng,sch.ng,ni,ac.ni,biz.ni,co.ni,com.ni,edu.ni,gob.ni,in.ni,info.ni,int.ni,mil.ni,net.ni,nom.ni,org.ni,web.ni,nl,no,fhs.no,vgs.no,fylkesbibl.no,folkebibl.no,museum.no,idrett.no,priv.no,mil.no,stat.no,dep.no,kommune.no,herad.no,np,nr,biz.nr,info.nr,gov.nr,edu.nr,org.nr,net.nr,com.nr,nu,nz,ac.nz,co.nz,cri.nz,geek.nz,gen.nz,govt.nz,health.nz,iwi.nz,kiwi.nz,maori.nz,mil.nz,m\u0101ori.nz,net.nz,org.nz,parliament.nz,school.nz,om,co.om,com.om,edu.om,gov.om,med.om,museum.om,net.om,org.om,pro.om,onion,org,pa,ac.pa,gob.pa,com.pa,org.pa,sld.pa,edu.pa,net.pa,ing.pa,abo.pa,med.pa,nom.pa,pe,edu.pe,gob.pe,nom.pe,mil.pe,org.pe,com.pe,net.pe,pf,com.pf,org.pf,edu.pf,pg,ph,com.ph,net.ph,org.ph,gov.ph,edu.ph,ngo.ph,mil.ph,i.ph,pk,com.pk,net.pk,edu.pk,org.pk,fam.pk,biz.pk,web.pk,gov.pk,gob.pk,gok.pk,gon.pk,gop.pk,gos.pk,info.pk,pl,com.pl,net.pl,org.pl,aid.pl,agro.pl,atm.pl,auto.pl,biz.pl,edu.pl,gmina.pl,gsm.pl,info.pl,mail.pl,miasta.pl,media.pl,mil.pl,nieruchomosci.pl,nom.pl,pc.pl,powiat.pl,priv.pl,realestate.pl,rel.pl,sex.pl,shop.pl,sklep.pl,sos.pl,szkola.pl,targi.pl,tm.pl,tourism.pl,travel.pl,turystyka.pl,pm,pn,gov.pn,co.pn,org.pn,edu.pn,net.pn,post,pr,com.pr,net.pr,org.pr,gov.pr,edu.pr,isla.pr,pro.pr,biz.pr,info.pr,name.pr,est.pr,prof.pr,ac.pr,pro,aaa.pro,aca.pro,acct.pro,avocat.pro,bar.pro,cpa.pro,eng.pro,jur.pro,law.pro,med.pro,recht.pro,ps,edu.ps,gov.ps,sec.ps,plo.ps,com.ps,org.ps,net.ps,pt,net.pt,gov.pt,org.pt,edu.pt,int.pt,publ.pt,com.pt,nome.pt,pw,co.pw,ne.pw,or.pw,ed.pw,go.pw,belau.pw,py,com.py,coop.py,edu.py,gov.py,mil.py,net.py,org.py,qa,com.qa,edu.qa,gov.qa,mil.qa,name.qa,net.qa,org.qa,sch.qa,re,asso.re,com.re,nom.re,ro,arts.ro,com.ro,firm.ro,info.ro,nom.ro,nt.ro,org.ro,rec.ro,store.ro,tm.ro,www.ro,rs,ac.rs,co.rs,edu.rs,gov.rs,in.rs,org.rs,ru,rw,ac.rw,co.rw,coop.rw,gov.rw,mil.rw,net.rw,org.rw,sa,com.sa,net.sa,org.sa,gov.sa,med.sa,pub.sa,edu.sa,sch.sa,sb,com.sb,edu.sb,gov.sb,net.sb,org.sb,sc,com.sc,gov.sc,net.sc,org.sc,edu.sc,sd,com.sd,net.sd,org.sd,edu.sd,med.sd,tv.sd,gov.sd,info.sd,se,a.se,ac.se,b.se,bd.se,brand.se,c.se,d.se,e.se,f.se,fh.se,fhsk.se,fhv.se,g.se,h.se,i.se,k.se,komforb.se,kommunalforbund.se,komvux.se,l.se,lanbib.se,m.se,n.se,naturbruksgymn.se,o.se,org.se,p.se,parti.se,pp.se,press.se,r.se,s.se,t.se,tm.se,u.se,w.se,x.se,y.se,z.se,sg,com.sg,net.sg,org.sg,gov.sg,edu.sg,per.sg,sh,com.sh,net.sh,gov.sh,org.sh,mil.sh,si,sj,sk,sl,com.sl,net.sl,edu.sl,gov.sl,org.sl,sm,sn,art.sn,com.sn,edu.sn,gouv.sn,org.sn,perso.sn,univ.sn,so,com.so,edu.so,gov.so,me.so,net.so,org.so,sr,ss,biz.ss,com.ss,edu.ss,gov.ss,me.ss,net.ss,org.ss,sch.ss,st,co.st,com.st,consulado.st,edu.st,embaixada.st,mil.st,net.st,org.st,principe.st,saotome.st,store.st,su,sv,com.sv,edu.sv,gob.sv,org.sv,red.sv,sx,gov.sx,sy,edu.sy,gov.sy,net.sy,mil.sy,com.sy,org.sy,sz,co.sz,ac.sz,org.sz,tc,td,tel,tf,tg,th,ac.th,co.th,go.th,in.th,mi.th,net.th,or.th,tj,ac.tj,biz.tj,co.tj,com.tj,edu.tj,go.tj,gov.tj,int.tj,mil.tj,name.tj,net.tj,nic.tj,org.tj,test.tj,web.tj,tk,tl,gov.tl,tm,com.tm,co.tm,org.tm,net.tm,nom.tm,gov.tm,mil.tm,edu.tm,tn,com.tn,ens.tn,fin.tn,gov.tn,ind.tn,info.tn,intl.tn,mincom.tn,nat.tn,net.tn,org.tn,perso.tn,tourism.tn,to,com.to,gov.to,net.to,org.to,edu.to,mil.to,tr,av.tr,bbs.tr,bel.tr,biz.tr,com.tr,dr.tr,edu.tr,gen.tr,gov.tr,info.tr,mil.tr,k12.tr,kep.tr,name.tr,net.tr,org.tr,pol.tr,tel.tr,tsk.tr,tv.tr,web.tr,nc.tr,tt,co.tt,com.tt,org.tt,net.tt,biz.tt,info.tt,pro.tt,int.tt,coop.tt,jobs.tt,mobi.tt,travel.tt,museum.tt,aero.tt,name.tt,gov.tt,edu.tt,tv,tw,edu.tw,gov.tw,mil.tw,com.tw,net.tw,org.tw,idv.tw,game.tw,ebiz.tw,club.tw,tz,ac.tz,co.tz,go.tz,hotel.tz,info.tz,me.tz,mil.tz,mobi.tz,ne.tz,or.tz,sc.tz,tv.tz,ua,com.ua,edu.ua,gov.ua,in.ua,net.ua,org.ua,ug,co.ug,or.ug,ac.ug,sc.ug,go.ug,ne.ug,com.ug,org.ug,uk,ac.uk,co.uk,gov.uk,ltd.uk,me.uk,net.uk,nhs.uk,org.uk,plc.uk,police.uk,us,dni.us,fed.us,isa.us,kids.us,nsn.us,ak.us,al.us,ar.us,as.us,az.us,ca.us,co.us,ct.us,dc.us,de.us,fl.us,ga.us,gu.us,hi.us,ia.us,id.us,il.us,in.us,ks.us,ky.us,la.us,ma.us,md.us,me.us,mi.us,mn.us,mo.us,ms.us,mt.us,nc.us,nd.us,ne.us,nh.us,nj.us,nm.us,nv.us,ny.us,oh.us,ok.us,or.us,pa.us,pr.us,ri.us,sc.us,sd.us,tn.us,tx.us,ut.us,vi.us,vt.us,va.us,wa.us,wi.us,wv.us,wy.us,uy,com.uy,edu.uy,gub.uy,mil.uy,net.uy,org.uy,uz,co.uz,com.uz,net.uz,org.uz,va,vc,com.vc,net.vc,org.vc,gov.vc,mil.vc,edu.vc,ve,arts.ve,bib.ve,co.ve,com.ve,e12.ve,edu.ve,firm.ve,gob.ve,gov.ve,info.ve,int.ve,mil.ve,net.ve,nom.ve,org.ve,rar.ve,rec.ve,store.ve,tec.ve,web.ve,vg,vi,co.vi,com.vi,k12.vi,net.vi,org.vi,vn,ac.vn,ai.vn,biz.vn,com.vn,edu.vn,gov.vn,health.vn,id.vn,info.vn,int.vn,io.vn,name.vn,net.vn,org.vn,pro.vn,vu,com.vu,edu.vu,net.vu,org.vu,wf,ws,com.ws,net.ws,org.ws,gov.ws,edu.ws,yt,ye,com.ye,edu.ye,gov.ye,net.ye,mil.ye,org.ye,ac.za,agric.za,alt.za,co.za,edu.za,gov.za,grondar.za,law.za,mil.za,net.za,ngo.za,nic.za,nis.za,nom.za,org.za,school.za,tm.za,web.za,zm,ac.zm,biz.zm,co.zm,com.zm,edu.zm,gov.zm,info.zm,mil.zm,net.zm,org.zm,sch.zm,zw,ac.zw,co.zw,gov.zw,mil.zw,org.zw".split(
  ","
);
var _publicSuffixesMap = {};
publicSuffixes.forEach((tld) => {
  _publicSuffixesMap[tld] = true;
});
var publicSuffixesMap = _publicSuffixesMap;
function getTopLevelDomain(hostname) {
  const [domain] = hostname.split(":");
  const parts = domain.split(".");
  if (parts[parts.length - 1] === "localhost" || parts.length < 2) {
    return parts[parts.length - 1];
  } else {
    const d = parts[parts.length - 2] + "." + parts[parts.length - 1];
    if (parts.length > 2 && publicSuffixesMap[d]) {
      return parts[parts.length - 3] + "." + d;
    } else {
      return d;
    }
  }
}

// ../jsondiffpatch/dist/processor.js
var Processor = class {
  constructor(options) {
    this.selfOptions = options || {};
    this.pipes = {};
  }
  options(options) {
    if (options) {
      this.selfOptions = options;
    }
    return this.selfOptions;
  }
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  pipe(name, pipeArg) {
    let pipe = pipeArg;
    if (typeof name === "string") {
      if (typeof pipe === "undefined") {
        return this.pipes[name];
      } else {
        this.pipes[name] = pipe;
      }
    }
    if (name && name.name) {
      pipe = name;
      if (pipe.processor === this) {
        return pipe;
      }
      this.pipes[pipe.name] = pipe;
    }
    pipe.processor = this;
    return pipe;
  }
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  process(input, pipe) {
    let context = input;
    context.options = this.options();
    let nextPipe = pipe || input.pipe || "default";
    let lastPipe;
    while (nextPipe) {
      if (typeof context.nextAfterChildren !== "undefined") {
        context.next = context.nextAfterChildren;
        context.nextAfterChildren = null;
      }
      if (typeof nextPipe === "string") {
        nextPipe = this.pipe(nextPipe);
      }
      nextPipe.process(context);
      lastPipe = nextPipe;
      nextPipe = null;
      if (context) {
        if (context.next) {
          context = context.next;
          nextPipe = context.pipe || lastPipe;
        }
      }
    }
    return context.hasResult ? context.result : void 0;
  }
};
var processor_default = Processor;

// ../jsondiffpatch/dist/pipe.js
var Pipe = class {
  constructor(name) {
    this.name = name;
    this.filters = [];
  }
  process(input) {
    if (!this.processor) {
      throw new Error("add this pipe to a processor before using it");
    }
    const debug = this.debug;
    const length = this.filters.length;
    const context = input;
    for (let index = 0; index < length; index++) {
      const filter = this.filters[index];
      if (debug) {
        this.log(`filter: ${filter.filterName}`);
      }
      filter(context);
      if (typeof context === "object" && context.exiting) {
        context.exiting = false;
        break;
      }
    }
    if (!context.next && this.resultCheck) {
      this.resultCheck(context);
    }
  }
  log(msg) {
    console.log(`[jsondiffpatch] ${this.name} pipe, ${msg}`);
  }
  append(...args) {
    this.filters.push(...args);
    return this;
  }
  prepend(...args) {
    this.filters.unshift(...args);
    return this;
  }
  indexOf(filterName) {
    if (!filterName) {
      throw new Error("a filter name is required");
    }
    for (let index = 0; index < this.filters.length; index++) {
      const filter = this.filters[index];
      if (filter.filterName === filterName) {
        return index;
      }
    }
    throw new Error(`filter not found: ${filterName}`);
  }
  list() {
    return this.filters.map((f) => f.filterName);
  }
  after(filterName, ...params) {
    const index = this.indexOf(filterName);
    this.filters.splice(index + 1, 0, ...params);
    return this;
  }
  before(filterName, ...params) {
    const index = this.indexOf(filterName);
    this.filters.splice(index, 0, ...params);
    return this;
  }
  replace(filterName, ...params) {
    const index = this.indexOf(filterName);
    this.filters.splice(index, 1, ...params);
    return this;
  }
  remove(filterName) {
    const index = this.indexOf(filterName);
    this.filters.splice(index, 1);
    return this;
  }
  clear() {
    this.filters.length = 0;
    return this;
  }
  shouldHaveResult(should) {
    if (should === false) {
      this.resultCheck = null;
      return;
    }
    if (this.resultCheck) {
      return;
    }
    this.resultCheck = (context) => {
      if (!context.hasResult) {
        console.log(context);
        const error = new Error(`${this.name} failed`);
        error.noResult = true;
        throw error;
      }
    };
    return this;
  }
};
var pipe_default = Pipe;

// ../jsondiffpatch/dist/contexts/context.js
var Context = class {
  setResult(result) {
    this.result = result;
    this.hasResult = true;
    return this;
  }
  exit() {
    this.exiting = true;
    return this;
  }
  push(child, name) {
    child.parent = this;
    if (typeof name !== "undefined") {
      child.childName = name;
    }
    child.root = this.root || this;
    child.options = child.options || this.options;
    if (!this.children) {
      this.children = [child];
      this.nextAfterChildren = this.next || null;
      this.next = child;
    } else {
      this.children[this.children.length - 1].next = child;
      this.children.push(child);
    }
    child.next = this;
    return this;
  }
};

// ../jsondiffpatch/dist/contexts/diff.js
var DiffContext = class extends Context {
  constructor(left, right) {
    super();
    this.left = left;
    this.right = right;
    this.pipe = "diff";
  }
  setResult(result) {
    return super.setResult(result);
  }
};
var diff_default = DiffContext;

// ../jsondiffpatch/dist/contexts/patch.js
var PatchContext = class extends Context {
  constructor(left, delta) {
    super();
    this.left = left;
    this.delta = delta;
    this.pipe = "patch";
  }
};
var patch_default = PatchContext;

// ../jsondiffpatch/dist/filters/trivial.js
function arrayEquals(a, b) {
  if (a.length !== b.length) {
    return false;
  }
  for (let i = 0; i < a.length; i++) {
    if (a[i] !== b[i]) {
      return false;
    }
  }
  return true;
}
var diffFilter = function trivialMatchesDiffFilter(context) {
  if (context.left === context.right) {
    context.setResult(void 0).exit();
    return;
  }
  if (typeof context.left === "undefined") {
    if (typeof context.right === "function") {
      throw new Error("functions are not supported");
    }
    context.setResult([context.right]).exit();
    return;
  }
  if (typeof context.right === "undefined") {
    context.setResult([context.left, 0, 0]).exit();
    return;
  }
  if (typeof context.left === "function" || typeof context.right === "function") {
    throw new Error("functions are not supported");
  }
  context.leftType = context.left === null ? "null" : typeof context.left;
  context.rightType = context.right === null ? "null" : typeof context.right;
  if (context.leftType !== context.rightType) {
    context.setResult([context.left, context.right]).exit();
    return;
  }
  if (context.leftType === "boolean" || context.leftType === "number" || context.leftType === "string") {
    context.setResult([context.left, context.right]).exit();
    return;
  }
  if (context.leftType === "object") {
    context.leftIsArray = Array.isArray(context.left);
  }
  if (context.rightType === "object") {
    context.rightIsArray = Array.isArray(context.right);
  }
  if (context.leftIsArray !== context.rightIsArray) {
    context.setResult([context.left, context.right]).exit();
    return;
  }
  if (context.leftIsArray && context.rightIsArray) {
    if (arrayEquals(context.left, context.right)) {
      context.setResult(void 0).exit();
      return;
    } else {
      context.setResult([context.left, context.right]).exit();
      return;
    }
  }
  if (context.left instanceof RegExp) {
    if (context.right instanceof RegExp) {
      context.setResult([context.left.toString(), context.right.toString()]).exit();
    } else {
      context.setResult([context.left, context.right]).exit();
    }
  }
};
diffFilter.filterName = "trivial";
var patchFilter = function trivialMatchesPatchFilter(context) {
  if (typeof context.delta === "undefined") {
    context.setResult(context.left).exit();
    return;
  }
  context.nested = !Array.isArray(context.delta);
  if (context.nested) {
    return;
  }
  const nonNestedDelta = context.delta;
  if (nonNestedDelta.length === 1) {
    context.setResult(nonNestedDelta[0]).exit();
    return;
  }
  if (nonNestedDelta.length === 2) {
    if (context.left instanceof RegExp) {
      const regexArgs = /^\/(.*)\/([gimyu]+)$/.exec(nonNestedDelta[1]);
      if (regexArgs) {
        context.setResult(new RegExp(regexArgs[1], regexArgs[2])).exit();
        return;
      }
    }
    context.setResult(nonNestedDelta[1]).exit();
    return;
  }
  if (nonNestedDelta.length === 3 && nonNestedDelta[2] === 0) {
    context.setResult(void 0).exit();
  }
};
patchFilter.filterName = "trivial";

// ../jsondiffpatch/dist/filters/nested.js
var collectChildrenDiffFilter = (context) => {
  if (!context || !context.children) {
    return;
  }
  const length = context.children.length;
  let child;
  let result = context.result;
  for (let index = 0; index < length; index++) {
    child = context.children[index];
    if (typeof child.result === "undefined") {
      continue;
    }
    result = result || {};
    result[child.childName] = child.result;
  }
  if (result && context.leftIsArray) {
    result._t = "a";
  }
  context.setResult(result).exit();
};
collectChildrenDiffFilter.filterName = "collectChildren";
var objectsDiffFilter = (context) => {
  if (context.leftIsArray || context.leftType !== "object") {
    return;
  }
  const left = context.left;
  const right = context.right;
  let name;
  let child;
  const propertyFilter = context.options.propertyFilter;
  for (name in left) {
    if (!Object.prototype.hasOwnProperty.call(left, name)) {
      continue;
    }
    if (propertyFilter && !propertyFilter(name, context)) {
      continue;
    }
    child = new diff_default(left[name], right[name]);
    context.push(child, name);
  }
  for (name in right) {
    if (!Object.prototype.hasOwnProperty.call(right, name)) {
      continue;
    }
    if (propertyFilter && !propertyFilter(name, context)) {
      continue;
    }
    if (typeof left[name] === "undefined") {
      child = new diff_default(void 0, right[name]);
      context.push(child, name);
    }
  }
  if (!context.children || context.children.length === 0) {
    context.setResult(void 0).exit();
    return;
  }
  context.exit();
};
objectsDiffFilter.filterName = "objects";
var patchFilter2 = function nestedPatchFilter(context) {
  if (!context.nested) {
    return;
  }
  const nestedDelta = context.delta;
  if (nestedDelta._t) {
    return;
  }
  const objectDelta = nestedDelta;
  let name;
  let child;
  for (name in objectDelta) {
    child = new patch_default(context.left[name], objectDelta[name]);
    context.push(child, name);
  }
  context.exit();
};
patchFilter2.filterName = "objects";
var collectChildrenPatchFilter = function collectChildrenPatchFilter2(context) {
  if (!context || !context.children) {
    return;
  }
  const deltaWithChildren = context.delta;
  if (deltaWithChildren._t) {
    return;
  }
  const object = context.left;
  const length = context.children.length;
  let child;
  for (let index = 0; index < length; index++) {
    child = context.children[index];
    const property = child.childName;
    if (Object.prototype.hasOwnProperty.call(context.left, property) && child.result === void 0) {
      delete object[property];
    } else if (object[property] !== child.result) {
      object[property] = child.result;
    }
  }
  context.setResult(object).exit();
};
collectChildrenPatchFilter.filterName = "collectChildren";

// ../jsondiffpatch/dist/filters/dates.js
var diffFilter2 = function datesDiffFilter(context) {
  if (context.left instanceof Date) {
    if (context.right instanceof Date) {
      if (context.left.getTime() !== context.right.getTime()) {
        context.setResult([context.left, context.right]);
      } else {
        context.setResult(void 0);
      }
    } else {
      context.setResult([context.left, context.right]);
    }
    context.exit();
  } else if (context.right instanceof Date) {
    context.setResult([context.left, context.right]).exit();
  }
};
diffFilter2.filterName = "dates";

// ../jsondiffpatch/dist/diffpatcher.js
var DiffPatcher = class {
  constructor(options) {
    this.processor = new processor_default(options);
    this.processor.pipe(new pipe_default("diff").append(collectChildrenDiffFilter, diffFilter, diffFilter2, objectsDiffFilter).shouldHaveResult());
    this.processor.pipe(new pipe_default("patch").append(collectChildrenPatchFilter, patchFilter, patchFilter2).shouldHaveResult());
  }
  options(options) {
    return this.processor.options(options);
  }
  diff(left, right) {
    return this.processor.process(new diff_default(left, right));
  }
  patch(left, delta) {
    return this.processor.process(new patch_default(left, delta));
  }
};
var diffpatcher_default = DiffPatcher;

// ../jsondiffpatch/dist/index.js
function create(options) {
  return new diffpatcher_default(options);
}

// src/analytics-plugin.ts
var diff = create();
var defaultConfig = {
  /* Your segment writeKey */
  writeKey: void 0,
  /* Disable anonymous MTU */
  host: void 0,
  debug: false,
  fetch: void 0,
  echoEvents: false,
  cookieDomain: void 0,
  cookieNames: {},
  cookieCapture: {},
  runtime: void 0,
  fetchTimeoutMs: void 0,
  s2s: void 0,
  idEndpoint: void 0,
  errorPolicy: "log",
  defaultPayloadContext: {},
  privacy: {
    dontSend: false,
    disableUserIds: false,
    ipPolicy: "keep",
    consentCategories: {}
  }
};
var mergeConfig = (current, newConfig) => {
  for (const key of Object.keys(defaultConfig)) {
    const value = newConfig[key];
    if (key === "privacy") {
      if (typeof value === "object") {
        current.privacy = __spreadValues(__spreadValues(__spreadProps(__spreadValues({}, defaultConfig.privacy), {
          consentCategories: __spreadValues({}, defaultConfig.privacy.consentCategories)
        }), current.privacy), value);
      } else if (typeof value === "undefined") {
        if (newConfig.hasOwnProperty("privacy") || !current.hasOwnProperty("privacy")) {
          current.privacy = __spreadProps(__spreadValues({}, defaultConfig.privacy), {
            consentCategories: __spreadValues({}, defaultConfig.privacy.consentCategories)
          });
        }
      }
    } else if (typeof value === "undefined") {
      if (newConfig.hasOwnProperty(key) || !current.hasOwnProperty(key)) {
        current[key] = defaultConfig[key];
      }
    } else {
      current[key] = value;
    }
  }
};
var parseQuery = (qs) => {
  if (!qs) {
    return {};
  }
  let queryString = qs.length > 0 && qs.charAt(0) === "?" ? qs.substring(1) : qs;
  let query = {};
  let pairs = (queryString[0] === "?" ? queryString.substr(1) : queryString).split("&");
  for (let i = 0; i < pairs.length; i++) {
    let pair = pairs[i].split("=");
    query[decodeURIComponent(pair[0])] = decodeURIComponent(pair[1] || "");
  }
  return query;
};
function utmToKey(key) {
  const name = key.substring("utm_".length);
  return name === "campaign" ? "name" : name;
}
function parseUtms(query) {
  return Object.entries(query).filter(([key]) => key.indexOf("utm_") === 0).reduce(
    (acc, [key, value]) => __spreadProps(__spreadValues({}, acc), {
      [utmToKey(key)]: value
    }),
    {}
  );
}
function safeCall(f, defaultVal) {
  try {
    return f();
  } catch (e) {
    return defaultVal;
  }
}
function restoreTraits(storage) {
  let val = storage.getItem("__user_traits");
  if (typeof val === "string") {
    val = safeCall(() => JSON.parse(val), {});
  }
  if (typeof val !== "object" || val === null || Array.isArray(val)) {
    val = {};
  }
  let groupVal = storage.getItem("__group_traits");
  if (typeof groupVal === "string") {
    groupVal = safeCall(() => JSON.parse(groupVal), {});
  }
  if (typeof groupVal !== "object" || groupVal === null || Array.isArray(groupVal)) {
    groupVal = {};
  }
  return __spreadValues(__spreadValues({}, groupVal || {}), val || {});
}
function getCookie(name) {
  var _a;
  const value = `; ${document.cookie}`;
  const parts = value.split(`; ${name}=`);
  return parts.length === 2 ? (_a = parts.pop()) == null ? void 0 : _a.split(";").shift() : void 0;
}
function getClientIds(runtime, customCookieCapture) {
  const cookieCapture = __spreadValues({
    fbc: "_fbc",
    fbp: "_fbp"
  }, customCookieCapture);
  const clientIds = Object.entries(cookieCapture).reduce((acc, [key, cookieName]) => {
    acc[key] = runtime.getCookie(cookieName);
    return acc;
  }, {});
  return __spreadValues(__spreadValues({}, clientIds), getGa4Ids(runtime));
}
function parseGa4SessionId(cookieValue) {
  if (typeof cookieValue !== "string") {
    return void 0;
  }
  if (cookieValue.startsWith("GA1") || cookieValue.startsWith("GS1")) {
    return cookieValue.split(".")[2];
  } else {
    const match = cookieValue.match(/^GS\d+\.\d+\.(?:[\w_-]+[$])*s(\d+)(?:$|[$])/);
    return match ? match[1] : void 0;
  }
}
function getGa4Ids(runtime) {
  var _a;
  const allCookies = runtime.getCookies();
  const clientId = (_a = allCookies["_ga"]) == null ? void 0 : _a.split(".").slice(-2).join(".");
  const gaSessionCookies = Object.entries(allCookies).filter(([key]) => key.startsWith("_ga_"));
  const sessionIds = gaSessionCookies.length > 0 ? Object.fromEntries(
    gaSessionCookies.map(([key, value]) => {
      const sessionId = parseGa4SessionId(value);
      if (!sessionId) {
        return null;
      }
      return [key.substring("_ga_".length), sessionId];
    }).filter((v) => v !== null)
  ) : void 0;
  if (clientId || sessionIds) {
    return { ga4: { clientId, sessionIds } };
  } else {
    return void 0;
  }
}
function removeCookie(name, { domain, secure }) {
  document.cookie = name + "=;domain=" + domain + ";path=/;expires=Thu, 01 Jan 1970 00:00:01 GMT;SameSite=" + (secure ? "None" : "Lax") + (secure ? ";secure" : "");
}
function setCookie(name, val, { domain, secure }) {
  document.cookie = name + "=" + val + ";domain=" + domain + ";path=/;expires=" + new Date((/* @__PURE__ */ new Date()).getTime() + 1e3 * 60 * 60 * 24 * 365 * 5).toUTCString() + ";SameSite=" + (secure ? "None" : "Lax") + (secure ? ";secure" : "");
}
var defaultCookie2Key = {
  __anon_id: "__eventn_id",
  __user_id: "__eventn_uid",
  __user_traits: "__eventn_id_usr",
  __group_id: "__group_id",
  __group_traits: "__group_traits"
};
var cookieStorage = (cookieDomain, key2cookie) => {
  return {
    setItem(key, val) {
      const cookieName = key2cookie(key) || key;
      if (typeof val === "undefined") {
        removeCookie(cookieName, {
          domain: cookieDomain,
          secure: window.location.protocol === "https:"
        });
        return;
      }
      const strVal = typeof val === "object" && val !== null ? encodeURIComponent(JSON.stringify(val)) : val;
      setCookie(cookieName, strVal, {
        domain: cookieDomain,
        secure: window.location.protocol === "https:"
      });
    },
    getItem(key) {
      const cookieName = key2cookie(key) || key;
      const result = getCookie(cookieName);
      if (key === "__anon_id") {
        return result;
      }
      if (typeof result === "undefined" && key === "__user_id") {
        const traits = parse(getCookie(key2cookie("__user_traits") || "__eventn_id_usr")) || {};
        return traits.internal_id || traits.user_id || traits.id || traits.userId;
      }
      return parse(result);
    },
    removeItem(key) {
      removeCookie(key2cookie(key) || key, {
        domain: cookieDomain,
        secure: window.location.protocol === "https:"
      });
    },
    reset() {
      for (const key of Object.keys(defaultCookie2Key)) {
        removeCookie(key2cookie(key) || key, {
          domain: cookieDomain,
          secure: window.location.protocol === "https:"
        });
      }
    }
  };
};
function windowRuntime(opts) {
  const key2Cookie = (key) => {
    var _a, _b, _c, _d, _e;
    switch (key) {
      case "__anon_id":
        return ((_a = opts.cookieNames) == null ? void 0 : _a.anonymousId) || defaultCookie2Key.__anon_id;
      case "__user_id":
        return ((_b = opts.cookieNames) == null ? void 0 : _b.userId) || defaultCookie2Key.__user_id;
      case "__user_traits":
        return ((_c = opts.cookieNames) == null ? void 0 : _c.userTraits) || defaultCookie2Key.__user_traits;
      case "__group_id":
        return ((_d = opts.cookieNames) == null ? void 0 : _d.groupId) || defaultCookie2Key.__group_id;
      case "__group_traits":
        return ((_e = opts.cookieNames) == null ? void 0 : _e.groupTraits) || defaultCookie2Key.__group_traits;
      default:
        return key;
    }
  };
  return {
    getCookie(name) {
      var _a;
      const value = `; ${document.cookie}`;
      const parts = value.split(`; ${name}=`);
      return parts.length === 2 ? (_a = parts.pop()) == null ? void 0 : _a.split(";").shift() : void 0;
    },
    getCookies() {
      const value = `; ${document.cookie}`;
      const cookies = {};
      const matches = value.matchAll(/(\w+)=([^;]+)/g);
      for (const match of matches) {
        cookies[match[1]] = match[2];
      }
      return cookies;
    },
    documentEncoding() {
      return window.document.characterSet;
    },
    timezoneOffset() {
      return (/* @__PURE__ */ new Date()).getTimezoneOffset();
    },
    store() {
      return cookieStorage(opts.cookieDomain || getTopLevelDomain(window.location.hostname), key2Cookie);
    },
    language() {
      return window.navigator.language;
    },
    pageTitle() {
      return window.document.title;
    },
    pageUrl() {
      return window.location.href;
    },
    referrer() {
      return window.document.referrer;
    },
    screen() {
      return {
        width: window.screen.width,
        height: window.screen.height,
        innerWidth: window.innerWidth,
        innerHeight: window.innerHeight,
        density: Math.floor(window.devicePixelRatio)
      };
    },
    userAgent() {
      return window.navigator.userAgent;
    }
  };
}
function createInMemoryStorage(debug) {
  const storage = {};
  return {
    reset() {
      Object.keys(storage).forEach((key) => delete storage[key]);
    },
    setItem(key, val) {
      if (debug) {
        console.log(`[JITSU EMPTY RUNTIME] Set storage item ${key}=${JSON.stringify(val)}`);
      }
      if (typeof val === "undefined") {
        delete storage[key];
      } else {
        storage[key] = val;
      }
    },
    getItem(key) {
      const val = storage[key];
      if (debug) {
        console.log(`[JITSU EMPTY RUNTIME] Get storage item ${key}=${JSON.stringify(val)}`);
      }
      return val;
    },
    removeItem(key) {
      if (debug) {
        console.log(`[JITSU EMPTY RUNTIME] Get storage item ${key}=${storage[key]}`);
      }
      delete storage[key];
    }
  };
}
var emptyRuntime = (config) => ({
  documentEncoding() {
    return void 0;
  },
  timezoneOffset() {
    return void 0;
  },
  getCookie(name) {
    return void 0;
  },
  getCookies() {
    return {};
  },
  store() {
    return createInMemoryStorage(config.debug);
  },
  language() {
    return void 0;
  },
  pageTitle() {
    return void 0;
  },
  pageUrl() {
    return void 0;
  },
  referrer() {
    return void 0;
  },
  screen() {
    return void 0;
  },
  userAgent() {
    return void 0;
  }
});
function deepCopy(o) {
  if (typeof o !== "object") {
    return o;
  }
  if (!o) {
    return o;
  }
  return JSON.parse(JSON.stringify(o));
}
function deepMerge(target, source) {
  if (typeof source !== "object" || source === null || Array.isArray(source) || source instanceof Date) {
    return source;
  }
  if (typeof target !== "object" || target === null || Array.isArray(target) || target instanceof Date) {
    return source;
  }
  return Object.entries(source).reduce((acc, [key, value]) => {
    acc[key] = deepMerge(target[key], value);
    return acc;
  }, target);
}
function isInBrowser() {
  return typeof document !== "undefined" && typeof window !== "undefined";
}
function ensureAnonymousId(opts) {
  var _a;
  if (!isInBrowser()) {
    return void 0;
  }
  const cookieName = ((_a = opts.cookieNames) == null ? void 0 : _a.anonymousId) || defaultCookie2Key.__anon_id;
  const existing = getCookie(cookieName);
  if (existing) {
    return existing;
  }
  const id = uuid();
  const domain = opts.cookieDomain || getTopLevelDomain(window.location.hostname);
  setCookie(cookieName, id, {
    domain,
    secure: window.location.protocol === "https:"
  });
  if (opts.debug) {
    console.log(
      `[JITSU DEBUG] preInitAnonymousId: created anonymous ID cookie '${cookieName}'=${id} on domain '${domain}'`
    );
  }
  return id;
}
function fixPath(path) {
  if (path.indexOf("//") === 0 && path.lastIndexOf("/") === 1) {
    return "/";
  }
  return path;
}
var hashRegex = /#.*$/;
function urlPath(url) {
  const regex = /(http[s]?:\/\/)?([^\/\s]+\/)(.*)/g;
  const matches = regex.exec(url);
  const pathMatch = matches && matches[3] ? matches[3].split("?")[0].replace(hashRegex, "") : "";
  return "/" + pathMatch;
}
function canonicalUrl() {
  if (!isInBrowser()) return;
  const tags = document.getElementsByTagName("link");
  for (var i = 0, tag; tag = tags[i]; i++) {
    if (tag.getAttribute("rel") === "canonical") {
      return tag.getAttribute("href");
    }
  }
}
function analyticsJsUrl() {
  if (!isInBrowser()) return;
  const canonical = canonicalUrl();
  if (!canonical) return window.location.href.replace(hashRegex, "");
  return canonical.match(/\?/) ? canonical : canonical + window.location.search;
}
function adjustPayload(payload, config, storage, s2s) {
  var _a, _b, _c, _d, _e, _f, _g, _h, _i, _j, _k, _l, _m, _n, _o;
  const runtime = config.runtime || (isInBrowser() ? windowRuntime(config) : emptyRuntime(config));
  const url = runtime.pageUrl();
  const parsedUrl = url ? safeCall(() => new URL(url), void 0) : void 0;
  const query = parsedUrl ? parseQuery(parsedUrl.search) : {};
  const properties = payload.properties || {};
  if (payload.type === "page" && (properties.url || url)) {
    let targetUrl = url || properties.url;
    if (properties.url && properties.url !== analyticsJsUrl()) {
      targetUrl = properties.url;
    }
    properties.url = targetUrl.replace(hashRegex, "");
    properties.path = fixPath(urlPath(targetUrl));
  }
  const customContext = deepMerge(
    config.defaultPayloadContext,
    ((_a = payload.properties) == null ? void 0 : _a.context) || ((_b = payload.options) == null ? void 0 : _b.context) || {}
  );
  (_c = payload.properties) == null ? true : delete _c.context;
  const referrer = runtime.referrer();
  const context = {
    library: {
      name: jitsuLibraryName,
      version: jitsuVersion,
      env: isInBrowser() ? "browser" : "node"
    },
    consent: typeof ((_d = config.privacy) == null ? void 0 : _d.consentCategories) === "object" && Object.keys((_e = config.privacy) == null ? void 0 : _e.consentCategories).length ? {
      categoryPreferences: config.privacy.consentCategories
    } : void 0,
    userAgent: (_f = runtime.userAgent) == null ? void 0 : _f.call(runtime),
    locale: (_g = runtime.language) == null ? void 0 : _g.call(runtime),
    screen: (_h = runtime.screen) == null ? void 0 : _h.call(runtime),
    ip: (_i = runtime == null ? void 0 : runtime.ip) == null ? void 0 : _i.call(runtime),
    traits: payload.type != "identify" && payload.type != "group" ? __spreadValues(__spreadValues({}, restoreTraits(storage) || {}), ((_j = payload == null ? void 0 : payload.options) == null ? void 0 : _j.traits) || {}) : void 0,
    page: {
      path: properties.path || parsedUrl && parsedUrl.pathname,
      referrer,
      referring_domain: safeCall(() => referrer && new URL(referrer).hostname),
      host: parsedUrl && parsedUrl.host,
      search: properties.search || parsedUrl && parsedUrl.search,
      title: properties.title || runtime.pageTitle(),
      url: properties.url || url,
      encoding: properties.encoding || runtime.documentEncoding()
    },
    clientIds: !((_k = config.privacy) == null ? void 0 : _k.disableUserIds) ? getClientIds(runtime, config.cookieCapture || {}) : void 0,
    campaign: parseUtms(query)
  };
  const withContext = __spreadProps(__spreadValues({}, payload), {
    userId: ((_l = payload == null ? void 0 : payload.options) == null ? void 0 : _l.userId) || (payload == null ? void 0 : payload.userId),
    anonymousId: ((_m = payload == null ? void 0 : payload.options) == null ? void 0 : _m.anonymousId) || (payload == null ? void 0 : payload.anonymousId),
    groupId: ((_n = payload == null ? void 0 : payload.options) == null ? void 0 : _n.groupId) || storage.getItem("__group_id"),
    timestamp: (/* @__PURE__ */ new Date()).toISOString(),
    sentAt: (/* @__PURE__ */ new Date()).toISOString(),
    messageId: randomId(properties.path || parsedUrl && parsedUrl.pathname),
    writeKey: maskWriteKey(config.writeKey),
    context: deepMerge(context, customContext)
  });
  delete withContext.meta;
  delete withContext.options;
  if ((_o = config.privacy) == null ? void 0 : _o.disableUserIds) {
    delete withContext.userId;
    delete withContext.anonymousId;
    delete withContext.context.traits;
    delete withContext.groupId;
  }
  return withContext;
}
function isDiff(obj) {
  const keys = Object.keys(obj);
  return keys.length === 1 && keys[0] === "__diff";
}
function processDestinations(destinations, method, originalEvent, debug, analyticsInstance) {
  return __async(this, null, function* () {
    const promises = [];
    for (const destination of destinations) {
      let newEvents = [];
      if (destination.newEvents) {
        try {
          newEvents = destination.newEvents.map(
            (e) => e === "same" ? deepCopy(originalEvent) : isDiff(e) ? diff.patch(deepCopy(originalEvent), e.__diff) : e
          );
        } catch (e) {
          console.error(`[JITSU] Error applying '${destination.id}' changes to event: ${e == null ? void 0 : e.message}`, e);
        }
      } else {
        newEvents = [deepCopy(originalEvent)];
      }
      const credentials = __spreadValues(__spreadValues({}, destination.credentials), destination.options);
      if (destination.deviceOptions.type === "internal-plugin") {
        const plugin = internalDestinationPlugins[destination.deviceOptions.name];
        if (plugin) {
          for (const event of newEvents) {
            try {
              promises.push(plugin.handle(__spreadProps(__spreadValues({}, credentials), { debug }), event));
            } catch (e) {
              console.warn(
                `[JITSU] Error processing event with internal plugin '${destination.deviceOptions.name}': ${e == null ? void 0 : e.message}`,
                e
              );
            }
          }
        } else {
          console.warn(
            `[JITSU] Unknown internal plugin '${destination.deviceOptions.name}' for destination '${destination.id}'`
          );
        }
      } else if (destination.deviceOptions.type === "analytics-plugin") {
        yield loadScript(destination.deviceOptions.packageCdn);
        const plugin = window[destination.deviceOptions.moduleVarName];
        if (!plugin) {
          console.warn(
            `[JITSU] Broken plugin '${destination.deviceOptions.packageCdn}' for destination '${destination.id}' - it doesn't export '${destination.deviceOptions.moduleVarName}' variable`
          );
        } else {
          let pluginInstance;
          try {
            pluginInstance = (typeof plugin === "function" ? plugin : plugin.init)(credentials);
          } catch (e) {
            console.warn(
              `[JITSU] Error creating plugin '${destination.deviceOptions.moduleVarName}@${destination.deviceOptions.packageCdn}' for destination '${destination.id}': ${e == null ? void 0 : e.message}`,
              e
            );
          }
          try {
            if (debug) {
              console.log(
                `[JITSU] Plugin '${destination.deviceOptions.moduleVarName}@${destination.deviceOptions.packageCdn}' for destination '${destination.id}' initialized with config:`,
                pluginInstance.config
              );
            }
            pluginInstance.initialize({ config: pluginInstance.config, instance: analyticsInstance });
          } catch (e) {
            console.warn(
              `[JITSU] Error initializing plugin '${destination.deviceOptions.moduleVarName}@${destination.deviceOptions.packageCdn}' for destination '${destination.id}': ${e == null ? void 0 : e.message}. Config: ${JSON.stringify(pluginInstance.config)}`,
              e
            );
            continue;
          }
          if (pluginInstance[method]) {
            for (const event of newEvents) {
              try {
                pluginInstance[method]({
                  payload: event,
                  config: pluginInstance.config,
                  instance: analyticsInstance
                });
              } catch (e) {
                console.warn(
                  `[JITSU] Error processing ${method}() with plugin '${destination.deviceOptions.moduleVarName}@${destination.deviceOptions.packageCdn}' for destination '${destination.id}': ${e == null ? void 0 : e.message}`,
                  e
                );
              }
            }
          }
        }
      }
    }
  });
}
function looksLikeCuid(id) {
  return id.length === 25 && id.charAt(0) === "c";
}
function validateWriteKey(writeKey) {
  if (writeKey) {
    const [, secret] = writeKey.split(":", 2);
    if (!secret && !looksLikeCuid(writeKey)) {
      throw new Error(
        `Legacy write key detected - ${writeKey}! This format doesn't work anymore, it should be 'key:secret'. Please download a new key from Jitsu UI`
      );
    }
  }
  return writeKey;
}
function maskWriteKey(writeKey) {
  if (writeKey) {
    const [id, secret] = writeKey.split(":", 2);
    if (secret) {
      return `${id}:***`;
    } else {
      return "***";
    }
  }
  return writeKey;
}
function getErrorHandler(opts) {
  const configuredHandler = opts.errorPolicy || "log";
  if (typeof configuredHandler === "function") {
    return configuredHandler;
  } else if (configuredHandler === "rethrow") {
    return (msg, ...args) => {
      throw new Error(msg);
    };
  } else {
    return (msg, ...args) => {
      console.error(msg, ...args);
    };
  }
}
function send(method, payload, jitsuConfig, instance, store, isAutoIdentify) {
  return __async(this, null, function* () {
    var _a, _b;
    const s2s = !!jitsuConfig.s2s;
    const debugHeader = jitsuConfig.debug ? { "X-Enable-Debug": "true" } : {};
    const adjustedPayload = adjustPayload(payload, jitsuConfig, store, s2s);
    if (jitsuConfig.echoEvents) {
      console.log(`[JITSU DEBUG] sending '${method}' event:`, adjustedPayload);
      return;
    }
    const url = s2s ? `${jitsuConfig.host}/api/s/s2s/${method}` : `${jitsuConfig.host}/api/s/${method}`;
    const fetch = jitsuConfig.fetch || globalThis.fetch;
    if (!fetch) {
      throw new Error(
        "Please specify fetch function in jitsu plugin initialization, fetch isn't available in global scope"
      );
    }
    const abortController = jitsuConfig.fetchTimeoutMs ? new AbortController() : void 0;
    const abortTimeout = jitsuConfig.fetchTimeoutMs ? setTimeout(() => {
      abortController == null ? void 0 : abortController.abort();
    }, jitsuConfig.fetchTimeoutMs) : void 0;
    const authHeader = jitsuConfig.writeKey ? { "X-Write-Key": jitsuConfig.writeKey } : {};
    const ipHeader = typeof ((_a = jitsuConfig.privacy) == null ? void 0 : _a.ipPolicy) === "undefined" || ((_b = jitsuConfig.privacy) == null ? void 0 : _b.ipPolicy) === "keep" ? {} : { "X-IP-Policy": jitsuConfig.privacy.ipPolicy };
    let fetchResult;
    try {
      fetchResult = yield fetch(url, {
        method: "POST",
        headers: __spreadValues(__spreadValues(__spreadValues({
          "Content-Type": "application/json"
        }, authHeader), debugHeader), ipHeader),
        body: JSON.stringify(adjustedPayload),
        signal: abortController == null ? void 0 : abortController.signal
      });
      if (abortTimeout) {
        clearTimeout(abortTimeout);
      }
    } catch (e) {
      const errorHandler = isAutoIdentify ? (msg) => console.warn(msg) : getErrorHandler(jitsuConfig);
      errorHandler(`Call to ${url} failed with error ${e.message}`);
      return Promise.resolve();
    }
    let responseText;
    try {
      responseText = yield fetchResult.text();
    } catch (e) {
      console.warn(
        `Can't read response text from ${url} (status - ${fetchResult.status}  ${fetchResult.statusText}): ${e == null ? void 0 : e.message}`
      );
    }
    if (jitsuConfig.debug) {
      console.log(
        `[JITSU DEBUG] ${url} replied ${fetchResult.status}: ${responseText}. Original payload:
${JSON.stringify(
          adjustedPayload,
          null,
          2
        )}`
      );
    }
    if (!fetchResult.ok) {
      const errorHandler = isAutoIdentify ? (msg) => console.warn(msg) : getErrorHandler(jitsuConfig);
      errorHandler(
        `Call to ${url} failed with error: ${fetchResult.status} - ${fetchResult.statusText}: ${responseText}`
      );
      return Promise.resolve();
    }
    let responseJson;
    try {
      responseJson = JSON.parse(responseText);
    } catch (e) {
      const errorHandler = isAutoIdentify ? (msg) => console.warn(msg) : getErrorHandler(jitsuConfig);
      errorHandler(`Can't parse JSON: ${responseText}: ${e == null ? void 0 : e.message}`);
      return Promise.resolve();
    }
    if (responseJson.destinations && responseJson.destinations.length > 0) {
      if (jitsuConfig.s2s) {
        console.warn(
          `[JITSU] ${payload.type} responded with list of ${responseJson.destinations.length} destinations. However, this code is running in server-to-server mode, so destinations will be ignored`,
          jitsuConfig.debug ? JSON.stringify(responseJson.destinations, null, 2) : void 0
        );
      } else {
        if (isInBrowser()) {
          if (jitsuConfig.debug) {
            console.log(`[JITSU] Processing device destinations: `, JSON.stringify(responseJson.destinations, null, 2));
          }
          return processDestinations(responseJson.destinations, method, adjustedPayload, !!jitsuConfig.debug, instance);
        }
      }
    }
    return adjustedPayload;
  });
}
var controllingTraits = ["$doNotSend"];
function stripControllingTraits(traits) {
  const res = __spreadValues({}, traits);
  for (const key of controllingTraits) {
    delete res[key];
  }
  return res;
}
var jitsuAnalyticsPlugin = (jitsuOptions = {}, storage) => {
  mergeConfig(jitsuOptions, jitsuOptions);
  return {
    name: "jitsu",
    config: jitsuOptions,
    initialize: (args) => __async(null, null, function* () {
      const { config } = args;
      if (config.debug) {
        console.debug("[JITSU DEBUG] Initializing Jitsu plugin with config: ", JSON.stringify(config, null, 2));
      }
      if (!config.host && !config.echoEvents) {
        throw new Error("Please specify host variable in jitsu plugin initialization, or set echoEvents to true");
      }
      validateWriteKey(config.writeKey);
      if (config.idEndpoint) {
        if (!isInBrowser()) {
          console.error(`[JITSU] 'idEndpoint' option can be used only in browser environment`);
          return;
        }
        try {
          const fetch = config.fetch || globalThis.fetch;
          const controller = new AbortController();
          setTimeout(() => controller.abort(), 1e3);
          const domain = config.cookieDomain || getTopLevelDomain(window.location.hostname);
          const res = yield fetch(config.idEndpoint + "?domain=" + encodeURIComponent(domain), {
            credentials: "include",
            signal: controller.signal
          });
          if (!res.ok) {
            console.error(`[JITSU] Can't fetch idEndpoint: ${res.status} - ${res.statusText}`);
          } else if (config.debug) {
            console.log(`[JITSU DEBUG] Fetch idEndpoint: ${res.status}`);
          }
        } catch (e) {
          console.error(`[JITSU] Can't fetch idEndpoint: ${e.message}`);
        }
      }
    }),
    page: (args) => {
      var _a;
      const { payload, config, instance } = args;
      if ((_a = config.privacy) == null ? void 0 : _a.dontSend) {
        return;
      }
      const promise = send("page", payload, config, instance, storage);
      if (promise && typeof promise.catch === "function") {
        promise.catch(() => {
        });
      }
      return promise;
    },
    track: (args) => {
      var _a;
      const { payload, config, instance } = args;
      if ((_a = config.privacy) == null ? void 0 : _a.dontSend) {
        return;
      }
      const promise = send("track", payload, config, instance, storage);
      if (promise && typeof promise.catch === "function") {
        promise.catch(() => {
        });
      }
      return promise;
    },
    identify: (args) => {
      var _a, _b, _c, _d;
      const { payload, config, instance } = args;
      if (((_a = config.privacy) == null ? void 0 : _a.dontSend) || ((_b = config.privacy) == null ? void 0 : _b.disableUserIds)) {
        return;
      }
      storage.setItem("__user_id", payload.userId);
      const doNotSend = (_c = payload.traits) == null ? void 0 : _c.$doNotSend;
      if (payload.traits && typeof payload.traits === "object") {
        payload.traits = stripControllingTraits(payload.traits);
        storage.setItem("__user_traits", payload.traits);
      }
      if (doNotSend) {
        return Promise.resolve();
      }
      const isAutoIdentify = ((_d = payload.options) == null ? void 0 : _d.autoIdentify) === true;
      const promise = send("identify", payload, config, instance, storage, isAutoIdentify);
      if (promise && typeof promise.catch === "function") {
        promise.catch(() => {
        });
      }
      return promise;
    },
    reset: (args) => {
      const { config, instance } = args;
      storage.reset();
      if (config.debug) {
        console.log("[JITSU DEBUG] Resetting Jitsu plugin storage");
      }
    },
    methods: {
      //analytics doesn't support group as a base method, so we need to add it manually
      configure(newOptions) {
        var _a, _b, _c, _d;
        const idsWasDisabled = ((_a = jitsuOptions.privacy) == null ? void 0 : _a.disableUserIds) || ((_b = jitsuOptions.privacy) == null ? void 0 : _b.dontSend);
        mergeConfig(jitsuOptions, newOptions);
        const idsDisabledNow = ((_c = jitsuOptions.privacy) == null ? void 0 : _c.disableUserIds) || ((_d = jitsuOptions.privacy) == null ? void 0 : _d.dontSend);
        if (!idsDisabledNow && idsWasDisabled) {
          if (jitsuOptions.debug) {
            console.log("[JITSU] Enabling Anonymous ID. Generating new Id.");
          }
          const instance = this.instance;
          const newAnonymousId = uuid();
          const userState = instance.user();
          if (userState) {
            userState.anonymousId = newAnonymousId;
          }
          storage.setItem("__anon_id", newAnonymousId);
          instance.setAnonymousId(newAnonymousId);
        }
      },
      group(groupId, traits, options, callback) {
        var _a, _b;
        if (((_a = jitsuOptions.privacy) == null ? void 0 : _a.dontSend) || ((_b = jitsuOptions.privacy) == null ? void 0 : _b.disableUserIds)) {
          return;
        }
        if (typeof groupId === "number") {
          groupId = groupId + "";
        }
        const instance = this.instance;
        const user = instance.user();
        const userId = (options == null ? void 0 : options.userId) || (user == null ? void 0 : user.userId);
        const anonymousId = (options == null ? void 0 : options.anonymousId) || (user == null ? void 0 : user.anonymousId) || storage.getItem("__anon_id");
        storage.setItem("__group_id", groupId);
        const doNotSend = traits == null ? void 0 : traits.$doNotSend;
        if (traits && typeof traits === "object") {
          traits = stripControllingTraits(traits);
          storage.setItem("__group_traits", traits);
        }
        if (doNotSend) {
          return Promise.resolve();
        }
        return send(
          "group",
          __spreadValues(__spreadValues({ type: "group", groupId, traits }, anonymousId ? { anonymousId } : {}), userId ? { userId } : {}),
          jitsuOptions,
          instance,
          storage
        );
      }
    }
  };
};
var seedCounter = 0;
function getSeed() {
  var _a;
  seedCounter = (seedCounter + 1) % Number.MAX_SAFE_INTEGER;
  const defaultSeed = Date.now() % 2147483647;
  const seed = isInBrowser() ? ((_a = window == null ? void 0 : window.performance) == null ? void 0 : _a.now()) || defaultSeed : defaultSeed;
  return seed + seedCounter;
}
function randomId(hashString = "") {
  const d = Date.now();
  return (Math.random() * d * hash(hashString != null ? hashString : "", getSeed()) % Number.MAX_SAFE_INTEGER).toString(36) + (Math.random() * d * hash(hashString != null ? hashString : "", getSeed()) % Number.MAX_SAFE_INTEGER).toString(36);
}
function uuid() {
  var u = "", m = "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx", i = 0, rb = Math.random() * 4294967295 | 0;
  while (i++ < 36) {
    var c = m[i - 1], r = rb & 15, v = c == "x" ? r : r & 3 | 8;
    u += c == "-" || c == "4" ? c : v.toString(16);
    rb = i % 8 == 0 ? Math.random() * 4294967295 | 0 : rb >> 4;
  }
  return u;
}
function hash(str, seed = 0) {
  let h1 = 3735928559 ^ seed, h2 = 1103547991 ^ seed;
  for (let i = 0, ch; i < str.length; i++) {
    ch = str.charCodeAt(i);
    h1 = Math.imul(h1 ^ ch, 2654435761);
    h2 = Math.imul(h2 ^ ch, 1597334677);
  }
  h1 = Math.imul(h1 ^ h1 >>> 16, 2246822507) ^ Math.imul(h2 ^ h2 >>> 13, 3266489909);
  h2 = Math.imul(h2 ^ h2 >>> 16, 2246822507) ^ Math.imul(h1 ^ h1 >>> 13, 3266489909);
  return 4294967296 * (2097151 & h2) + (h1 >>> 0);
}

// src/index.ts
function parse(input) {
  let value = input;
  if ((input == null ? void 0 : input.indexOf("%7B%22")) === 0) {
    value = decodeURIComponent(input);
  }
  try {
    value = JSON.parse(value);
    if (value === "true") return true;
    if (value === "false") return false;
    if (typeof value === "object") return value;
    if (parseFloat(value) === value) {
      value = parseFloat(value);
    }
  } catch (e) {
  }
  if (value === null || value === "") {
    return;
  }
  return value;
}
var emptyAnalytics = {
  setAnonymousId: () => {
  },
  setContextProperty(name, value) {
  },
  getContextProperty(name) {
    return void 0;
  },
  track: () => Promise.resolve(),
  page: () => Promise.resolve(),
  user: () => ({}),
  identify: () => Promise.resolve({}),
  group: () => Promise.resolve({}),
  reset: () => Promise.resolve({}),
  configure: () => {
  },
  getConfiguration() {
    return {};
  }
};
function createUnderlyingAnalyticsInstance(opts, rt, plugins = []) {
  var _a, _b, _c, _e, _f;
  const storageCache = {};
  let resetCalled = false;
  const cachingStorageWrapper = (persistentStorage) => ({
    setItem(key, val) {
      var _a2, _b2;
      if (((_a2 = opts.privacy) == null ? void 0 : _a2.dontSend) || ((_b2 = opts.privacy) == null ? void 0 : _b2.disableUserIds)) {
        return;
      }
      if (opts.debug) {
        console.log(`[JITSU DEBUG] [Caching storage] setItem: ${key}=${val}`);
      }
      storageCache[key] = val;
      persistentStorage.setItem(key, val);
    },
    getItem(key) {
      var _a2, _b2;
      if (((_a2 = opts.privacy) == null ? void 0 : _a2.dontSend) || ((_b2 = opts.privacy) == null ? void 0 : _b2.disableUserIds)) {
        return;
      }
      const value = storageCache[key] || persistentStorage.getItem(key);
      if (opts.debug) {
        console.log(`[JITSU DEBUG] [Caching storage] getItem: ${key}=${value}. From cache: ${!!storageCache[key]}`);
      }
      return value;
    },
    reset() {
      for (const key of [...Object.keys(storageCache)]) {
        delete storageCache[key];
      }
      persistentStorage.reset();
    },
    removeItem(key) {
      if (opts.debug) {
        console.log(`[JITSU DEBUG] Caching storage removeItem: ${key}`);
      }
      delete storageCache[key];
      persistentStorage.removeItem(key);
    }
  });
  const storage = cachingStorageWrapper(((_a = rt.store) == null ? void 0 : _a.call(rt)) || createInMemoryStorage(opts.debug));
  if (opts.userId && !((_b = opts.privacy) == null ? void 0 : _b.disableUserIds) && !((_c = opts.privacy) == null ? void 0 : _c.dontSend)) {
    storage.setItem("__user_id", opts.userId);
  }
  const analytics = (0, import_analytics.default)({
    debug: !!opts.debug,
    storage,
    plugins: [jitsuAnalyticsPlugin(opts, storage), ...plugins]
  });
  const a = __spreadProps(__spreadValues({}, analytics), {
    page: (...args) => {
      if (args.length === 2 && typeof args[0] === "string" && typeof args[1] === "object") {
        return analytics.page(__spreadValues({
          name: args[0]
        }, args[1]));
      } else {
        return analytics.page(...args);
      }
    },
    identify: (...args) => {
      if (args[0] && typeof args[0] !== "object" && typeof args[0] !== "string") {
        args[0] = args[0] + "";
      }
      const storage2 = analytics.storage;
      const storageWrapper = cachingStorageWrapper(storage2);
      if (typeof args[0] === "string") {
        storageWrapper.setItem("__user_id", args[0]);
      } else if (typeof args[0] === "object") {
        storageWrapper.setItem("__user_traits", args[0]);
      }
      if (args.length === 2 && typeof args[1] === "object") {
        storageWrapper.setItem("__user_traits", args[1]);
      }
      return analytics.identify(...args);
    },
    setContextProperty(name, value) {
      if (opts.debug) {
        console.log(`[JITSU DEBUG] Setting context property '${name}':${JSON.stringify(value)}`);
      }
      if (!opts.defaultPayloadContext) {
        opts.defaultPayloadContext = {
          [name]: value
        };
      } else {
        opts.defaultPayloadContext[name] = value;
      }
    },
    getContextProperty(name) {
      var _a2;
      if (opts.debug) {
        console.log(`[JITSU DEBUG] Getting context property '${name}'`);
      }
      return (_a2 = opts.defaultPayloadContext) == null ? void 0 : _a2[name];
    },
    user() {
      var _a2, _b2;
      const u = analytics.user();
      const idsDisabled = ((_a2 = opts.privacy) == null ? void 0 : _a2.disableUserIds) || ((_b2 = opts.privacy) == null ? void 0 : _b2.dontSend);
      if (idsDisabled) {
        return __spreadProps(__spreadValues({}, u), {
          id: void 0
        });
      }
      if (u && !u.id && opts.userId && !resetCalled) {
        return __spreadProps(__spreadValues({}, u), {
          id: opts.userId
        });
      }
      return u;
    },
    setAnonymousId: (id) => {
      if (opts.debug) {
        console.log("[JITSU DEBUG] Setting anonymous id to " + id);
      }
      storage.setItem("__anon_id", id);
      const userState = analytics.user();
      if (userState) {
        userState.anonymousId = id;
      }
      analytics.setAnonymousId(id);
    },
    reset() {
      return __async(this, null, function* () {
        resetCalled = true;
        if (opts.debug) {
          console.log("[JITSU DEBUG] Called reset(). Storage state", JSON.stringify(analytics.user()));
        }
        storage.reset();
        yield analytics.reset();
        this.setAnonymousId(uuid());
        if (opts.debug) {
          console.log("[JITSU DEBUG] User state after reset", JSON.stringify(analytics.user()));
        }
      });
    },
    configure(options) {
      return __async(this, null, function* () {
        var _a2, _b2;
        if (opts.debug) {
          console.log("[JITSU DEBUG] Update Jitsu config with", JSON.stringify(options));
        }
        if (((_a2 = options.privacy) == null ? void 0 : _a2.disableUserIds) || ((_b2 = options.privacy) == null ? void 0 : _b2.dontSend)) {
          storage.reset();
        }
        for (const plugin of Object.values(analytics.plugins)) {
          if (typeof plugin["configure"] === "function") {
            plugin["configure"](options);
          }
        }
      });
    },
    group(groupId, traits, options, callback) {
      return __async(this, null, function* () {
        const results = [];
        for (const plugin of Object.values(analytics.plugins)) {
          if (plugin["group"]) {
            results.push(yield plugin["group"](groupId, traits, options, callback));
          }
        }
        return results[0];
      });
    },
    getConfiguration() {
      return opts;
    }
  });
  if (((_e = opts.privacy) == null ? void 0 : _e.disableUserIds) || ((_f = opts.privacy) == null ? void 0 : _f.dontSend)) {
    storage.reset();
  } else if (opts.userId) {
    a.identify(opts.userId, {}, { autoIdentify: true }).catch((err) => {
      if (opts.debug) {
        console.warn("[JITSU WARNING] Auto-identification failed on initialization:", err);
      }
    });
  }
  return a;
}
function fixOptions(opts) {
  var _a, _b;
  return __spreadProps(__spreadValues({}, opts), {
    host: ((_a = opts.host) != null ? _a : "").indexOf("https://") !== 0 && ((_b = opts.host) != null ? _b : "").indexOf("http://") !== 0 ? `https://${opts.host}` : opts.host
  });
}
function jitsuAnalytics(_opts) {
  const opts = fixOptions(_opts);
  if (opts.preInitAnonymousId) {
    ensureAnonymousId(opts);
  }
  const inBrowser = isInBrowser();
  const rt = opts.runtime || (inBrowser ? windowRuntime(opts) : emptyRuntime(opts));
  return createUnderlyingAnalyticsInstance(opts, rt);
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  createInMemoryStorage,
  emptyAnalytics,
  emptyRuntime,
  ensureAnonymousId,
  isInBrowser,
  jitsuAnalytics,
  jitsuAnalyticsPlugin,
  parseQuery,
  randomId,
  uuid,
  windowRuntime
});
