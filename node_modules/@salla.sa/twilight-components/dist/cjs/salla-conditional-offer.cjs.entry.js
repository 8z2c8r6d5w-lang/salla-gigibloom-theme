/*!
 * Crafted with ❤ by Salla
 */
'use strict';

var index = require('./index-DnjGg4mr.js');

var DiscountType;
(function (DiscountType) {
    DiscountType["PERCENTAGE"] = "percentage";
    DiscountType["FIXED"] = "fixed";
    DiscountType["FREE_PRODUCT"] = "free_product";
})(DiscountType || (DiscountType = {}));
var OfferType;
(function (OfferType) {
    OfferType["PRODUCT_COUNT"] = "products_count";
    OfferType["PRODUCT_PURCHASE"] = "order_amount";
})(OfferType || (OfferType = {}));

const sallaConditionalOfferCss = "/*!twlt*/:host{display:block}.fixed{position:fixed}.absolute{position:absolute}.-top-2{top:-0.5rem}.left-0{left:0px}.mb-1{margin-bottom:0.25rem}.block{display:block}.flex{display:flex}.hidden{display:none}.h-12{height:3rem}.w-12{width:3rem}.flex-1{flex:1 1 0%}.flex-shrink-0{flex-shrink:0}.gap-3{gap:0.75rem}.rounded-lg{border-radius:0.5rem}.object-cover{object-fit:cover}.text-start{text-align:start}.text-xs{font-size:0.75rem;line-height:1rem}.leading-tight{line-height:1.25}.text-gray-300{--tw-text-opacity:1;color:rgb(209 213 219 / var(--tw-text-opacity, 1))}.filter{filter:var(--tw-blur) var(--tw-brightness) var(--tw-contrast) var(--tw-grayscale) var(--tw-hue-rotate) var(--tw-invert) var(--tw-saturate) var(--tw-sepia) var(--tw-drop-shadow)}.s-conditional-offer-container{position:relative;display:inline-flex;width:100%;flex-direction:column;align-items:center;justify-content:flex-start;gap:1.25rem;border-radius:0.375rem;--tw-bg-opacity:1;background-color:rgb(255 255 255 / var(--tw-bg-opacity, 1));padding-left:1.25rem;padding-right:1.25rem;padding-top:1rem;padding-bottom:4rem}@media (min-width: 768px){.s-conditional-offer-container{padding-left:2.5rem;padding-right:2.5rem;padding-top:2rem;padding-bottom:2rem}}.s-conditional-offer-title-wrapper{display:flex;flex-direction:column;align-items:center;justify-content:flex-start;align-self:stretch}.s-conditional-offer-title{text-align:center;font-size:1.25rem;line-height:1.75rem;font-weight:700;color:var(--color-primary-dark)}.s-conditional-offer-subtitle{display:inline-flex;align-items:center;justify-content:center;gap:0.25rem;align-self:stretch;text-align:center;font-size:0.75rem;line-height:1rem;font-weight:400;--tw-text-opacity:1;color:rgb(64 64 64 / var(--tw-text-opacity, 1))}.s-conditional-offer-subtitle i{position:relative}.s-conditional-offer-progress-line{height:1px;align-self:stretch;background-color:var(--color-primary)}.s-conditional-offer-checkpoint-border{display:flex;height:60px;min-width:60px;flex-direction:column;align-items:center;justify-content:center;border-radius:9999px;border-width:1px;--tw-bg-opacity:1;background-color:rgb(255 255 255 / var(--tw-bg-opacity, 1))}.s-conditional-offer-checkpoint-border-colored{border-color:var(--color-primary);color:var(--color-primary-dark)}.s-conditional-offer-product-link{display:grid;aspect-ratio:1 / 1;height:58px;place-content:center;overflow:hidden;border-radius:9999px}.s-conditional-offer-checkpoint-image-content{height:58px;width:58px;object-fit:cover}.s-conditional-offer-progress-container{position:relative;margin-bottom:2rem;display:flex;height:8rem;width:100%;flex-wrap:wrap;align-items:center;justify-content:center}@media (min-width: 768px){.s-conditional-offer-progress-container{height:4rem}}.s-conditional-offer-item-avatar-content{text-align:center;font-size:0.875rem;font-weight:700;line-height:1.25rem}.s-conditional-offer-item-avatar-content.active{color:var(--color-primary-dark);transition-property:all;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1);transition-duration:500ms;transition-timing-function:cubic-bezier(0, 0, 0.2, 1)}.s-conditional-offer-checkpoint{position:relative;display:flex;height:60px;width:60px;flex-shrink:0;flex-direction:column;align-items:center;justify-content:center;border-radius:9999px;border-width:1px}.s-conditional-offer-progress-line-container{display:grid;width:100%;overflow:hidden;grid-template-areas:'progress-line'}.s-conditional-offer-progress-line-inactive{height:1.5px;width:100%;--tw-bg-opacity:1;background-color:rgb(209 213 219 / var(--tw-bg-opacity, 1));transition-property:all;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1);transition-duration:500ms;transition-timing-function:cubic-bezier(0, 0, 0.2, 1);grid-area:progress-line}.s-conditional-offer-progress-line-active{height:1.5px;width:0%;background-color:var(--color-primary);transition-property:all;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1);transition-duration:500ms;transition-timing-function:cubic-bezier(0, 0, 0.2, 1);grid-area:progress-line}.s-conditional-offer-checkpoint-label{position:absolute;top:100%;left:50%;width:max-content;--tw-translate-x:-50%;--tw-translate-y:20%;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));transition-property:all;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1);transition-duration:500ms;transition-timing-function:cubic-bezier(0, 0, 0.2, 1)}.s-conditional-offer-checkpoint-label.active{color:var(--color-primary-dark);transition-property:all;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1);transition-duration:500ms;transition-timing-function:cubic-bezier(0, 0, 0.2, 1)}.s-conditional-offer-checkpoint-label.first-checkpoint{--tw-translate-y:132%;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y))}.s-conditional-offer-checkpoint-label.first-checkpoint:where([dir=\"ltr\"],[dir=\"ltr\"] *){--tw-translate-x:0%;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y))}.s-conditional-offer-checkpoint-label.first-checkpoint:where([dir=\"rtl\"],[dir=\"rtl\"] *){--tw-translate-x:-100%;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y))}.s-conditional-offer-checkpoint-container{position:relative;display:flex;align-items:center}.s-conditional-offer-active-checkpoint{display:flex;justify-content:center;border-color:var(--color-primary);transition-property:all;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1);transition-duration:500ms;transition-timing-function:cubic-bezier(0, 0, 0.2, 1)}.s-conditional-offer-skeleton-inner-container{margin-bottom:2rem;width:100%}@media (min-width: 768px){.s-conditional-offer-skeleton-inner-container{margin-bottom:0px}}.s-conditional-offer-skeleton-title{margin-bottom:1rem;display:flex;justify-content:center}.s-conditional-offer-skeleton-subtitle{margin-bottom:1.5rem;display:flex;justify-content:center}.s-conditional-offer-skeleton-checkpoints-wrapper{display:flex;align-items:center}@media (min-width: 640px){.sm\\:block{display:block}}";

const SallaConditionalOffer = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.offer = null;
        this.products = {};
        this.isLoading = false;
        this.canRender = true;
    }
    componentWillLoad() {
        salla.onReady().then(() => {
            if (!salla.config.get('store.features')?.includes('conditional-offer') || (salla.storage.get("cart") !== '' && !salla.storage.get("cart")?.summary?.count)) {
                throw new Error('feature or cart object does not existed');
            }
        })
            .then(() => salla.api.cart.offers())
            .then(({ data }) => {
            this.offer = data.find(offer => offer.type === "conditional");
            if (!this.offer) {
                this.canRender = false;
                return;
            }
            this.isLoading = true;
            this.offer.details.discounts.unshift(({ value: 0, min_spend: 0 }));
            return this.updateInitialOfferValue();
        }).then(() => {
            if (!this.offer)
                return;
            return this.getProducts();
        }).then(() => {
            if (!this.offer)
                return;
            salla.event.on("cart::updated", (updatedCart) => this.updateOfferValues(updatedCart));
        })
            .catch(error => {
            this.canRender = false;
            salla.logger.warn('salla-conditional-offer:: ', error);
        }).finally(() => {
            this.isLoading = false;
        });
    }
    getProducts() {
        const freeProductIDs = this.offer.details.discounts.filter(({ type }) => type === DiscountType.FREE_PRODUCT).map(({ value }) => value).filter(Boolean);
        if (freeProductIDs.length > 0) {
            return salla.product.fetch({ source: 'selected', source_value: freeProductIDs }).then((response) => {
                const data = response?.data;
                if (data && Array.isArray(data)) {
                    const updatedProducts = { ...this.products };
                    data.forEach(({ id, url, image, name, price }) => {
                        if (id && name && image?.url && url && price !== undefined) {
                            updatedProducts[id] = { url, image, name, price };
                        }
                    });
                    this.products = updatedProducts;
                }
            });
        }
    }
    updateInitialOfferValue() {
        return salla.api.cart.details().then(({ data: { cart } }) => this.updateOfferValues(cart));
    }
    updateOfferValues({ items, sub_total }) {
        const current_value = this.offer.details.based_on === OfferType.PRODUCT_COUNT ? items?.reduce((count, { quantity }) => count + quantity, 0) ?? 0 : sub_total;
        //we need to assign the offer object a new reference to trigger re-rendering
        this.offer = {
            ...this.offer, details: {
                ...this.offer?.details, current_value,
            }
        };
        return this.offer;
    }
    renderTooltip(discount) {
        const productId = discount.value;
        const productItem = this.products[productId];
        const targetId = `avatar-product-${productId}`;
        if (!productItem) {
            salla.logger.error(`salla-conditional-offer:: there is no product with id (${discount.value})!`);
            return null;
        }
        return index.h("salla-tooltip", { class: 'absolute left-0 -top-2', targetId: targetId, theme: "dark" }, index.h("div", { class: "flex gap-3 text-start" }, productItem?.image?.url && index.h("img", { src: productItem.image.url, alt: productItem?.image?.alt || productItem.name || "", class: "w-12 h-12 object-cover rounded-lg flex-shrink-0", loading: "lazy", decoding: "async" }), index.h("div", { class: "flex-1" }, index.h("div", { class: "leading-tight mb-1" }, productItem.name), index.h("div", { class: "text-xs text-gray-300", innerHTML: salla.money(productItem.price) }))));
    }
    getCheckpointContent(discount) {
        if (discount.type === DiscountType.PERCENTAGE)
            return `${discount.value}%`;
        if (discount.type === DiscountType.FIXED)
            return index.h("span", { innerHTML: salla.money(discount.value) });
        if (discount.type !== DiscountType.FREE_PRODUCT) {
            salla.logger.error(`salla-conditional-offer:: unexpected type (${discount.type})!`);
            return "";
        }
        const productItem = this.products[discount.value];
        if (!productItem) {
            salla.logger.error(`salla-conditional-offer:: there is no product with id (${discount.value})!`);
            return "";
        }
        return index.h("a", { class: "s-conditional-offer-product-link", href: productItem.url }, index.h("img", { class: "s-conditional-offer-checkpoint-image-content", loading: "lazy", decoding: "async", alt: productItem?.image?.alt || "", src: productItem?.image?.url }));
    }
    getOfferType(discount) {
        const basedOn = this.offer.details?.based_on;
        if (basedOn === OfferType.PRODUCT_COUNT) {
            try {
                return salla.lang.choice("blocks.header.products_count", discount.min_spend);
            }
            catch (error) {
                // Some stores may define an invalid interval (e.g. [1,*]) in translations.
                // Fallback to a safe plain label so the component does not crash.
                salla.logger.warn("salla-conditional-offer:: invalid products_count interval", error);
                return `${discount.min_spend}`;
            }
        }
        if (basedOn === OfferType.PRODUCT_PURCHASE)
            return salla.money(discount.min_spend);
        salla.logger.warn(`salla-conditional-offer:: Unexpected offer detail's based_on value: ${basedOn}`);
        return `${discount.min_spend}`;
    }
    clamp(value, min, max) {
        return Math.max(min, Math.min(value, max));
    }
    mapValueRanges(value, initialMinRange, initialMaxRange, newMinRange, newMaxRange) {
        const newRange = ((value - initialMinRange) * (newMaxRange - newMinRange)) / (initialMaxRange - initialMinRange) + newMinRange;
        if (newRange === Number.POSITIVE_INFINITY)
            return 100;
        if (newRange === Number.NEGATIVE_INFINITY)
            return 0;
        return this.clamp(newRange, 0, 100);
    }
    getCheckPointView(discount, index$1) {
        const checkpointIndex = this.offer.details.discounts.findIndex(({ min_spend }) => min_spend === discount.min_spend);
        const previousCheckpointValue = this.offer.details.discounts[checkpointIndex - 1]?.min_spend ?? 0;
        const progressPercentage = this.mapValueRanges(this.offer.details.current_value, previousCheckpointValue, discount.min_spend, 0, 100);
        const isActive = discount.min_spend <= this.offer.details.current_value;
        return (index.h("div", { class: "s-conditional-offer-checkpoint-container" }, index$1 > 0 ? [
            index.h("div", { key: "progress-line", class: "s-conditional-offer-progress-line-container" }, index.h("div", { class: "s-conditional-offer-progress-line-inactive" }), index.h("div", { class: "s-conditional-offer-progress-line-active", style: { width: `${progressPercentage}%` } })),
            index.h("div", { key: "checkpoint", class: `s-conditional-offer-checkpoint ${isActive ? "s-conditional-offer-active-checkpoint" : ""}` }, index.h("div", { class: `s-conditional-offer-item-avatar-content ${isActive ? "active" : ""}`, id: discount.type === DiscountType.FREE_PRODUCT ? `avatar-product-${discount.value}` : `avatar-${discount.value ?? index$1}` }, this.products && this.getCheckpointContent(discount)), this.products && discount.type === DiscountType.FREE_PRODUCT && this.renderTooltip(discount), index.h("div", { class: `s-conditional-offer-checkpoint-label ${isActive ? "active" : ""}`, innerHTML: this.getOfferType(discount) }))
        ] :
            index.h("div", { key: "label", class: { "s-conditional-offer-checkpoint-label": true, "first-checkpoint": index$1 === 0, active: isActive, "hidden": index$1 === 0, "sm:block": index$1 === 0 }, innerHTML: this.getOfferType(discount) })));
    }
    getLoadingSkeletonView() {
        return index.h(index.Host, { class: "s-conditional-offer-container" }, index.h("div", { class: "s-conditional-offer-skeleton-inner-container" }, index.h("div", { class: "s-conditional-offer-skeleton-subtitle" }, index.h("salla-skeleton", { height: "16px", width: "30%" })), index.h("div", { class: "s-conditional-offer-skeleton-subtitle" }, index.h("salla-skeleton", { height: "16px", width: "35%" })), index.h("div", { class: "s-conditional-offer-skeleton-checkpoints-wrapper" }, Array(3).fill(null).map(() => ([index.h("salla-skeleton", { key: "checkpoint-line", height: "8px" }), index.h("div", { key: "checkpoint" }, index.h("salla-skeleton", { height: "60px", width: "60px", type: "circle" }))])))));
    }
    render() {
        if (!this.canRender)
            return null;
        if (this.isLoading)
            return this.getLoadingSkeletonView();
        if (!this.offer)
            return null;
        return index.h(index.Host, { class: "s-conditional-offer-container" }, index.h("div", { class: "s-conditional-offer-title-wrapper" }, index.h("div", { class: "s-conditional-offer-title" }, this.offer.title), this.offer.description ? index.h("div", { class: "s-conditional-offer-subtitle" }, this.offer.description, " ", index.h("i", { class: "sicon-information" })) : null), index.h("div", { class: "s-conditional-offer-progress-container" }, this.offer.details.discounts.map((discount, index$1) => (index.h("div", { class: { "flex-1": index$1 > 0 }, key: discount.min_spend }, this.getCheckPointView(discount, index$1))))));
    }
};
SallaConditionalOffer.style = sallaConditionalOfferCss;

exports.salla_conditional_offer = SallaConditionalOffer;
