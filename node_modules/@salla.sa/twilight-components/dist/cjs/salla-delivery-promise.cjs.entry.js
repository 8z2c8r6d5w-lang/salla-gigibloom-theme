/*!
 * Crafted with ❤ by Salla
 */
'use strict';

var index = require('./index-DnjGg4mr.js');
var helpers = require('./helpers-DBynEhOO.js');

const DEBUG_KEY = 'salla-delivery-promise-debug';
function log(message, data) {
    if (localStorage.getItem(DEBUG_KEY)) {
        data !== undefined ? console.log(message, data) : console.log(message);
    }
}
/** Fetch available cities for a product's delivery promises. */
async function fetchCities(productId, keyword) {
    log('fetchCities start', { productId, keyword });
    const queryString = keyword != null ? `?keyword=${encodeURIComponent(keyword)}` : '';
    const response = await salla.api.request(salla.url.api(`products/${productId}/delivery-promises/cities${queryString}`));
    if (!response.success || !Array.isArray(response.data) || response.data.length === 0) {
        log('fetchCities: no data', { response });
        return { cities: [] };
    }
    log('fetchCities success', { count: response.data.length });
    return { cities: response.data };
}
/** Fetch the delivery promise message for a given city or branch. */
async function fetchDeliveryMessage(productId, id, optionType, isLoginCycleEnabled) {
    log('fetchDeliveryMessage start', { productId, id, optionType });
    let queryString = '';
    if (optionType === 'city' && !isLoginCycleEnabled) {
        queryString = `?reference_id=${id}`;
    }
    else {
        queryString = `?reference_id=${id}&reference_type=${optionType}`;
    }
    const response = (await salla.api.withoutNotifier(() => salla.api.request(salla.url.api(`products/${productId}/delivery-promises${queryString}`))));
    if (!response.success) {
        log('fetchDeliveryMessage error', { response });
        return null;
    }
    const message = response.data?.message?.trim();
    log('fetchDeliveryMessage success', { message });
    return message || null;
}

/**
 * Bullet delivery intent helpers.
 * Reads/writes the same storage key as salla-bullet-delivery so both components stay in sync.
 */
const INTENT_KEY = 'bullet_delivery_intent';
function getIntentStorage() {
    const rememberLastSession = Boolean(salla.config.get('store.settings.bullet_delivery.settings.remember_last_session'));
    return rememberLastSession ? salla.storage.store : salla.storage.session;
}
function getStoredIntent() {
    const raw = getIntentStorage().get(INTENT_KEY);
    return raw && typeof raw === 'object' ? raw : null;
}
/** Resolve the stored intent into a city or branch selection. Returns null when no usable intent. */
function resolveIntent() {
    const intent = getStoredIntent();
    if (!intent)
        return null;
    if (intent.type === 'address') {
        const city = intent.address_details?.city;
        const cityId = city?.id != null ? Number(city.id) : intent.city_id != null ? Number(intent.city_id) : null;
        const cityName = city?.name?.trim() || '';
        if (cityId != null) {
            return { type: 'address', option: { id: cityId, name: cityName } };
        }
        // Map-origin pick: no city id to query a delivery estimate by, but still show the address.
        if (helpers.isMapOriginIntent(intent) && intent.address_details?.formatted_address) {
            return { type: 'address', option: { name: intent.address_details.formatted_address } };
        }
        return null;
    }
    if (intent.type === 'branch') {
        const branchId = intent.branch_id != null
            ? Number(intent.branch_id)
            : intent.branch_details?.id != null
                ? Number(intent.branch_details.id)
                : null;
        const branchName = intent.branch_details?.name?.trim() || '';
        if (branchId != null) {
            return { type: 'branch', option: { id: branchId, name: branchName } };
        }
        return null;
    }
    return null;
}
/** Build the payload for opening the bullet delivery modal with preselection. */
function buildBulletOpenPayload() {
    const intent = getStoredIntent();
    const payload = {};
    if (intent?.type === 'address' && intent.address_id != null) {
        payload.preselected_address_id = Number(intent.address_id);
    }
    if (intent?.type === 'branch') {
        const branchId = intent.branch_id ?? intent.branch_details?.id;
        if (branchId != null) {
            payload.preselected_branch_id = Number(branchId);
        }
    }
    return payload;
}

const DEFAULT_LABELS = {
    deliveryTo: 'توصيل إلى',
    pickupFromBranch: 'الاستلام من فرع',
    noResults: 'لا توجد نتائج',
    selectCity: 'اختر المدينة',
    changeCityTitle: 'تغيير المدينة',
    changeCitySubtitle: '',
    cityFieldLabel: 'المدينة',
    modalSearchPlaceholder: 'ابحث عن مدينة',
    confirmAddress: 'تأكيد العنوان',
    errorDeliveryPromise: 'لا يتوفر وعد تسليم لهذه المدينة',
    errorPickupPromise: 'لا يتوفر وعد تسليم لهذا الفرع',
};
function loadLabels() {
    return {
        deliveryTo: salla.lang.getWithDefault('pages.products.promise_deliver_to', DEFAULT_LABELS.deliveryTo),
        pickupFromBranch: salla.lang.getWithDefault('pages.products.promise_pickup_from_branch', DEFAULT_LABELS.pickupFromBranch),
        noResults: salla.lang.getWithDefault('common.elements.no_options', DEFAULT_LABELS.noResults),
        selectCity: salla.lang.getWithDefault('common.elements.select_city', DEFAULT_LABELS.selectCity),
        changeCityTitle: salla.lang.getWithDefault('pages.products.promise_change_city_title', DEFAULT_LABELS.changeCityTitle),
        changeCitySubtitle: salla.lang.getWithDefault('pages.products.promise_change_city_subtitle', 'قد تتغيّر مدة التوصيل حسب المدينة.'),
        cityFieldLabel: salla.lang.getWithDefault('pages.products.promise_city_field', DEFAULT_LABELS.cityFieldLabel),
        modalSearchPlaceholder: salla.lang.getWithDefault('pages.products.promise_search_city', DEFAULT_LABELS.modalSearchPlaceholder),
        confirmAddress: salla.lang.getWithDefault('pages.checkout.confirm_address', DEFAULT_LABELS.confirmAddress),
        errorDeliveryPromise: salla.lang.getWithDefault('pages.products.promise_delivery_not_available', DEFAULT_LABELS.errorDeliveryPromise),
        errorPickupPromise: salla.lang.getWithDefault('pages.products.promise_pickup_not_available', DEFAULT_LABELS.errorPickupPromise),
    };
}
/** Localized city display name (English when available and user language is not Arabic). */
function getCityDisplayName(city) {
    const lang = salla.config.get('user.language_code');
    if (lang && lang !== 'ar' && city.name_en?.trim()) {
        return city.name_en.trim();
    }
    return city.name;
}

const sallaDeliveryPromiseCss = "/*!twlt*/@charset \"UTF-8\";:host{display:block}.s-delivery-promise-wrapper{font-family:inherit}.s-delivery-promise-container{position:relative;background:var(--color-surface, #fff);border:1px solid var(--color-border, #e5e7eb);border-radius:8px;padding:12px 16px;margin-bottom:12px}.s-delivery-promise-header{display:flex;align-items:center;cursor:pointer;padding:4px 0}.s-delivery-promise-skeleton .s-delivery-promise-header-skeleton{cursor:default;pointer-events:none}.s-delivery-promise-skeleton .s-delivery-promise-location salla-skeleton{display:block;flex-shrink:0}.s-delivery-promise-location{display:flex;align-items:center;gap:8px}.s-delivery-promise-title{color:var(--color-text-primary, #2b2d34);font-size:14px;font-weight:bold}.s-delivery-promise-arrow{color:var(--color-text-secondary, #666);font-size:18px;transition:transform 0.2s ease}.s-delivery-promise-arrow.open{transform:rotate(180deg)}.s-delivery-promise-city-modal{}.s-delivery-promise-city-modal .s-modal-title{color:var(--color-primary, #00897b);font-weight:700}.s-delivery-promise-city-modal .s-modal-sub-title{color:var(--color-text-secondary, #5c6c7c);font-size:14px;line-height:1.45}.s-delivery-promise-city-modal .s-modal-body{overflow:visible !important}.s-delivery-promise-city-modal .s-modal-body>salla-button{position:relative;z-index:0;margin-top:16px}.s-delivery-promise-modal-body{position:relative;z-index:10;display:flex;flex-direction:column;gap:12px;text-align:start;margin-bottom:16px}.s-delivery-promise-modal-combobox{position:relative;isolation:isolate}.s-delivery-promise-modal-field-label{display:block;font-size:14px;font-weight:500;color:var(--color-text-primary, #2b2d34)}.s-delivery-promise-required{color:var(--color-danger, #dc3545)}.s-delivery-promise-modal-search{display:flex;align-items:center;gap:8px;padding:10px 12px;border:1px solid var(--color-border, #e5e7eb);border-radius:8px;background:var(--color-surface, #fff)}.s-delivery-promise-modal-search .sicon-search{flex-shrink:0;color:var(--color-text-secondary, #999);font-size:16px}.s-delivery-promise-modal-search input{flex:1;border:none;outline:none;font-size:14px;background:transparent;color:var(--color-text-primary, #333)}.s-delivery-promise-modal-search input::placeholder{color:var(--color-text-secondary, #999)}.s-delivery-promise-modal-popover{position:absolute;top:calc(100% + 4px);left:0;right:0;z-index:1;border-radius:8px;border:1px solid var(--color-border, #e5e7eb);background:var(--color-surface, #fff);box-shadow:0 8px 24px rgba(0, 0, 0, 0.12);animation:s-delivery-promise-popover-in 0.2s ease}@keyframes s-delivery-promise-popover-in{from{opacity:0;transform:translateY(-4px)}to{opacity:1;transform:translateY(0)}}.s-delivery-promise-modal-list{max-height:min(40vh, 260px);overflow-y:auto;background:var(--color-surface, #fff)}.s-delivery-promise-modal-item{display:flex;width:100%;align-items:center;justify-content:space-between;gap:8px;padding:12px 16px;margin:0;border:none;border-bottom:1px solid var(--color-border, #eee);cursor:pointer;font:inherit;text-align:inherit;background:transparent;color:var(--color-text-primary, #2b2d34);transition:background 0.15s ease}.s-delivery-promise-modal-item:last-child{border-bottom:none}.s-delivery-promise-modal-item:hover{background:var(--color-hover, #f5f5f5)}.s-delivery-promise-modal-item--selected{background:var(--color-primary-light, rgba(0, 137, 123, 0.08))}.s-delivery-promise-modal-item--selected .s-delivery-promise-modal-item-name{color:var(--color-primary, #00897b);font-weight:600}.s-delivery-promise-modal-item .sicon-check{flex-shrink:0;color:var(--color-primary, #00897b);font-size:14px}.s-delivery-promise-modal-empty{padding:24px 16px;text-align:center;color:var(--color-text-secondary, #999);font-size:14px}.s-delivery-promise-message{margin-top:8px;font-size:13px;line-height:1.5;color:var(--color-text-secondary, #2b2d34)}.s-delivery-promise-loading{margin-top:12px;padding-top:12px}.s-delivery-promise-error{margin-top:12px;padding-top:12px;font-size:14px;color:var(--color-danger, #dc3545)}.shadow{--tw-shadow:0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);--tw-shadow-colored:0 1px 3px 0 var(--tw-shadow-color), 0 1px 2px -1px var(--tw-shadow-color);box-shadow:var(--tw-ring-offset-shadow, 0 0 #0000), var(--tw-ring-shadow, 0 0 #0000), var(--tw-shadow)}.filter{filter:var(--tw-blur) var(--tw-brightness) var(--tw-contrast) var(--tw-grayscale) var(--tw-hue-rotate) var(--tw-invert) var(--tw-saturate) var(--tw-sepia) var(--tw-drop-shadow)}.s-delivery-promise-wrapper{display:block;font-family:inherit}.s-delivery-promise-container{position:relative;margin-bottom:0.75rem;border-radius:0.5rem;border-width:1px;border-color:var(--color-border,#e5e7eb);background-color:var(--color-surface,#fff);padding-left:1rem;padding-right:1rem;padding-top:0.75rem;padding-bottom:0.75rem}.s-delivery-promise-header{display:flex;cursor:pointer;align-items:center;padding-top:0.25rem;padding-bottom:0.25rem}.s-delivery-promise-skeleton .s-delivery-promise-header-skeleton{pointer-events:none;cursor:default}.s-delivery-promise-skeleton .s-delivery-promise-location salla-skeleton{display:block;flex-shrink:0}.s-delivery-promise-location{display:flex;align-items:center;gap:0.5rem}.s-delivery-promise-title{font-size:0.875rem;line-height:1.25rem;font-weight:700;color:var(--color-text-primary,#2b2d34)}.s-delivery-promise-arrow{font-size:1.125rem;line-height:1.75rem;color:var(--color-text-secondary,#666);transition-property:transform;transition-duration:200ms;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1)}.s-delivery-promise-arrow.open{--tw-rotate:180deg;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y))}.s-delivery-promise-message{margin-top:0.5rem;font-size:13px;line-height:1.5;color:var(--color-text-secondary,#2b2d34)}.s-delivery-promise-loading{margin-top:0.75rem;padding-top:0.75rem}.s-delivery-promise-error{margin-top:0.75rem;padding-top:0.75rem;font-size:0.875rem;line-height:1.25rem;color:var(--color-danger,#dc3545)}.s-delivery-promise-city-modal .s-modal-title{font-weight:700;color:var(--color-primary,#00897b)}.s-delivery-promise-city-modal .s-modal-sub-title{font-size:0.875rem;line-height:1.25rem;line-height:1.45;color:var(--color-text-secondary,#5c6c7c)}.s-delivery-promise-city-modal .s-modal-body{overflow:visible !important}.s-delivery-promise-city-modal .s-modal-body>salla-button{position:relative;z-index:0;margin-top:1rem}.s-delivery-promise-modal-body{position:relative;z-index:10;margin-bottom:1rem;display:flex;flex-direction:column;gap:0.75rem;text-align:start}.s-delivery-promise-modal-combobox{position:relative;isolation:isolate}.s-delivery-promise-modal-field-label{display:block;font-size:0.875rem;line-height:1.25rem;font-weight:500;color:var(--color-text-primary,#2b2d34)}.s-delivery-promise-modal-required{color:var(--color-danger,#dc3545)}.s-delivery-promise-modal-search{display:flex;align-items:center;gap:0.5rem;border-radius:0.5rem;border-width:1px;border-color:var(--color-border,#e5e7eb);background-color:var(--color-surface,#fff);padding-left:0.75rem;padding-right:0.75rem;padding-top:0.625rem;padding-bottom:0.625rem}.s-delivery-promise-modal-search .sicon-search{flex-shrink:0;font-size:1rem;line-height:1.5rem;color:var(--color-text-secondary,#999)}.s-delivery-promise-modal-search input{min-width:0px;flex:1 1 0%;border-width:0px;background-color:transparent;font-size:0.875rem;line-height:1.25rem;color:var(--color-text-primary,#333);outline:2px solid transparent;outline-offset:2px}.s-delivery-promise-modal-search input::placeholder{color:var(--color-text-secondary,#999)}.s-delivery-promise-modal-popover{position:absolute;left:0px;right:0px;top:calc(100% + 4px);z-index:1;border-radius:0.5rem;border-width:1px;border-color:var(--color-border,#e5e7eb);background-color:var(--color-surface,#fff);--tw-shadow:0 8px 24px rgba(0,0,0,0.12);--tw-shadow-colored:0 8px 24px var(--tw-shadow-color);box-shadow:var(--tw-ring-offset-shadow, 0 0 #0000), var(--tw-ring-shadow, 0 0 #0000), var(--tw-shadow)}.s-delivery-promise-modal-list{max-height:min(40vh, 260px);overflow-y:auto;background-color:var(--color-surface,#fff)}.s-delivery-promise-modal-item{margin:0px;display:flex;width:100%;cursor:pointer;align-items:center;justify-content:space-between;gap:0.5rem;border-width:0px;border-bottom-width:1px;border-color:var(--color-border,#eee);background-color:transparent;padding-left:1rem;padding-right:1rem;padding-top:0.75rem;padding-bottom:0.75rem;text-align:start;color:var(--color-text-primary,#2b2d34);transition-property:color, background-color, border-color, text-decoration-color, fill, stroke;transition-duration:150ms;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1)}.s-delivery-promise-modal-item:last-child{border-bottom-width:0px}.s-delivery-promise-modal-item--selected{background-color:var(--color-primary-light,rgba(0,137,123,0.08))}.s-delivery-promise-modal-item--selected .s-delivery-promise-modal-item-name{font-weight:600;color:var(--color-primary,#00897b)}.s-delivery-promise-modal-item .sicon-check{flex-shrink:0;font-size:0.875rem;line-height:1.25rem;color:var(--color-primary,#00897b)}.s-delivery-promise-modal-empty{padding-left:1rem;padding-right:1rem;padding-top:1.5rem;padding-bottom:1.5rem;text-align:center;font-size:0.875rem;line-height:1.25rem;color:var(--color-text-secondary,#999)}";

const OVERRIDE_IP_KEY = 'salla-bullet-delivery-override-ip';
const SallaDeliveryPromise = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        // ── Feature flags & config ────────────────────────────────────────────
        this.isDeliveryPromiseEnabled = false;
        this.isLoginCycleEnabled = false;
        this.canRender = false;
        // ── Shared display state ──────────────────────────────────────────────
        this.labels = DEFAULT_LABELS;
        this.selectedLabel = null;
        this.deliveryMessage = null;
        this.hasError = false;
        this.errorMessage = '';
        this.isLoadingPromises = false;
        // ── Login-cycle state (bullet delivery integration) ───────────────────
        this.selectedCityLoginCycle = null;
        this.selectedBranchLoginCycle = null;
        // ── Standard flow state (city list) ───────────────────────────────────
        this.cities = [];
        this.selectedCity = null;
        this.isLoadingCities = false;
        // ── City-change modal state (non–login-cycle) ─────────────────────────
        this.cityPendingSelection = null;
        this.modalCities = [];
        this.isSearchingCities = false;
        this.citySearchCounter = 0;
        // ═══════════════════════════════════════════════════════════════════════
        //  Login-cycle (bullet delivery) integration
        // ═══════════════════════════════════════════════════════════════════════
        this.onBulletDeliveryConfirmed = () => {
            this.applyBulletIntent();
        };
        this.handleCitySearch = (keyword) => {
            clearTimeout(this.citySearchTimer);
            if (!keyword.trim()) {
                this.modalCities = [...this.cities];
                this.isSearchingCities = false;
                return;
            }
            this.citySearchTimer = setTimeout(() => this.searchCitiesRemote(keyword), 300);
        };
        // ═══════════════════════════════════════════════════════════════════════
        //  Header click (dispatches to correct flow)
        // ═══════════════════════════════════════════════════════════════════════
        this.handleHeaderClick = (e) => {
            e.stopPropagation();
            if (this.isLoginCycleEnabled) {
                this.openBulletDeliveryModal();
            }
            else {
                void this.openCityChangeModal();
            }
        };
    }
    // ═══════════════════════════════════════════════════════════════════════
    //  Lifecycle
    // ═══════════════════════════════════════════════════════════════════════
    async componentDidLoad() {
        await salla.onReady();
        await salla.lang.onLoaded();
        this.labels = loadLabels();
        this.isDeliveryPromiseEnabled = salla.config.get('store.features', []).includes('delivery-promises');
        this.isLoginCycleEnabled = salla.config.get('store.features', []).includes('bullet-delivery-v2') && Salla.config.get('store.settings.bullet_delivery.enabled');
        this.productId = salla.config.get('page.id');
        if (!this.isDeliveryPromiseEnabled) {
            this.canRender = false;
            return;
        }
        if (this.isLoginCycleEnabled) {
            await this.initLoginCycleFlow();
        }
        else {
            this.selectedLabel = `${this.labels.deliveryTo} ${this.labels.selectCity}`;
            await this.loadCities();
        }
    }
    disconnectedCallback() {
        window.removeEventListener('bulletDeliveryConfirmed', this.onBulletDeliveryConfirmed);
    }
    getIPDeliveryLocation() {
        const configPath = "store.shipping.delivery_location";
        const ipAddress = {
            cityId: salla.config.get(`${configPath}.city_id`),
            cityName: salla.config.get(`${configPath}.city_name`),
        };
        return localStorage.getItem(OVERRIDE_IP_KEY) ?
            JSON.parse(localStorage.getItem(OVERRIDE_IP_KEY)) : ipAddress;
    }
    async initLoginCycleFlow() {
        const hasIntent = this.applyBulletIntent();
        const { cityId, cityName } = this.getIPDeliveryLocation();
        log("delivery promise login cycle flow getIPDeliveryLocation", this.getIPDeliveryLocation());
        if (cityId && cityName && !hasIntent) {
            this.selectedLabel = `${this.labels.deliveryTo} ${cityName}`;
            await this.loadDeliveryMessage(cityId, 'city');
        }
        this.canRender = true;
        window.addEventListener('bulletDeliveryConfirmed', this.onBulletDeliveryConfirmed);
    }
    applyBulletIntent() {
        const resolved = resolveIntent();
        if (!resolved) {
            this.selectedLabel = `${this.labels.deliveryTo} ${this.labels.selectCity}`;
            this.selectedCityLoginCycle = null;
            this.selectedBranchLoginCycle = null;
            this.deliveryMessage = null;
            return false;
        }
        if (resolved.type === 'address') {
            this.selectedCityLoginCycle = resolved.option;
            this.selectedBranchLoginCycle = null;
            this.selectedLabel = `${this.labels.deliveryTo} ${resolved.option.name || this.labels.selectCity}`;
            // Map-origin picks have no resolved city id — nothing to query a delivery estimate by.
            if (resolved.option.id != null) {
                void this.loadDeliveryMessage(resolved.option.id, 'city');
            }
            else {
                this.deliveryMessage = null;
            }
        }
        else {
            this.selectedBranchLoginCycle = resolved.option;
            this.selectedCityLoginCycle = null;
            this.selectedLabel = `${this.labels.pickupFromBranch} ${resolved.option.name}`;
            // Branch resolution in resolveIntent() always sets an id.
            void this.loadDeliveryMessage(resolved.option.id, 'branch');
        }
        return true;
    }
    openBulletDeliveryModal() {
        salla.event.emit('salla::bullet-delivery.modal.open.requested', buildBulletOpenPayload());
    }
    // ═══════════════════════════════════════════════════════════════════════
    //  Standard flow (city list, no login cycle)
    // ═══════════════════════════════════════════════════════════════════════
    async loadCities() {
        this.isLoadingCities = true;
        this.hasError = false;
        const { cityId, cityName } = this.getIPDeliveryLocation();
        log("delivery promise standard flow getIPDeliveryLocation", this.getIPDeliveryLocation());
        try {
            const { cities } = await fetchCities(this.productId);
            if (!cities.length) {
                this.canRender = false;
                return;
            }
            this.cities = cities;
            this.canRender = true;
            // get delivery message for the IP location
            if (cityId && cityName) {
                this.selectedLabel = `${this.labels.deliveryTo} ${cityName}`;
                const autoCity = this.cities.find(c => String(c.id) === String(cityId));
                if (autoCity) {
                    log("delivery promise standard flow autoCity found", autoCity);
                    this.selectedCity = autoCity;
                }
                await this.loadDeliveryMessage(cityId, 'city');
            }
        }
        catch (error) {
            this.hasError = true;
            this.errorMessage = error.message || 'Failed to load cities';
            this.canRender = false;
        }
        finally {
            this.isLoadingCities = false;
        }
    }
    // ═══════════════════════════════════════════════════════════════════════
    //  Delivery message (shared by both flows)
    // ═══════════════════════════════════════════════════════════════════════
    async loadDeliveryMessage(id, optionType) {
        this.isLoadingPromises = true;
        this.hasError = false;
        this.deliveryMessage = null;
        try {
            this.deliveryMessage = await fetchDeliveryMessage(this.productId, id, optionType, this.isLoginCycleEnabled);
        }
        catch (error) {
            log('loadDeliveryMessage failed', { id, optionType, error });
            this.deliveryMessage = null;
        }
        finally {
            this.isLoadingPromises = false;
        }
    }
    // ═══════════════════════════════════════════════════════════════════════
    //  City-change modal handlers (non–login-cycle)
    // ═══════════════════════════════════════════════════════════════════════
    async openCityChangeModal() {
        if (!this.cityModalRef)
            return;
        this.cityPendingSelection = null;
        this.modalCities = [...this.cities];
        await this.cityModalRef.setTitle(this.labels.changeCityTitle);
        await this.cityModalRef.open();
    }
    async searchCitiesRemote(keyword) {
        const requestId = ++this.citySearchCounter;
        this.isSearchingCities = true;
        try {
            const { cities } = await fetchCities(this.productId, keyword);
            if (requestId !== this.citySearchCounter)
                return;
            this.modalCities = cities;
        }
        catch {
            if (requestId !== this.citySearchCounter)
                return;
            this.modalCities = [];
        }
        finally {
            if (requestId === this.citySearchCounter) {
                this.isSearchingCities = false;
            }
        }
    }
    async handleConfirmCityModal() {
        if (!this.cityPendingSelection) {
            await this.cityModalRef?.close();
            return;
        }
        const city = this.cityPendingSelection;
        this.selectedCity = city;
        this.selectedLabel = `${this.labels.deliveryTo} ${getCityDisplayName(city)}`;
        await this.loadDeliveryMessage(city.id, 'city');
        await this.cityModalRef?.close();
    }
    // ═══════════════════════════════════════════════════════════════════════
    //  Render helpers
    // ═══════════════════════════════════════════════════════════════════════
    renderLoadingSkeleton() {
        return (index.h(index.Host, { class: "s-delivery-promise-wrapper s-delivery-promise-skeleton" }, index.h("div", { class: "s-delivery-promise-container" }, index.h("div", { class: "s-delivery-promise-header s-delivery-promise-header-skeleton" }, index.h("div", { class: "s-delivery-promise-location" }, index.h("salla-skeleton", { height: "14px", width: "180px" })), index.h("salla-skeleton", { height: "18px", width: "18px" })), index.h("div", { class: "s-delivery-promise-loading" }, index.h("salla-skeleton", { height: "20px", width: "80%" })))));
    }
    renderDeliveryMessage() {
        if (this.isLoadingPromises) {
            return (index.h("div", { class: "s-delivery-promise-loading" }, index.h("salla-skeleton", { height: "20px", width: "80%" })));
        }
        if (!this.deliveryMessage)
            return null;
        return index.h("div", { class: "s-delivery-promise-message" }, this.deliveryMessage);
    }
    renderCityModalBody() {
        return (index.h("div", { class: "s-delivery-promise-modal-body" }, index.h("salla-searchable-dropdown", { label: this.labels.cityFieldLabel, placeholder: this.labels.modalSearchPlaceholder, items: this.modalCities, selectedItem: this.cityPendingSelection, searching: this.isSearchingCities, required: true, noResultsText: this.labels.noResults, inputId: "s-delivery-promise-modal-search", onItemSelected: (e) => {
                this.cityPendingSelection = e.detail;
            }, onSearchInput: (e) => {
                this.handleCitySearch(e.detail);
            } })));
    }
    renderCityChangeModal() {
        if (this.isLoginCycleEnabled)
            return null;
        return (index.h("salla-modal", { id: "s-delivery-promise-city-modal", ref: el => { this.cityModalRef = el; }, class: "s-delivery-promise-city-modal", isClosable: true, width: "md", "modal-title": this.labels.changeCityTitle, subTitle: this.labels.changeCitySubtitle }, this.renderCityModalBody(), index.h("salla-button", { slot: "footer", width: "wide", disabled: !this.cityPendingSelection, onClick: () => this.handleConfirmCityModal() }, !this.isLoadingPromises && this.labels.confirmAddress)));
    }
    // ═══════════════════════════════════════════════════════════════════════
    //  Main render
    // ═══════════════════════════════════════════════════════════════════════
    render() {
        if (!this.isDeliveryPromiseEnabled)
            return null;
        if (this.isLoadingCities)
            return this.renderLoadingSkeleton();
        if (!this.canRender)
            return null;
        return (index.h(index.Host, { class: "s-delivery-promise-wrapper" }, index.h("div", { class: "s-delivery-promise-container" }, index.h("div", { class: "s-delivery-promise-header", onClick: this.handleHeaderClick }, index.h("div", { class: "s-delivery-promise-location" }, index.h("span", { class: "s-delivery-promise-title" }, this.selectedLabel)), index.h("i", { class: `sicon-keyboard_arrow_down s-delivery-promise-arrow` })), this.hasError && index.h("div", { class: "s-delivery-promise-error" }, this.errorMessage), !this.hasError && this.renderDeliveryMessage()), this.renderCityChangeModal()));
    }
    get host() { return index.getElement(this); }
};
SallaDeliveryPromise.style = sallaDeliveryPromiseCss;

exports.salla_delivery_promise = SallaDeliveryPromise;
