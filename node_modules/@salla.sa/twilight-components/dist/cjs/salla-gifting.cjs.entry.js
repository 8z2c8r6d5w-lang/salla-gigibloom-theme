/*!
 * Crafted with ❤ by Salla
 */
'use strict';

var index = require('./index-DnjGg4mr.js');
var anime_es = require('./anime.es-BqW8JHZi.js');
var arrowLeft = require('./arrow-left-xSEmRpp6.js');
var cancel = require('./cancel-De6vslRA.js');
var giftSharing = require('./gift-sharing-CStz46FA.js');
var applePayOrderOptions = require('./apple-pay-order-options-B036Wu4O.js');

var Images = `<svg version="1.1" xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
<title>images</title>
<path d="M22.667 29.333h-13.333c-3.676 0-6.667-2.991-6.667-6.667v-13.333c0-0.737-0.597-1.333-1.333-1.333s-1.333 0.596-1.333 1.333v13.333c0 5.145 4.187 9.333 9.333 9.333h13.333c0.736 0 1.333-0.596 1.333-1.333s-0.597-1.333-1.333-1.333zM22.667 5.333c-2.205 0-4 1.795-4 4s1.795 4 4 4 4-1.795 4-4-1.795-4-4-4zM22.667 10.667c-0.735 0-1.333-0.597-1.333-1.333s0.599-1.333 1.333-1.333 1.333 0.597 1.333 1.333-0.599 1.333-1.333 1.333zM25.333 0h-13.333c-3.676 0-6.667 2.991-6.667 6.667v13.333c0 1.055 0.268 2.040 0.707 2.927 0.039 0.099 0.088 0.188 0.151 0.277 1.137 2.053 3.301 3.463 5.809 3.463h13.333c3.676 0 6.667-2.991 6.667-6.667v-13.333c0-3.676-2.991-6.667-6.667-6.667zM12 24c-1.244 0-2.344-0.583-3.077-1.476l5.721-7.047 6.629 8.523zM29.333 20c0 2.205-1.795 4-4 4h-0.68l-8.933-11.485c-0.251-0.32-0.633-0.511-1.039-0.515h-0.015c-0.401 0-0.781 0.181-1.035 0.492l-5.632 6.935v-12.76c0-2.205 1.795-4 4-4h13.333c2.205 0 4 1.795 4 4z"></path>
</svg>
`;

var Alert = `<!-- Generated by IcoMoon.io -->
<svg version="1.1" xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 32 32">
<title>alert</title>
<path d="M16 13.333c-0.736 0-1.333 0.597-1.333 1.333v8c0 0.736 0.597 1.333 1.333 1.333s1.333-0.597 1.333-1.333v-8c0-0.736-0.597-1.333-1.333-1.333zM16 9.327c-0.736 0-1.333 0.604-1.333 1.34s0.597 1.333 1.333 1.333 1.333-0.597 1.333-1.333v-0.013c0-0.736-0.597-1.327-1.333-1.327zM27.313 4.687c-6.237-6.237-16.389-6.239-22.628 0-6.237 6.239-6.237 16.389 0 22.628 3.119 3.119 7.216 4.679 11.313 4.679s8.195-1.559 11.313-4.679c6.24-6.239 6.24-16.391 0.001-22.628zM25.428 25.428c-5.199 5.197-13.657 5.197-18.857 0-5.197-5.199-5.197-13.659 0-18.857 5.199-5.197 13.659-5.197 18.857 0 5.199 5.2 5.199 13.659 0 18.857z"></path>
</svg>
`;

const sallaGiftingCss = "/*!twlt*/.s-gifting-steps-wrapper{transition:height 0.8s cubic-bezier(0.55, 0, 0.1, 1) 0s}.s-gifting-select{background-image:url(\"data:image/svg+xml;utf8,<svg version='1.1' fill='gray' xmlns='http://www.w3.org/2000/svg' width='32' height='32' viewBox='0 0 32 32'><title>keyboard_arrow_down</title><path d='M9.875 11.104l6.125 6.125 6.125-6.125 1.875 1.875-8 8-8-8z'></path></svg>\");background-size:24px;background-repeat:no-repeat;background-position:99%;appearance:none}[dir=rtl] .s-gifting-select{background-position:5px}.s-gifting-widget-vertical .s-list-tile-item [slot=title]{display:flex;align-items:center;gap:1.5rem}.s-gifting-widget-vertical .s-list-tile-item-content{width:100%}.s-gifting-modal .s-searchable-dropdown-trigger{border-color:#d1d5db}.s-gifting-modal .s-searchable-dropdown-trigger:hover:not(.s-searchable-dropdown-trigger--disabled){border-color:#d1d5db}.s-gifting-modal .s-searchable-dropdown-trigger--open,.s-gifting-modal .s-searchable-dropdown-trigger:focus{border-color:var(--color-primary, #00897b);box-shadow:0 0 0 1px var(--color-primary, #00897b)}.s-gifting-modal .s-searchable-dropdown-trigger-placeholder{color:#000}.s-gifting-modal .s-gifting-image-section{margin-bottom:1.5rem}.s-gifting-modal .s-gifting-slider{margin-bottom:0.5rem}.s-gifting-modal .s-gifting-image{display:block}.s-gifting-modal .s-gifting-image img{display:block;width:auto;height:100px;object-fit:contain}.s-gifting-modal .s-gifting-image-section+.anime-item .s-gifting-text-title{margin-top:1rem}.pointer-events-none{pointer-events:none}.pointer-events-auto{pointer-events:auto}.static{position:static}.fixed{position:fixed}.relative{position:relative}.mt-1{margin-top:0.25rem}.block{display:block}.inline{display:inline}.hidden{display:none}.grow{flex-grow:1}.transform{transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y))}.resize{resize:both}.opacity-0{opacity:0}.outline{outline-style:solid}.transition{transition-property:color, background-color, border-color, text-decoration-color, fill, stroke, opacity, box-shadow, transform, filter, backdrop-filter;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1);transition-duration:150ms}.s-hidden{display:none}.has-error .s-form-label{color:#ff443a}.has-error .s-form-control,.has-error .s-tel-input-control,.has-error .s-datetime-picker-input{border-color:#ff443a;color:#ff443a}.s-hidden{display:none}.s-form-control{display:block;width:100%;border-radius:var(--s-radius, 0.25rem);border-width:1px;--tw-border-opacity:1;border-color:rgb(209 213 219 / var(--tw-border-opacity, 1));--tw-bg-opacity:1;background-color:rgb(255 255 255 / var(--tw-bg-opacity, 1));padding:0.5rem;font-size:0.875rem;line-height:1.25rem;--tw-text-opacity:1;color:rgb(17 24 39 / var(--tw-text-opacity, 1))}.s-form-control:focus{border-color:var(--color-primary);--tw-ring-offset-shadow:var(--tw-ring-inset) 0 0 0 var(--tw-ring-offset-width) var(--tw-ring-offset-color);--tw-ring-shadow:var(--tw-ring-inset) 0 0 0 calc(0px + var(--tw-ring-offset-width)) var(--tw-ring-color);box-shadow:var(--tw-ring-offset-shadow), var(--tw-ring-shadow), var(--tw-shadow, 0 0 #0000)}.s-form-label{margin-bottom:0.25rem;display:block;font-size:0.875rem;line-height:1.25rem;font-weight:500;--tw-text-opacity:1;color:rgb(17 24 39 / var(--tw-text-opacity, 1))}.s-form-group{margin-top:0.75rem;margin-bottom:0.75rem}.s-checkbox{cursor:pointer;--tw-border-opacity:1;border-color:rgb(209 213 219 / var(--tw-border-opacity, 1));appearance:none;background-color:#fff;margin:0;width:18px;height:18px;border-width:1px;border-radius:4px;display:grid;place-content:center}.s-checkbox:before{width:10px;height:10px;clip-path:polygon(14% 44%, 0 65%, 50% 100%, 100% 16%, 80% 0%, 43% 62%);transform:scale(0);transform-origin:bottom left;transition:120ms transform ease-in-out;box-shadow:inset 1em 1em #444;background-color:#444}.s-checkbox:checked:before{transform:scale(1)}.s-checkbox::before{--tw-content:'';content:var(--tw-content)}.s-form-has-error .s-form-label{color:#ff443a}.s-form-has-error .s-form-control,.s-form-has-error .s-tel-input-control,.s-form-has-error .s-datetime-picker-input{border-color:#ff443a;color:#ff443a}.s-gifting-widget .s-list-tile-item-icon{display:none}.s-gifting-widget-vertical .s-list-tile-item-icon{display:block}.s-gifting-widget-vertical .s-list-tile-item-title svg{fill:var(--color-primary)}.s-gifting-widget-vertical .s-list-tile-item-subtitle{padding-right:3.5rem;font-size:0.75rem;line-height:1rem;--tw-text-opacity:1;color:rgb(107 114 128 / var(--tw-text-opacity, 1))}.s-gifting-widget-vertical .s-list-tile-item{flex-wrap:wrap;padding:0px}@media (min-width: 1024px){.s-gifting-widget-vertical .s-list-tile-item-action{flex:1 1 0%}}.s-gifting-widget-vertical .widget-icon{--tw-translate-y:0.625rem;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));font-size:1.875rem;line-height:2.25rem;color:var(--color-primary)}.s-gifting-widget .s-button-text{display:flex}.s-gifting-widget .s-list-tile-item{flex-direction:column;justify-content:space-between;gap:1rem;border-radius:0.75rem;border-width:1px;--tw-border-opacity:1;border-color:rgb(229 231 235 / var(--tw-border-opacity, 1));padding:1.25rem;text-align:center}@media (min-width: 768px){.s-gifting-widget .s-list-tile-item{text-align:left}}@media (min-width: 1024px){.s-gifting-widget .s-list-tile-item{flex-direction:row}}@media (min-width: 768px){.s-gifting-widget .s-list-tile-item:where([dir=\"rtl\"],[dir=\"rtl\"] *){text-align:right}}.s-gifting-widget-horizontal .s-gifting-widget-action button{padding-left:2rem;padding-right:2rem;font-weight:400}.s-gifting-info{display:flex;align-items:center;gap:0.5rem;border-radius:0.5rem;--tw-border-opacity:1;border-color:rgb(191 219 254 / var(--tw-border-opacity, 1));--tw-bg-opacity:1;background-color:rgb(219 234 254 / var(--tw-bg-opacity, 1));padding:1rem;font-size:0.875rem;line-height:1.25rem;--tw-text-opacity:1;color:rgb(29 78 216 / var(--tw-text-opacity, 1))}.s-gifting-info svg{height:1.25rem;width:1.25rem;fill:#1d4ed8}.s-gifting-error{font-size:0.75rem;line-height:1rem;--tw-text-opacity:1;color:rgb(239 68 68 / var(--tw-text-opacity, 1))}.s-gifting-info span{font-size:0.75rem;line-height:1rem}.s-gifting-widget-action-content{display:flex;align-items:center}.s-gifting-widget-action-content svg{margin-left:0.5rem;margin-right:0.5rem}.s-gifting-modal-header{margin-bottom:1.25rem;display:flex;flex-direction:column;align-items:center;justify-content:center}.s-gifting-modal .s-modal-header{display:block}.s-gifting-modal .s-modal-body{position:relative !important;left:unset !important;bottom:unset !important;padding-bottom:2rem}@media (min-width: 640px){.s-gifting-modal .s-modal-body{padding-bottom:1.5rem}}.s-gifting-modal-icon{margin-bottom:1.25rem;display:flex;height:5rem;width:5rem;flex-direction:row;align-items:center;justify-content:center;border-radius:9999px;border-width:1px;--tw-border-opacity:1;border-color:rgb(229 231 235 / var(--tw-border-opacity, 1));font-size:1.875rem;line-height:2.25rem}.s-gifting-modal-title{display:flex;width:100%;align-items:center;justify-content:space-between}.s-gifting-modal-title::before{display:block;height:1px;flex:1 1 auto;--tw-bg-opacity:1;background-color:rgb(229 231 235 / var(--tw-bg-opacity, 1));--tw-content:'';content:var(--tw-content)}.s-gifting-modal-title::after{display:block;height:1px;flex:1 1 auto;--tw-bg-opacity:1;background-color:rgb(229 231 235 / var(--tw-bg-opacity, 1));--tw-content:'';content:var(--tw-content)}.s-gifting-modal-badge-wrapper{display:flex;align-items:center;padding-left:0.75rem;padding-right:0.75rem}.s-gifting-modal-badge{margin-right:0.5rem;border-radius:0.5rem;--tw-bg-opacity:1;background-color:rgb(229 231 235 / var(--tw-bg-opacity, 1));padding-left:0.5rem;padding-right:0.5rem;padding-top:0.25rem;padding-bottom:0.25rem;font-size:0.875rem;line-height:1.25rem}.s-gifting-modal-badge:where([dir=\"rtl\"],[dir=\"rtl\"] *){margin-right:0px;margin-left:0.5rem}.s-gifting-modal-uploader.has-bg{position:relative}.s-gifting-modal-uploader.has-bg .s-gifting-remove-preview{pointer-events:auto;--tw-scale-x:1;--tw-scale-y:1;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));opacity:1}.s-gifting-modal-uploader-title{margin-bottom:0.5rem;font-size:0.875rem;line-height:1.25rem}.s-gifting-steps-wrapper{position:relative}.s-gifting-steps-wrapper .s-button-wrap{pointer-events:auto !important}.is-current-step-2 .gift-step-1,.is-current-step-1 .gift-step-2{pointer-events:none;z-index:-1}.s-gifting-step-one{padding-top:1.25rem}.s-gifting-step-two{position:absolute;top:0px;left:0px;margin-left:auto;margin-right:auto;display:flex;width:100%;--tw-translate-x:-105%;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));flex-direction:column;opacity:0}.s-gifting-step-one .swiper-wrapper{height:100px}.s-gifting-step-one .swiper-slide{width:auto !important}.s-gifting-step-one .filepond--list-scroller{overflow-y:hidden}.s-gifting-step-one .swiper-wrapper img{border-radius:0.375rem;object-fit:cover}.s-gifting-step-one .filepond--root{height:230px}.s-gifting-step-one .filepond--list{left:0px !important;height:100% !important}.s-gifting-step-one .filepond-bg{position:absolute;top:0px;z-index:10;height:100%;width:100%;border-radius:var(--s-radius, 0.25rem);--tw-bg-opacity:1;background-color:rgb(55 65 81 / var(--tw-bg-opacity, 1));background-size:cover}.s-gifting-step-one .s-slider-button-next{top:35% !important}.s-gifting-step-one .s-slider-button-next button{height:35px;width:35px;--tw-shadow:0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);--tw-shadow-colored:0 1px 3px 0 var(--tw-shadow-color), 0 1px 2px -1px var(--tw-shadow-color);box-shadow:var(--tw-ring-offset-shadow, 0 0 #0000), var(--tw-ring-shadow, 0 0 #0000), var(--tw-shadow)}.s-gifting-step-one .s-slider-button-prev{top:35% !important}.s-gifting-step-one .s-slider-button-prev button{height:35px;width:35px;--tw-shadow:0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);--tw-shadow-colored:0 1px 3px 0 var(--tw-shadow-color), 0 1px 2px -1px var(--tw-shadow-color);box-shadow:var(--tw-ring-offset-shadow, 0 0 #0000), var(--tw-ring-shadow, 0 0 #0000), var(--tw-shadow)}.s-gifting-tel-input .s-tel-input-control{--tw-border-opacity:1;border-color:rgb(209 213 219 / var(--tw-border-opacity, 1))}.s-gifting-tel-input .s-tel-input-error-msg{display:none}.s-gifting-step-two-footer{margin-top:1.25rem;display:flex;flex-direction:row;align-items:center;justify-content:center}.s-gifting-step-two-footer>:not([hidden])~:not([hidden]){--tw-space-x-reverse:0;margin-right:calc(0.625rem * var(--tw-space-x-reverse));margin-left:calc(0.625rem * calc(1 - var(--tw-space-x-reverse)))}.s-gifting-step-two-footer:where([dir=\"rtl\"],[dir=\"rtl\"] *){flex-direction:row-reverse}.s-gifting-step-two-footer a{border-radius:0.375rem;border-width:1px;border-color:var(--color-primary);fill:var(--color-primary);padding-top:8px;padding-bottom:8px;padding-left:25px;padding-right:25px}.s-gifting-step-two-footer a svg{height:1.25rem;width:1.25rem}.s-gifting-step-two .s-form-group.s-form-has-error .s-tel-input-control{--tw-border-opacity:1 !important;border-color:rgb(255 68 58 / var(--tw-border-opacity, 1)) !important}.s-gifting-selectText{margin-bottom:20px !important}.s-gifting-schedule .s-form-label{margin-bottom:0px !important}.s-gifting-schedule{margin-bottom:0.5rem;display:flex;align-items:center;justify-content:flex-start}.s-gifting-schedule>:not([hidden])~:not([hidden]){--tw-space-x-reverse:0;margin-right:calc(0.375rem * var(--tw-space-x-reverse));margin-left:calc(0.375rem * calc(1 - var(--tw-space-x-reverse)))}.s-gifting-schedule:where([dir=\"rtl\"],[dir=\"rtl\"] *)>:not([hidden])~:not([hidden]){--tw-space-x-reverse:1}.s-gifting-clickable{cursor:pointer}.s-gifting-remove-preview{pointer-events:none;position:absolute;top:-7px;left:-7px;z-index:10;display:flex;height:1.25rem;width:1.25rem;--tw-scale-x:0;--tw-scale-y:0;transform:translate(var(--tw-translate-x), var(--tw-translate-y)) rotate(var(--tw-rotate)) skewX(var(--tw-skew-x)) skewY(var(--tw-skew-y)) scaleX(var(--tw-scale-x)) scaleY(var(--tw-scale-y));cursor:pointer;align-items:center;justify-content:center;border-radius:9999px;--tw-bg-opacity:1;background-color:rgb(185 28 28 / var(--tw-bg-opacity, 1));text-align:center;font-size:1rem;line-height:1.5rem;--tw-text-opacity:1;color:rgb(255 255 255 / var(--tw-text-opacity, 1));opacity:0;transition-property:all;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1);transition-duration:300ms}.s-gifting-remove-preview:where([dir=\"rtl\"],[dir=\"rtl\"] *){left:unset;right:-7px}.s-gifting-remove-preview svg{height:100%;width:15px;fill:#fff}.s-gifting-textarea{transition-property:all;transition-duration:200ms;transition-timing-function:cubic-bezier(0.4, 0, 0.2, 1)}.s-gifting-calendar-hint{font-size:0.75rem;line-height:1rem;--tw-text-opacity:1;color:rgb(220 38 38 / var(--tw-text-opacity, 1))}.s-gifting-calendar.shown{margin-top:0.75rem;height:75px;opacity:1}.s-gifting-textarea.shown{height:122px;opacity:1}.s-gifting-calendar.hide{pointer-events:none;visibility:hidden;margin:0px;height:0px;opacity:0}.s-gifting-textarea.hide{pointer-events:none;visibility:hidden;margin:0px;height:0px;opacity:0}.s-gifting-filepond-placeholder-icon svg{margin-top:12px;margin-bottom:12px;margin-left:auto;margin-right:auto;height:4rem;width:3rem;fill:#e5e7eb}.s-gifting-filepond-placeholder-text{margin-left:auto;margin-right:auto;width:65%;min-width:18rem;font-size:0.75rem !important;line-height:1rem !important;--tw-text-opacity:1;color:rgb(107 114 128 / var(--tw-text-opacity, 1));margin-top:0.75rem;margin-bottom:0.75rem;font-family:var(--font-main), -apple-system, BlinkMacSystemFont}.s-gifting-skeleton-content salla-skeleton:nth-child(1){margin-bottom:0.75rem}.s-gifting-skeleton-content salla-skeleton:nth-child(2){margin-bottom:1.25rem}.s-gifting-skeleton-content salla-skeleton:nth-child(3){margin-bottom:0.75rem}.s-gifting-skeleton-content salla-skeleton:nth-child(4){margin-bottom:1.25rem}.s-gifting-skeleton .s-gifting-modal-header salla-skeleton{margin-bottom:1.25rem}.s-gifting-skeleton .s-gifting-modal-title{margin-bottom:1.25rem}.s-gifting-image{margin-left:5px;margin-right:5px}";

const SallaGifting = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.defaultEvent = "gifting:open";
        /** Order options picked in the pre-gift modal, sent as `order_options` with the gift payload. */
        this.collectedOrderOptions = [];
        /** Live PDP price, kept in sync by `onPriceUpdated`, used to seed the order-options total. */
        this.livePrice = 0;
        this.selectImageForYourGift = salla.lang.get('blocks.buy_as_gift.select_image_for_your_gift');
        this.currentStep = 1;
        this.showCalendar = false;
        this.showGiftText = false;
        this.currentLang = '';
        this.parentClass = "is-current-step-1";
        this.errors = {};
        this.gift = undefined;
        this.selectedGiftTextOption = undefined;
        this.selectedCountryId = undefined;
        this.selectedCountryCode = undefined;
        this.loadingCities = false;
        this.loadingRegions = false;
        this.regions = [];
        this.selectedRegionId = undefined;
        this.isKSA = false;
        this.selectedCountry = null;
        this.selectedRegion = null;
        this.displayedCities = [];
        this.selectedCity = null;
        this.searchingCities = false;
        this.enableCityAjaxSearch = false;
        this.citySearchQuery = '';
        this.step2Animated = false;
        this.citySearchTimer = null;
        this.citySearchCounter = 0;
        this.locationRequestCounter = 0;
        this.showTextArea = false;
        /// Gift Form Data
        this.selectedImage = undefined;
        this.uploadedImage = undefined;
        this.selectedText = undefined;
        this.hasError = false;
        this.timeZone = null;
        this.receiverCountryCode = salla.config.get('store.scope.countries')?.[0] || salla.config.get('store.store_country') || 'SA';
        this.hostId = `salla-gifting-${Math.random().toString(36).slice(2, 10)}`;
        this.disableClass = this.gift?.show_image_upload ? '' : 'pointer-events-none';
        /**
         * The form selector to be used to get the form data
         */
        this.formSelector = 'form.product-form, form.form--product-options';
        this.handleCitySearch = (query) => {
            this.citySearchQuery = query;
            if (!this.enableCityAjaxSearch)
                return;
            if (this.citySearchTimer)
                clearTimeout(this.citySearchTimer);
            const requestId = ++this.citySearchCounter;
            this.citySearchTimer = setTimeout(async () => {
                this.searchingCities = true;
                try {
                    const cities = await this.fetchCities(query);
                    if (requestId === this.citySearchCounter) {
                        this.displayedCities = cities;
                    }
                }
                catch (e) {
                    if (requestId === this.citySearchCounter) {
                        this.displayedCities = [];
                    }
                }
                finally {
                    if (requestId === this.citySearchCounter) {
                        this.searchingCities = false;
                    }
                }
            }, SallaGifting.CITY_SEARCH_DEBOUNCE_MS);
        };
        this.handleCitySelected = (city) => {
            this.selectedCity = city;
            this.receiverCity = String(city.id);
            this.clearError('receiver.city_id');
        };
        this.handleCityDropdownClosed = () => {
            this.citySearchQuery = '';
            if (this.citySearchTimer) {
                clearTimeout(this.citySearchTimer);
                this.citySearchTimer = null;
            }
            ++this.citySearchCounter;
            this.displayedCities = this.gift?.cities ?? [];
            this.searchingCities = false;
        };
        this.handleCountryDropdownClosed = () => { };
        this.handleRegionDropdownClosed = () => { };
        salla.lang.onLoaded(() => {
            this.selectImageOrUpload = salla.lang.get('blocks.buy_as_gift.select_image_or_upload');
            this.selectImageForYourGift = salla.lang.get('blocks.buy_as_gift.select_image_for_your_gift');
            this.sectionTitle = salla.lang.get('blocks.buy_as_gift.gift_the_one_you_love');
            this.sectionSubtitle = salla.lang.get('blocks.buy_as_gift.gift_the_one_you_love_message');
            this.sectionBtnText = salla.lang.get('blocks.buy_as_gift.send_as_a_gift');
            this.giftDetails = salla.lang.get('blocks.buy_as_gift.gift_details');
            this.selectGiftMessage = salla.lang.get('blocks.buy_as_gift.select_gift_message');
            this.giftText = salla.lang.get('blocks.buy_as_gift.gift_text');
            this.giftCustomText = salla.lang.get('blocks.buy_as_gift.gift_custom_text');
            this.textId = salla.lang.get('blocks.buy_as_gift.text_id');
            this.incorrectGiftText = salla.lang.get('blocks.buy_as_gift.incorrect_gift_text');
            this.nextStep = salla.lang.get('blocks.buy_as_gift.next_step');
            this.senderNameLabel = salla.lang.get('blocks.buy_as_gift.sender_name');
            this.receiverNameFieldLabel = salla.lang.get('blocks.buy_as_gift.receiver_name');
            this.receiverMobileFieldLabel = salla.lang.get('blocks.buy_as_gift.receiver_mobile');
            this.receiverCountryFieldLabel = salla.lang.get('blocks.buy_as_gift.receiver_country');
            this.ksa = salla.lang.get('pages.checkout.ksa');
            this.selectCity = salla.lang.get('pages.checkout.select_city');
            this.selectCountry = salla.lang.get('pages.checkout.select_country');
            this.selectCityInfo = salla.lang.get('blocks.buy_as_gift.city_info');
            this.cityChangeNotice = salla.lang.get('blocks.buy_as_gift.city_change_notice');
            this.receiverCityFieldLabel = salla.lang.get('blocks.buy_as_gift.receiver_city');
            this.receiverRegionFieldLabel = salla.lang.get('blocks.buy_as_gift.receiver_region') || salla.lang.get('pages.checkout.region');
            this.selectRegion = salla.lang.get('pages.checkout.select_region');
            this.receiverEmailFieldLabel = salla.lang.get('blocks.buy_as_gift.receiver_email');
            this.emailPlaceholder = salla.lang.get('common.elements.email_placeholder');
            this.sendLater = salla.lang.get('blocks.buy_as_gift.send_later');
            this.selectSendDateAndTime = salla.lang.get('blocks.buy_as_gift.select_send_date_and_time');
            this.canNotEditOrderAfterSelectDate = salla.lang.get('blocks.buy_as_gift.can_not_edit_order_after_select_date');
            this.sendGift = salla.lang.get('blocks.buy_as_gift.send_gift');
            this.donationRequired = salla.lang.get('pages.products.donation_amount_required');
            this.currentLang = salla.lang.locale;
        });
        salla.event.on(this.defaultEvent, () => {
            this.open();
        });
    }
    isPhysical() {
        return this.physicalProducts || this.physical;
    }
    /**
     * Resolves the city notice text shown in the physical-product gift step:
     * - recipient can't change address           -> default city info notice
     * - recipient can change, fixed shipping cost -> city-change notice
     * - recipient can change, any shipping cost   -> no notice
     */
    cityNoticeText() {
        if (!this.gift?.recipient_can_change_address) {
            return this.selectCityInfo;
        }
        if (!this.gift?.allows_different_shipping_cost) {
            return this.cityChangeNotice;
        }
        return null;
    }
    componentDidLoad() {
        salla.event.product.onPriceUpdated((response) => {
            const quantityInput = document.querySelector(`.s-quantity-input-input`);
            // @ts-ignore
            this.quantity = quantityInput?.value;
            // Seeds the order-options modal total so it reflects the current variant/quantity price.
            this.livePrice = Number(response?.data?.price) || this.livePrice;
        });
    }
    getFormDataFromForm() {
        if (!this.formSelector)
            return null;
        const form = document.querySelector(this.formSelector);
        if (!form)
            return null;
        return new FormData(form);
    }
    mergeObjectToFormData(obj, formData, namespace) {
        for (let property in obj) {
            if (!obj.hasOwnProperty(property) || obj[property] === undefined || obj[property] === null)
                continue;
            const formKey = namespace ? `${namespace}[${property}]` : property;
            if (typeof obj[property] === 'object' && obj[property] !== null && !(obj[property] instanceof File)) {
                this.mergeObjectToFormData(obj[property], formData, formKey);
            }
            else {
                formData.set(formKey, obj[property]);
            }
        }
        return formData;
    }
    /**
     * Order-options gate. Two shapes, because the two gifting entry points already differ:
     *
     * - **Cart page**: the order-option forms are rendered inline and synced to the cart on change,
     *   so gifting reuses the cart-submit check — block on invalid, never re-ask in a modal.
     * - **PDP**: nothing renders the options, so they're collected in the shared
     *   <salla-order-options-modal> *before* the gifting modal, the same modal and sequence the PDP
     *   uses for Quick Purchase and Apple Pay.
     *
     * Returns false when the buyer must fix something first (invalid inline form, or a dismissed
     * modal); the gifting modal then stays closed and they retry from the widget button.
     */
    async collectOrderOptions() {
        this.collectedOrderOptions = [];
        if (!this.productId) {
            return this.validateInlineCartOptions();
        }
        const { products, basePrice } = this.getOrderOptionsTarget();
        if (!products.length) {
            return true;
        }
        let response;
        try {
            response = await salla.api.withoutNotifier(() => salla.product.api.getOrderOptions(products));
        }
        catch (error) {
            salla?.logger?.warn?.('order-options: fetch failed', error);
            return true;
        }
        const orderOptions = response?.data?.order_options || [];
        if (!orderOptions.length) {
            return true;
        }
        const price = basePrice > 0 ? basePrice : Number(response?.data?.products?.[0]?.price?.amount) || 0;
        try {
            const result = await this.ensureOrderOptionsModal().open({ orderOptions, basePrice: price });
            this.collectedOrderOptions = result?.orderOptions || [];
            return true;
        }
        catch {
            // dismissed — keep the gifting modal closed.
            return false;
        }
    }
    /**
     * Same check the theme runs on cart submit: every `form[id^="item-"]` (cart items *and* the
     * inline order-option forms) must be valid. Values are already on the cart — each form syncs
     * through `cart.updateItem` on change — so nothing is collected here, gifting is only blocked.
     */
    validateInlineCartOptions() {
        const forms = document.querySelectorAll('form[id^="item-"]');
        let isValid = true;
        forms.forEach(form => {
            if (!form.reportValidity()) {
                isValid = false;
            }
        });
        if (!isValid) {
            salla.error(salla.lang.get('common.messages.required_fields'));
        }
        return isValid;
    }
    /** What to ask order options for on the PDP. `basePrice` seeds the modal's live total. */
    getOrderOptionsTarget() {
        return {
            products: [{ id: this.productId, quantity: this.getGiftQuantity() }],
            basePrice: this.livePrice,
        };
    }
    /** `quantity` state is only seeded on price updates, so fall back to the product form. */
    getGiftQuantity() {
        const fromState = Number(this.quantity);
        if (Number.isFinite(fromState) && fromState > 0) {
            return fromState;
        }
        const fromForm = Number(this.getFormDataFromForm()?.get('quantity'));
        return Number.isFinite(fromForm) && fromForm > 0 ? fromForm : 1;
    }
    /** Get-or-create the singleton order-options modal on <body>. */
    ensureOrderOptionsModal() {
        let modal = document.querySelector('salla-order-options-modal');
        if (!modal) {
            modal = document.createElement('salla-order-options-modal');
            document.body.appendChild(modal);
        }
        return modal;
    }
    /**
     * Show / Open the gifting modal window
    */
    async open() {
        if (salla.config.isGuest()) {
            salla.api.auth.setAfterLoginEvent(this.defaultEvent);
            salla.event.dispatch('login::open', { withoutReload: true });
            return;
        }
        // Required customizations can't be bypassed by gifting: the order-options modal runs first, and
        // dismissing it keeps the gifting modal closed. Runs here (not in the button handler) so the
        // `gifting:open` event and the after-login re-entry go through the same gate.
        if (!(await this.collectOrderOptions())) {
            return;
        }
        // Reopen on step 1, but KEEP the user's selections (image / text / custom
        // message / sender+receiver). The modal element is never destroyed, so its
        // state and DOM survive close→reopen without a page refresh — we only reset
        // the step view and clear anime.js's leftover inline styles so the slide
        // doesn't reopen with step 2 painted on top.
        this.resetStepsAnimation();
        this.currentStep = 1;
        this.parentClass = 'is-current-step-1';
        this.step2Animated = false;
        // Drop any stale inline height from the previous session's step (e.g. step 2,
        // shorter for digital products) so reopening to step 1 isn't clipped. Cleared
        // while the modal is hidden, so the later set won't animate.
        this.stepsWrapper?.style.removeProperty('height');
        await this.modal.open();
        // Determine the appropriate method to call based on the `physicalProducts` flag
        const giftDetailsMethod = this.productId ? () => salla.product.getGiftProductDetails(this.productId) : () => salla.cart.getGiftCartDetails();
        return await salla.api.withoutNotifier(giftDetailsMethod)
            .then((response) => {
            this.gift = response.data;
            this.senderName = this.gift.sender_name;
            this.disableClass = this.gift?.show_image_upload ? '' : 'pointer-events-none';
            if (!this.gift.show_texts) {
                this.selectedGiftTextOption = undefined;
                this.selectedText = undefined;
            }
            if (!this.gift.show_images) {
                this.selectedImage = undefined;
                this.uploadedImage = undefined;
            }
            if (this.skipStep1()) {
                this.currentStep = 2;
                this.parentClass = 'is-current-step-2';
                this.step2Animated = true;
            }
        })
            .catch(e => {
            this.hasError = true;
            this.errorMessage = e.response?.data?.error?.message || e.response?.data;
        })
            .finally(() => this.modal.stopLoading());
    }
    skipStep1() {
        return !!this.gift && !this.gift.show_images && !this.gift.show_texts;
    }
    /**
     * anime.js writes inline transform/opacity onto the step containers and their
     * `.anime-item` children while transitioning between steps. The modal is only
     * hidden on close (the component is never destroyed), and Stencil never set
     * those inline styles so it won't clear them on re-render. Without this reset,
     * reopening keeps step 2 painted on top (leftover opacity/translate) while the
     * markup is back on step 1 — clicks then fall through to step 1's uploader.
     */
    resetStepsAnimation() {
        // `.anime-item` children carry no JSX-managed style, so clearing them fully is safe.
        [this.step1Elems, this.step2Elems].forEach(stepEl => {
            stepEl?.querySelectorAll('.anime-item')
                .forEach(item => item.removeAttribute('style'));
        });
        // step 1 container can carry anime leftovers (goToStep1 animates it) and has
        // no JSX-managed style, so a full clear is safe.
        this.step1Elems?.removeAttribute('style');
        // step 2 container is animated by goToStep2 too, but in the skip-step-1 case
        // its style is JSX-managed. Only strip the anime-applied props so we don't
        // fight Stencil's vnode-level style diffing (which would not restore it).
        if (this.step2Elems && !this.skipStep1()) {
            this.step2Elems.style.removeProperty('opacity');
            this.step2Elems.style.removeProperty('transform');
        }
    }
    /**
     *
     * Hide / close the gifting modal window
    */
    async close() {
        return this.modal.close();
    }
    /**
     * Update the modal height based on the changes on the inner elements height for a specific step OR just a pass a new fixed height
    */
    async setWrapperHeight(asStep = 1, delay = 250, additionSpace = 0, newHeight = 0) {
        let currentStep = document.querySelector(`#${this.hostId} .gift-step-${asStep}`);
        const updateHeight = () => {
            let currentStepHeight = currentStep.offsetHeight;
            if (newHeight) {
                this.stepsWrapper.style.height = `${newHeight}px`;
            }
            else {
                this.stepsWrapper.style.height = currentStepHeight + additionSpace + 'px';
            }
        };
        if (this.step2Animated && asStep === 2) {
            requestAnimationFrame(updateHeight);
        }
        else {
            setTimeout(updateHeight, delay);
        }
    }
    toggleCalendar() {
        this.showCalendar = !this.showCalendar;
        this.setWrapperHeight(2, 150, 0);
    }
    toggleGiftText(event) {
        this.textSelect?.classList.remove('s-form-has-error');
        let dataID = event.target.children[event.target.selectedIndex].getAttribute('data-id');
        let customID = dataID == "custom";
        // Only the custom-text textarea changes the layout; resize the wrapper just
        // when it actually appears/disappears so picking or clearing a normal option
        // doesn't jump the modal height.
        const customVisibilityChanged = this.showGiftText !== customID;
        this.showGiftText = customID;
        if (dataID) {
            this.selectedGiftTextOption = dataID;
            customID ? this.selectedText = undefined : this.selectedText = event.target.value;
        }
        else {
            // empty textarea value
            this.textArea.value = '';
            this.selectedText = undefined;
            this.selectedGiftTextOption = undefined;
        }
        if (customVisibilityChanged) {
            this.setWrapperHeight(1, 150, 5);
        }
    }
    /**
     *
     * Go to the step 2
     */
    async goToStep2(withValidation = true) {
        if (!this.step1Elems)
            return;
        if (withValidation && !this.validateStep1()) {
            return;
        }
        // Pin the wrapper to step 1's current height as an explicit px value first.
        // The wrapper is `auto` on open, and CSS can't transition from `auto` — it
        // would snap (flash straight to the big size). With a px starting point the
        // grow to step 2 animates smoothly, in sync with the slide.
        if (this.stepsWrapper && this.step1Elems) {
            this.stepsWrapper.style.height = `${this.step1Elems.offsetHeight}px`;
        }
        // Step 2 is absolutely positioned, so its height is measurable even while it's
        // translated off-screen. Grow right away (small delay only to flush the pending
        // re-render and let the px start-height paint) so the background expands in sync.
        this.setWrapperHeight(2, 50, 0);
        let stepNextAnime = new anime_es.anime.timeline();
        stepNextAnime.add({
            targets: this.step1Elems.querySelectorAll(`#${this.hostId} .anime-item`),
            opacity: [1, 0],
            translateX: [0, 50],
            delay: anime_es.anime.stagger(70),
            // easing: 'easeOutExpo',
            duration: 1200,
        })
            .add({
            targets: '.gift-step-2',
            translateX: ['-110%', 0],
            opacity: [0, 1],
        }, '-=1800')
            .add({
            targets: this.step2Elems.querySelectorAll(`#${this.hostId} .anime-item`),
            opacity: [0, 1],
            translateX: [-50, 0],
            delay: anime_es.anime.stagger(70),
            duration: 1200,
            complete: () => {
                this.step2Elems.querySelectorAll(`#${this.hostId} .anime-item`).forEach(item => {
                    item.classList.remove('opacity-0');
                    item.removeAttribute('style');
                });
                this.step2Animated = true;
            }
        }, '-=1200');
        this.currentStep = 2;
        this.parentClass = `is-current-step-${this.currentStep}`;
    }
    validateStep1() {
        if (!this.gift?.show_texts)
            return true;
        if (!this.selectedGiftTextOption) {
            this.textSelect?.classList.add('s-form-has-error');
            this.customTextArea?.classList.remove('s-form-has-error');
            return false;
        }
        else if (this.selectedGiftTextOption == 'custom' && !this.selectedText) {
            this.textSelect?.classList.remove('s-form-has-error');
            this.customTextArea?.classList.add('s-form-has-error');
            return false;
        }
        this.textSelect?.classList.remove('s-form-has-error');
        this.customTextArea?.classList.remove('s-form-has-error');
        return true;
    }
    /**
     *
     * Go to the step 1
     */
    goToStep1(e) {
        e.preventDefault();
        let stepBackAnime = new anime_es.anime.timeline({
            autoplay: false,
        });
        stepBackAnime.add({
            targets: this.step2Elems.querySelectorAll(`#${this.hostId} .anime-item`),
            opacity: [1, 0],
            translateX: [0, -50],
            delay: anime_es.anime.stagger(70),
            // easing: 'easeOutExpo',
            duration: 1200,
        })
            .add({
            targets: '.gift-step-1',
            translateX: ['110%', 0],
            opacity: [0, 1],
        }, '-=1800')
            .add({
            targets: this.step1Elems.querySelectorAll(`#${this.hostId} .anime-item`),
            opacity: [0, 1],
            translateX: [50, 0],
            delay: anime_es.anime.stagger(70),
            duration: 1200,
            complete: () => {
                this.step1Elems.querySelectorAll(`#${this.hostId} .anime-item`).forEach(item => {
                    item.classList.remove('opacity-0');
                    item.removeAttribute('style');
                });
            }
        }, '-=1200');
        stepBackAnime.play();
        this.step2Animated = false;
        this.setWrapperHeight(1, 600, 0);
        this.currentStep = 1;
        this.parentClass = `is-current-step-${this.currentStep}`;
    }
    getFilepondPlaceholder() {
        return `<div class="s-gifting-filepond-placeholder"><span class="s-gifting-filepond-placeholder-icon">${Images}</span><p class="s-gifting-filepond-placeholder-text">${this.selectImageOrUpload && this.gift.show_image_upload ? this.selectImageOrUpload : this.selectImageForYourGift}</p></div>`;
    }
    setPreview(image) {
        this.uploader?.classList.add('has-bg');
        var bg = document.querySelector(`#${this.hostId}.filepond-bg`) ? document.querySelector(`#${this.hostId} .filepond-bg`) : document.createElement('div');
        bg.classList.add('filepond-bg');
        bg.classList.remove('s-hidden');
        bg.style.backgroundImage = "url('" + image.url + "')";
        this.uploader?.querySelector('.filepond--root')?.appendChild(bg);
        this.uploadedImage = image.url;
        if (!!this.gift && this.gift.gift_images?.length) {
            this.setWrapperHeight(1, 150, 0);
        }
    }
    removePreview() {
        this.uploader.classList.remove('has-bg');
        let bg = document.querySelector(`#${this.hostId} .filepond-bg`);
        bg.removeAttribute('style');
        bg.classList.add('s-hidden');
        this.handleRemoveImage();
    }
    handleTextAreaChange(event) {
        this.selectedText = event.target.value;
        this.customTextArea?.classList.remove('s-form-has-error');
    }
    clearError(key) {
        if (this.errors?.[key]) {
            const { [key]: _, ...rest } = this.errors;
            this.errors = rest;
        }
    }
    handleSenderName(event) {
        this.senderName = event.target.value;
        this.clearError('sender_name');
    }
    handleReceiverName(event) {
        this.receiverName = event.target.value;
        this.clearError('receiver.name');
    }
    resetCityState() {
        if (this.citySearchTimer) {
            clearTimeout(this.citySearchTimer);
            this.citySearchTimer = null;
        }
        ++this.citySearchCounter;
        this.gift = { ...this.gift, cities: [] };
        this.displayedCities = [];
        this.receiverCity = undefined;
        this.selectedCity = null;
        this.citySearchQuery = '';
        this.searchingCities = false;
        this.enableCityAjaxSearch = false;
    }
    setInitialCities(cities) {
        this.gift = { ...this.gift, cities };
        this.displayedCities = cities;
        this.enableCityAjaxSearch = cities.length >= SallaGifting.CITY_AJAX_THRESHOLD;
    }
    async fetchCities(query = '') {
        if (!this.selectedCountryId)
            return [];
        if (this.isKSA && !this.selectedRegionId)
            return [];
        const params = [
            'for_branch=0',
            `country_id=${this.selectedCountryId}`,
        ];
        if (this.isKSA && this.selectedRegionId) {
            params.push(`region_id=${this.selectedRegionId}`);
        }
        if (query?.trim()) {
            params.push(`query=${encodeURIComponent(query.trim())}`);
        }
        const response = await salla.api.request(`shipping/cities?${params.join('&')}`);
        return response?.data ?? [];
    }
    async handleCountrySelected(country) {
        const countryId = country?.id;
        this.selectedCountry = country ?? null;
        this.selectedCountryId = countryId;
        this.selectedCountryCode = country?.country_code;
        this.isKSA = country?.country_code === 'SA';
        this.clearError('receiver.country');
        // Reset region and city; bump request counter so in-flight loads from a
        // previous country/region selection get discarded when they resolve.
        this.regions = [];
        this.selectedRegionId = undefined;
        this.selectedRegion = null;
        this.resetCityState();
        const requestId = ++this.locationRequestCounter;
        if (!countryId) {
            this.selectedCountryCode = undefined;
            this.isKSA = false;
            this.setWrapperHeight(2, 0, 0);
            return;
        }
        if (this.isKSA) {
            // For KSA, fetch regions first
            this.loadingRegions = true;
            this.setWrapperHeight(2, 0, 0);
            try {
                const response = await salla.api.request(`shipping/countries/${countryId}/regions`);
                if (requestId !== this.locationRequestCounter)
                    return;
                if (response && response.data) {
                    this.regions = response.data;
                }
            }
            catch (error) {
                if (requestId !== this.locationRequestCounter)
                    return;
                console.error('Error fetching regions:', error);
            }
            finally {
                if (requestId === this.locationRequestCounter) {
                    this.loadingRegions = false;
                    this.setWrapperHeight(2, 0, 0);
                }
            }
        }
        else {
            // For non-KSA, fetch cities directly
            this.loadingCities = true;
            this.setWrapperHeight(2, 0, 0);
            try {
                const cities = await this.fetchCities();
                if (requestId !== this.locationRequestCounter)
                    return;
                this.setInitialCities(cities);
            }
            catch (error) {
                if (requestId !== this.locationRequestCounter)
                    return;
                console.error('Error fetching cities:', error);
            }
            finally {
                if (requestId === this.locationRequestCounter) {
                    this.loadingCities = false;
                }
            }
        }
    }
    async handleRegionSelected(region) {
        const regionId = region?.id;
        this.selectedRegion = region ?? null;
        this.selectedRegionId = regionId;
        this.clearError('receiver.region');
        // Reset city when region changes; bump request counter so any earlier
        // in-flight city fetch (for a previous region) is discarded on resolve.
        this.resetCityState();
        const requestId = ++this.locationRequestCounter;
        if (!regionId)
            return;
        this.loadingCities = true;
        try {
            const cities = await this.fetchCities();
            if (requestId !== this.locationRequestCounter)
                return;
            this.setInitialCities(cities);
        }
        catch (error) {
            if (requestId !== this.locationRequestCounter)
                return;
            console.error('Error fetching cities:', error);
        }
        finally {
            if (requestId === this.locationRequestCounter) {
                this.loadingCities = false;
                this.setWrapperHeight(2, 0, 0);
            }
        }
    }
    handleUploadImage(img) {
        // SP-22307: salla-file-upload emits {id, url} now (PR #2049); extract the url string the gift API expects.
        // Single typeof guard narrows for tsc; the `img && img.url` short-circuit keeps null safe
        // (typeof null === 'object', so null must NOT reach the bare `.url` access).
        this.uploadedImage = typeof img !== 'object' ? img : (img && img.url);
        if (!!this.gift && this.gift.gift_images?.length) {
            this.setWrapperHeight(1, 150, 0);
        }
    }
    handleRemoveImage() {
        this.uploadedImage = '';
        if (!!this.gift && this.gift.gift_images?.length) {
            this.setWrapperHeight(1, 150, 0);
        }
    }
    // private handleReceiverEmail(event) {
    //   this.receiverEmail = event.target.value;
    // }
    handlePhoneInputChange(event) {
        let phone = event.detail;
        this.receiverMobile = phone.number;
        this.receiverCountryCode = phone.country_code;
        this.clearError('receiver.mobile');
    }
    handleDateTimePicker(event) {
        this.deliveryDate = event.detail;
    }
    getCalendarClasses() {
        return {
            "s-form-group": true,
            "anime-item": true,
            "s-gifting-calendar": true,
            "shown": this.showCalendar,
            "hide": !this.showCalendar,
            "s-form-has-error": !!this.errors && this.errors['deliver_at']
        };
    }
    async submitForm() {
        // @ts-ignore
        const donatingAmount = (document.querySelector(`#donating-amount`))?.value;
        this.calendarFormGroup.classList.remove('s-form-has-error');
        if (!!this.errors) {
            this.errors = {};
            this.setWrapperHeight(2, 150, 0);
        }
        if (this.showCalendar && !this.deliveryDate) {
            this.calendarFormGroup.classList.add('s-form-has-error');
            return;
        }
        // Validate required fields
        const requiredFields = [
            { key: 'sender_name', value: this.senderName, label: this.senderNameLabel },
            { key: 'receiver.name', value: this.receiverName, label: this.receiverNameFieldLabel },
            { key: 'receiver.mobile', value: this.receiverMobile, label: this.receiverMobileFieldLabel },
            ...(this.isPhysical() ? [
                { key: 'receiver.country', value: this.selectedCountryId, label: this.receiverCountryFieldLabel },
                ...(this.isKSA ? [{ key: 'receiver.region', value: this.selectedRegionId, label: this.receiverRegionFieldLabel }] : []),
                { key: 'receiver.city_id', value: this.receiverCity, label: this.receiverCityFieldLabel },
            ] : []),
        ];
        const validationErrors = {};
        requiredFields.forEach(({ key, value, label }) => {
            if (!value) {
                validationErrors[key] = salla.lang.get('common.errors.field_required', { attribute: label });
            }
        });
        if (Object.keys(validationErrors).length) {
            this.errors = validationErrors;
            this.setWrapperHeight(2, 150, 0);
            return;
        }
        // A preset option from the list sends its id as `text_id`; the custom option
        // sends the typed message as `text`.
        const isCustomText = this.selectedGiftTextOption === 'custom';
        const giftPayload = {
            text: isCustomText ? this.selectedText : undefined,
            text_id: !isCustomText && this.selectedGiftTextOption ? Number(this.selectedGiftTextOption) : undefined,
            sender_name: this.senderName,
            quantity: this.quantity,
            deliver_at: this.showCalendar ? this.deliveryDate : null,
            image_url: this.uploadedImage ?? this.selectedImage,
            donation_amount: donatingAmount ? donatingAmount : null,
            receiver: {
                name: this.receiverName,
                mobile_country_code: this.receiverCountryCode,
                mobile: this.receiverMobile,
                ...(this.isPhysical() && this.selectedCountryCode ? { country_code: this.selectedCountryCode } : {}),
                ...(this.isPhysical() && this.selectedRegionId ? { region_id: this.selectedRegionId } : {}),
                ...(this.receiverCity && this.isPhysical() ? { city_id: this.receiverCity } : {})
            },
            has_apple_pay: salla.helpers.hasApplePay(),
        };
        let formData;
        if (this.productId && this.formSelector) {
            formData = this.getFormDataFromForm() || new FormData();
            formData = this.mergeObjectToFormData(giftPayload, formData);
        }
        // PDP flow only — the cart flow's options are already on the cart, and the API keeps existing
        // selections when the request carries no `order_options`.
        applePayOrderOptions.appendOrderOptionsToPayload(formData ?? giftPayload, this.collectedOrderOptions);
        const addGiftMethod = this.productId
            ? () => salla.product.addProductGiftToCart(this.productId, formData, true)
            : () => salla.cart.addCartGiftToCart(giftPayload, true);
        return await addGiftMethod()
            .then(() => this.modal.close())
            .catch((e) => {
            if (e.response && e.response.status == 422) {
                this.errors = e.response.data.error.fields;
            }
            else {
                console.log(e);
            }
            this.setWrapperHeight(2, 150, 0);
        });
    }
    step2ItemClass(extra = '') {
        const base = `anime-item${this.step2Animated ? '' : ' opacity-0'}`;
        return extra ? `${extra} ${base}` : base;
    }
    generateClass() {
        return {
            "s-gifting-widget": true,
            "s-gifting-widget-vertical": !!this.vertical,
            "s-gifting-widget-horizontal": !this.vertical
        };
    }
    async handleGiftButtonClick() {
        if (!!this.productId && this.formSelector) {
            const form = document.querySelector(this.formSelector);
            if (!form) {
                console.error('SallaGifting:: Form not found in the document!');
                return;
            }
            if (!(form instanceof HTMLFormElement)) {
                console.error('SallaGifting:: The specified selector does not correspond to a form tag!');
                return;
            }
            if (!form.reportValidity()) {
                console.error('SallaGifting:: Form is not valid!');
                salla.error(salla.lang.get('common.messages.required_fields'));
                return;
            }
        }
        const optionsElement = document.querySelector(`salla-product-options[product-id="${this.productId}"]`);
        if (optionsElement && !(await optionsElement.reportValidity())) {
            salla.error(salla.lang.get('common.messages.required_fields'));
            return;
        }
        this.open();
    }
    render() {
        return [
            index.h("div", { key: '02f095cd71654941cc3d11f9c9be13e019247992' }, index.h("salla-list-tile", { key: '5dfefabc5e6fcb2f0193bf11dcd324c517b0e649', class: this.generateClass() }, index.h("div", { key: '35b5b8cbf54ccbf9368cdc95d2e0505ca317a555', slot: "title" }, this.vertical ? index.h("span", { innerHTML: giftSharing.GiftSharing }) : '', index.h("h3", { key: '2399e07a06a808b396cf57a5233be97938e7b9b5' }, !!this.widgetTitle ? this.widgetTitle : this.sectionTitle)), index.h("div", { key: '12f460ce1df5e294e77b25e07d00d85370b2b939', slot: "subtitle" }, index.h("div", { key: '28fd4125c6f18fb976bc271ffc7c731683757bfb' }, !!this.widgetSubtitle ? this.widgetSubtitle : this.sectionSubtitle)), index.h("div", { key: '2ccb5745341390a9aeb80c56b4d7bb171cc2940e', slot: "action" }, index.h("salla-button", { key: '4e74313bd4020ae32a1c2ba30b0133da0aaf410a', color: "primary", fill: "outline", width: "wide", class: "s-gifting-widget-action", onClick: () => this.handleGiftButtonClick() }, index.h("slot", { key: '85ef811160f391d2fc456b13a89120407d6e4c36', name: "widget-btn-content" }, index.h("div", { key: '6d4d1162f7147488f8ce11d7da1bf7b82bc407b0', class: "s-gifting-widget-action-content" }, index.h("span", { key: 'e7aa877309bc7346fe56227365ad75c0d7c1a8dc', innerHTML: giftSharing.GiftSharing }), " \u00A0", index.h("span", { key: '5c7f1a43fa84e8e5ba74a449063fc89576626db2' }, this.sectionBtnText)))))), index.h("salla-modal", { key: '1de82b1f9a7fc49321eb766b1451eacd2d210480', id: this.hostId, isLoading: true, class: "s-gifting-modal", width: "sm", ref: modal => this.modal = modal }, index.h("div", { key: 'f3a97db3aa3a2591a1d391af61efa15ab76f1c8c', slot: "loading" }, index.h("div", { key: '67fb44c701b3f2771e041b91a2a438a0419ed4dd', class: "s-gifting-skeleton" }, index.h("div", { key: '70b2841fbafff80879cf2d68550cc9659da140a2', class: "s-gifting-modal-header" }, index.h("salla-skeleton", { key: '8d337e1bfe29aea80e93ed2d1e172fa4fe23e1db', type: 'circle', height: '5rem', width: '5rem' }), index.h("h2", { key: 'b8586d28a3dda5abbdbc341830560c157efb566c', class: "s-gifting-modal-title" }, index.h("div", { key: '956ec550fd7ddeb6b11f174b92d0133408f95165', class: "s-gifting-modal-badge-wrapper" }, index.h("salla-skeleton", { key: 'f495e7ec761c8499efb7a6e7025bd205dca49f2c', height: '15px', width: '150px' })))), index.h("div", { key: '28e5ad1386cebcd93a5f9307bd812dca77a66188', class: "s-gifting-skeleton-content" }, index.h("salla-skeleton", { key: '951dbed5a03ab246d661dfd2a59ae099b468fa89', height: '10px', width: '150px' }), index.h("salla-skeleton", { key: 'f200d5a5a4a258fb0755b6adb48e25606ab93bb7', height: '230px' }), index.h("salla-skeleton", { key: '1b4f69321a13d1ddea4ad038bfad22385b3b51ed', height: '10px', width: '150px' }), index.h("salla-skeleton", { key: '5d9219d3826de28b8e585bada604bff60dfc80f8', height: '30px' }), index.h("salla-skeleton", { key: 'a248552e288bea2e9839f381ec2d96bdb0e53cdf', height: '40px' })))), index.h("slot", { key: '3c8f9965766d7ce17fb20b4df72a3e46c273b23a', name: "header" }), !!this.hasError ?
                index.h("salla-placeholder", { alignment: "center" }, index.h("span", { slot: "title" }, this.errorMessage || salla.lang.get('common.errors.empty_results')), index.h("span", { slot: "description" }, " "))
                :
                    [
                        index.h("div", { class: "s-gifting-modal-header" }, index.h("span", { class: "s-gifting-modal-icon" }, index.h("span", { innerHTML: giftSharing.GiftSharing })), index.h("h2", { class: "s-gifting-modal-title" }, index.h("div", { class: "s-gifting-modal-badge-wrapper" }, !this.skipStep1() && index.h("div", { class: "s-gifting-modal-badge" }, index.h("span", null, index.h("span", null, this.currentStep), "/2")), index.h("span", null, this.giftDetails)))),
                        index.h("div", { class: "s-gifting-steps-wrapper " + this.parentClass, ref: el => this.stepsWrapper = el }, !this.skipStep1() && index.h("div", { class: "s-gifting-step-one gift-step-1", ref: el => this.step1Elems = el }, this.gift && this.gift.show_images &&
                            index.h("div", { class: "s-gifting-modal-uploader-title anime-item" }, this.selectImageForYourGift), this.gift && this.gift.show_images &&
                            index.h("div", { class: "s-gifting-image-section" }, index.h("div", { class: `s-gifting-modal-uploader anime-item ${this.disableClass}`, ref: el => this.uploader = el }, index.h("span", { class: "s-gifting-remove-preview pointer-events-auto", onClick: () => this.removePreview(), innerHTML: cancel.Cancel }), this.selectImageOrUpload && this.gift && index.h("salla-file-upload", { "instant-upload": true, name: "image_url", url: salla.url.api(salla.product.api.getUrl('giftImage')), onUploaded: event => this.handleUploadImage(event.detail), labelIdle: this.getFilepondPlaceholder(), onRemoved: () => this.handleRemoveImage() })), index.h("div", { class: "anime-item" }, !this.uploadedImage && !!this.gift && !!this.gift.gift_images && this.gift.gift_images.length > 0 ?
                                index.h("salla-slider", { id: "gifting-slider", loop: false, "controls-outer": true, class: "s-gifting-slider", type: "carousel" }, index.h("div", { slot: "items" }, this.gift && this.gift.gift_images ?
                                    this.gift?.gift_images.map((item) => index.h("a", { class: "s-gifting-clickable s-gifting-image", onClick: () => this.setPreview(item) }, index.h("img", { src: item.url, alt: `${item.id}` }))) : ''))
                                : "")), this.gift && this.gift.show_texts ?
                            index.h("div", { class: "anime-item" }, index.h("div", { class: "s-gifting-modal-uploader-title s-gifting-text-title" }, this.giftText), index.h("div", { class: "s-form-group s-gifting-selectText", ref: el => this.textSelect = el }, index.h("select", { id: "gift-text-selection", name: "gift-text-selection", class: "s-form-control s-gifting-select", onChange: e => this.toggleGiftText(e) }, index.h("option", { "data-id": null, selected: !this.selectedGiftTextOption }, this.selectGiftMessage), this.gift && this.gift.gift_texts ?
                                this.gift?.gift_texts.map((txt) => index.h("option", { "data-id": txt.id, value: txt.text, key: txt.id, selected: String(this.selectedGiftTextOption) === String(txt.id) }, txt.text)) : '', this.gift && this.gift.show_custom_text ? index.h("option", { "data-id": "custom", selected: this.selectedGiftTextOption === 'custom' }, this.giftCustomText) : null)), index.h("div", { class: this.showGiftText && this.gift && this.gift.show_custom_text ? "s-form-group s-gifting-textarea shown" : "s-form-group s-gifting-textarea hide", ref: (el) => this.customTextArea = el }, index.h("label", { htmlFor: "gift-custom-text", class: "s-form-label" }, this.giftCustomText), index.h("div", { class: "mt-1" }, index.h("textarea", { onInput: (event) => this.handleTextAreaChange(event), rows: 4, ref: (el) => this.textArea = el, name: "gift-custom-text", id: "gift-custom-text", class: "s-form-control" })))) : null, index.h("div", { class: "anime-item" }, index.h("salla-button", { color: "primary", width: "wide", onClick: () => this.goToStep2(!!this.gift?.show_texts) }, index.h("span", null, this.nextStep)))), index.h("div", { class: "s-gifting-step-two gift-step-2", style: this.skipStep1() ? { opacity: '1', transform: 'translateX(0)', position: 'relative' } : undefined, ref: el => this.step2Elems = el }, index.h("div", { class: this.step2ItemClass(this.errors?.['sender_name'] ? 's-form-group s-form-has-error' : 's-form-group') }, index.h("label", { htmlFor: "sender_name", class: "s-form-label" }, this.senderNameLabel), index.h("input", { type: "text", class: "s-form-control", name: "sender_name", id: "sender_name", value: this.senderName, onInput: (event) => this.handleSenderName(event), placeholder: "" }), this.errors && this.errors['sender_name'] ?
                            index.h("span", { class: "s-gifting-error" }, this.errors['sender_name']) : ''), index.h("div", { class: this.step2ItemClass(this.errors?.['receiver.name'] ? 's-form-group s-form-has-error' : 's-form-group') }, index.h("label", { htmlFor: "receiver_name", class: "s-form-label" }, this.receiverNameFieldLabel), index.h("input", { type: "text", class: "s-form-control", name: "receiver_name", id: "receiver_name", value: "", onInput: (event) => this.handleReceiverName(event), placeholder: "" }), this.errors && this.errors['receiver.name'] ?
                            index.h("span", { class: "s-gifting-error" }, this.errors['receiver.name']) : ''), index.h("div", { class: this.step2ItemClass(this.errors?.['receiver.mobile'] ? 's-form-group s-form-has-error' : 's-form-group') }, index.h("label", { class: "s-form-label" }, this.receiverMobileFieldLabel), index.h("salla-tel-input", { class: "s-gifting-tel-input", phone: this.receiverMobile, countryCode: this.receiverCountryCode, onPhoneEntered: (e) => this.handlePhoneInputChange(e) }), this.errors && this.errors['receiver.mobile'] ?
                            index.h("span", { class: "s-gifting-error" }, this.errors['receiver.mobile']) : ''), this.isPhysical() &&
                            index.h("div", { class: this.step2ItemClass(this.errors?.['receiver.country'] ? 's-form-group s-form-has-error' : 's-form-group') }, index.h("label", { class: "s-form-label", htmlFor: `${this.hostId}-country` }, this.receiverCountryFieldLabel), index.h("salla-searchable-dropdown", { placeholder: this.selectCountry, items: this.gift?.countries ?? [], selectedItem: this.selectedCountry, disabled: !this.gift?.countries?.length, required: true, inputId: `${this.hostId}-country`, searchable: false, keepParentScroll: true, onItemSelected: (e) => this.handleCountrySelected(e.detail), onDropdownClosed: () => this.handleCountryDropdownClosed() }), this.errors && this.errors['receiver.country'] ?
                                index.h("span", { class: "s-gifting-error" }, this.errors['receiver.country']) : ''), this.isPhysical() && this.isKSA &&
                            index.h("div", { class: this.step2ItemClass(this.errors?.['receiver.region'] ? 's-form-group s-form-has-error' : 's-form-group') }, index.h("label", { class: "s-form-label", htmlFor: `${this.hostId}-region` }, this.receiverRegionFieldLabel), index.h("salla-searchable-dropdown", { placeholder: this.selectRegion, items: this.regions ?? [], selectedItem: this.selectedRegion, loading: this.loadingRegions, disabled: this.loadingRegions || !this.regions?.length, required: true, inputId: `${this.hostId}-region`, searchable: false, keepParentScroll: true, onItemSelected: (e) => this.handleRegionSelected(e.detail), onDropdownClosed: () => this.handleRegionDropdownClosed() }), this.errors && this.errors['receiver.region'] ?
                                index.h("span", { class: "s-gifting-error" }, this.errors['receiver.region']) : ''), this.isPhysical() &&
                            index.h("div", { class: this.step2ItemClass(this.errors?.['receiver.city_id'] ? 's-form-group s-form-has-error' : 's-form-group') }, index.h("label", { class: "s-form-label", htmlFor: `${this.hostId}-city` }, this.receiverCityFieldLabel), index.h("salla-searchable-dropdown", { placeholder: this.selectCity, items: this.displayedCities, selectedItem: this.selectedCity, loading: this.loadingCities, searching: this.searchingCities, disabled: this.loadingCities || (this.isKSA && !this.selectedRegionId), required: true, inputId: `${this.hostId}-city`, searchQuery: this.citySearchQuery, clientSearch: !this.enableCityAjaxSearch, dropUp: true, keepParentScroll: true, onItemSelected: (e) => this.handleCitySelected(e.detail), onSearchInput: (e) => this.handleCitySearch(e.detail), onDropdownClosed: () => this.handleCityDropdownClosed() }), this.errors && this.errors['receiver.city_id'] ?
                                index.h("span", { class: "s-gifting-error" }, this.errors['receiver.city_id']) : ''), this.isPhysical() && this.cityNoticeText() &&
                            index.h("div", { class: this.step2ItemClass('s-gifting-info') }, index.h("span", { innerHTML: Alert }), index.h("span", null, this.cityNoticeText())), !this.isPhysical() && index.h("div", { class: this.step2ItemClass() }, index.h("label", { class: "s-gifting-schedule s-gifting-clickable", htmlFor: `schedule-${this.hostId}` }, index.h("input", { type: "checkbox", name: 'schedule', id: `schedule-${this.hostId}`, onChange: () => this.toggleCalendar(), class: "s-checkbox" }), index.h("span", { class: "s-form-label" }, " ", this.sendLater, " "))), index.h("div", { class: this.getCalendarClasses(), ref: (el) => this.calendarFormGroup = el }, index.h("label", { class: "s-form-label" }, this.selectSendDateAndTime), index.h("salla-datetime-picker", { value: this.deliveryDate, placeholder: this.selectSendDateAndTime, "enable-time": true, "date-format": "Y-m-d h:i K", onPicked: (event) => this.handleDateTimePicker(event) }), index.h("span", { class: "s-gifting-calendar-hint" }, this.canNotEditOrderAfterSelectDate)), index.h("div", { class: this.step2ItemClass('s-gifting-step-two-footer') }, !this.skipStep1() && index.h("a", { href: "#!", innerHTML: arrowLeft.arrowLeft, onClick: (e) => this.goToStep1(e) }), index.h("salla-button", { onClick: () => this.submitForm(), color: "primary", width: 'wide' }, index.h("span", null, this.sendGift)))))
                    ], index.h("slot", { key: '688de0af2e48acc0d91d128a321856197861eb16', name: "footer" })))
        ];
    }
};
SallaGifting.CITY_AJAX_THRESHOLD = 400;
SallaGifting.CITY_SEARCH_DEBOUNCE_MS = 100;
SallaGifting.style = sallaGiftingCss;

exports.salla_gifting = SallaGifting;
