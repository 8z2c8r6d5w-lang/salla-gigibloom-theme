/*!
 * Crafted with ❤ by Salla
 */
'use strict';

var index = require('./index-DnjGg4mr.js');

const sallaOrderCancelCss = "/*!twlt*/.s-order-cancel-reason .s-order-cancel-radio::before,.s-order-cancel-reason .s-radio::before{content:\"\";width:10px;height:10px;border-radius:50%;transform:scale(0);transition:120ms transform ease-in-out;background-color:#fff;box-shadow:inset 1em 1em #fff}.s-order-cancel-reason .s-order-cancel-radio:checked::before,.s-order-cancel-reason .s-radio:checked::before{transform:scale(1);background-color:var(--color-primary);box-shadow:inset 1em 1em var(--color-primary)}.block{display:block}.flex{display:flex}.table{display:table}.w-full{width:100%}.outline{outline-style:solid}.filter{filter:var(--tw-blur) var(--tw-brightness) var(--tw-contrast) var(--tw-grayscale) var(--tw-hue-rotate) var(--tw-invert) var(--tw-saturate) var(--tw-sepia) var(--tw-drop-shadow)}.s-scrollbar::-webkit-scrollbar{width:5px}.s-scrollbar::-webkit-scrollbar-track{background-color:#eee}.s-scrollbar::-webkit-scrollbar-thumb{background-color:var(--color-primary);border-radius:10px;-webkit-border-radius:10px;-moz-border-radius:10px;-ms-border-radius:10px;-o-border-radius:10px}salla-modal.s-order-cancel-modal .s-modal-body{display:inline-flex;max-height:100svh;flex-direction:column;overflow:hidden;text-align:start}@media (min-width: 768px){salla-modal.s-order-cancel-modal .s-modal-body{max-height:92svh}}salla-modal.s-order-cancel-modal .s-modal-header{width:100%;flex-shrink:0;align-items:flex-start;text-align:start}salla-modal.s-order-cancel-modal .s-modal-header-inner{margin-bottom:1.25rem;display:block !important;width:100%;padding-inline-end:2.5rem;padding-inline-start:0px;text-align:start}@media (min-width: 768px){salla-modal.s-order-cancel-modal .s-modal-header-inner{padding:0px}}salla-modal.s-order-cancel-modal .s-modal-header-inner:where([dir=\"rtl\"],[dir=\"rtl\"] *){padding-inline-start:2.5rem;padding-inline-end:0px}salla-modal.s-order-cancel-modal .s-modal-header-content{width:100%;align-items:flex-start;text-align:start}salla-modal.s-order-cancel-modal .s-modal-title{margin-bottom:0px;width:100%;text-align:start;font-size:1.125rem;line-height:1.75rem;font-weight:700;--tw-text-opacity:1;color:rgb(31 41 55 / var(--tw-text-opacity, 1))}.s-order-cancel-cancelled-header-item{display:flex;width:100%;min-width:0px;align-items:center;justify-content:space-between;padding-left:0.75rem;padding-right:0.75rem;padding-top:0.5rem;padding-bottom:0.5rem;font-size:0.875rem;line-height:1.25rem}@media (min-width: 768px){.s-order-cancel-cancelled-header-item{display:table-cell;width:auto;max-width:14rem;padding-left:1.5rem;padding-right:1.5rem;padding-top:1rem;padding-bottom:1rem}}.s-order-cancel-cancelled-header-label{overflow:hidden;display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:2;font-weight:400;--tw-text-opacity:1;color:rgb(156 163 175 / var(--tw-text-opacity, 1))}.s-order-cancel-cancelled-header-label:where([dir=\"ltr\"],[dir=\"ltr\"] *){margin-right:0.5rem}.s-order-cancel-cancelled-header-label:where([dir=\"rtl\"],[dir=\"rtl\"] *){margin-left:0.5rem}.s-order-cancel-cancelled-header-reason{overflow:hidden;display:-webkit-box;-webkit-box-orient:vertical;-webkit-line-clamp:2;display:block;overflow-wrap:break-word;font-weight:700}.s-order-cancel-wrapper{display:block}.s-order-cancel-content{display:flex;min-height:0px;width:100%;flex:1 1 0%;flex-direction:column;gap:1.5rem;overflow:hidden;text-align:start}.s-order-cancel-intro{display:flex;width:100%;flex-shrink:0;flex-direction:column;gap:0.5rem}.s-order-cancel-heading{font-size:1rem;font-weight:700;line-height:1.5rem;--tw-text-opacity:1;color:rgb(31 41 55 / var(--tw-text-opacity, 1))}.s-order-cancel-subtitle{font-size:0.875rem;line-height:1.25rem;font-weight:400;line-height:1.5;--tw-text-opacity:1;color:rgb(107 114 128 / var(--tw-text-opacity, 1))}.s-order-cancel-reasons{display:flex;min-height:0px;width:100%;flex:1 1 0%;flex-direction:column;gap:1rem;overflow-y:auto;padding-left:0.25rem;padding-right:0.25rem;padding-top:0.5rem;padding-bottom:0.5rem}.s-order-cancel-reason{display:flex;width:100%;cursor:pointer;align-items:center;gap:0.5rem;accent-color:var(--color-primary)}.s-order-cancel-reason-label{flex:1 1 0%;text-align:start;font-size:0.875rem;font-weight:500;line-height:1.25rem;--tw-text-opacity:1;color:rgb(31 41 55 / var(--tw-text-opacity, 1))}.s-order-cancel-radio{margin:0px;display:grid;height:1.25rem;width:1.25rem;min-width:1.25rem;flex-shrink:0;cursor:pointer;appearance:none;place-content:center;border-radius:9999px;border-width:1px;border-color:var(--color-primary);--tw-bg-opacity:1;background-color:rgb(255 255 255 / var(--tw-bg-opacity, 1))}.s-order-cancel-radio:checked{border-color:var(--color-primary);--tw-bg-opacity:1;background-color:rgb(255 255 255 / var(--tw-bg-opacity, 1))}.s-order-cancel-radio:focus-visible{border-color:var(--color-primary);outline-color:var(--color-primary)}.s-order-cancel-radio:focus-visible:checked{border-color:var(--color-primary);outline-color:var(--color-primary)}.s-order-cancel-other-field{display:flex;width:100%;flex-shrink:0;flex-direction:column;gap:0.25rem}.s-order-cancel-textarea{min-height:6rem;width:100%;resize:none;text-align:start}.has-error .s-order-cancel-textarea{border-color:#ff443a;color:#ff443a}.s-order-cancel-textarea{display:block;width:100%;border-radius:var(--s-radius, 0.25rem);border-width:1px;--tw-border-opacity:1;border-color:rgb(209 213 219 / var(--tw-border-opacity, 1));--tw-bg-opacity:1;background-color:rgb(255 255 255 / var(--tw-bg-opacity, 1));padding:0.5rem;font-size:0.875rem;line-height:1.25rem;--tw-text-opacity:1;color:rgb(17 24 39 / var(--tw-text-opacity, 1))}.s-order-cancel-textarea:focus{border-color:var(--color-primary);--tw-ring-offset-shadow:var(--tw-ring-inset) 0 0 0 var(--tw-ring-offset-width) var(--tw-ring-offset-color);--tw-ring-shadow:var(--tw-ring-inset) 0 0 0 calc(0px + var(--tw-ring-offset-width)) var(--tw-ring-color);box-shadow:var(--tw-ring-offset-shadow), var(--tw-ring-shadow), var(--tw-shadow, 0 0 #0000)}.s-order-cancel-counter{text-align:end;font-size:0.75rem;line-height:1rem;--tw-text-opacity:1;color:rgb(107 114 128 / var(--tw-text-opacity, 1))}.s-order-cancel-counter--limit{--tw-text-opacity:1;color:rgb(239 68 68 / var(--tw-text-opacity, 1))}.s-order-cancel-actions{display:flex;width:100%;flex-shrink:0;flex-direction:column;gap:1rem;padding-top:0px}";

const MAX_NOTE = 250;
const ORDER_WAIT_MS = 2500;
const DATE_HEADER = 'td.single-order-header-item:not(.flex)';
const CANCELLED_MARKER = '[data-salla-order-cancelled-reason]';
const CANCELLED_BY_LABELS = {
    customer: 'أُلغي الطلب من قِبل العميل',
    merchant: 'أُلغي الطلب من قِبل المتجر',
};
const LANG_PAIRS = [
    ['pages.orders.cancel_modal_title', 'إلغاء الطلب', 'Cancel order'],
    ['pages.orders.cancel_reason_title', 'لماذا ترغب في إلغاء الطلب؟', 'Why do you want to cancel this order?'],
    ['pages.orders.cancel_reason_subtitle', 'ساعدنا نعرف السبب لتحسين تجربتك.', 'Help us understand why to improve your experience.'],
    ['pages.orders.cancel_reason_confirm', 'تأكيد إلغاء الطلب', 'Confirm order cancellation'],
    ['pages.orders.cancel_reason_back', 'تراجع', 'Go back'],
    ['pages.orders.cancel_reason_note_placeholder', 'اذكر السبب', 'Mention the reason'],
    ['pages.orders.cancelled_by_customer', CANCELLED_BY_LABELS.customer, 'Order cancelled by the customer'],
    ['pages.orders.cancelled_by_merchant', CANCELLED_BY_LABELS.merchant, 'Order cancelled by the merchant'],
    ['pages.orders.cancel_reason_char_limit', 'يتجاوز النص حد الأحرف المسموح', 'Text exceeds the allowed character limit'],
];
const parseJson = (value) => {
    if (!value)
        return null;
    try {
        const data = typeof value === 'string' ? JSON.parse(value) : value;
        return data && typeof data === 'object' ? data : null;
    }
    catch {
        return null;
    }
};
const parseCancelledReason = (raw) => {
    const data = parseJson(raw);
    const reasons = Array.isArray(data?.reasons)
        ? data.reasons.filter((r) => typeof r === 'string' && r.length > 0)
        : [];
    if (!reasons.length)
        return null;
    return { cancelled_by: data?.cancelled_by === 'merchant' ? 'merchant' : 'customer', reasons };
};
const normalizeReasons = (reasons) => reasons.filter(r => r.is_active !== false).sort((a, b) => (a.sort ?? 0) - (b.sort ?? 0));
const parseReasonsProp = (value) => {
    if (value == null || value === '')
        return null;
    const parsed = typeof value === 'string' ? parseJson(value) : value;
    return Array.isArray(parsed) && parsed.length ? parsed : null;
};
const SallaOrderCancel = class {
    constructor(hostRef) {
        index.registerInstance(this, hostRef);
        this.reasons = [];
        this.selectedReasonId = null;
        this.reasonNote = '';
        this.confirmLoading = false;
        this.loading = true;
        this.modalTitleLabel = '';
        this.questionLabel = '';
        this.subtitleLabel = '';
        this.confirmLabel = '';
        this.backLabel = '';
        this.notePlaceholder = '';
        this.charLimitLabel = '';
        salla.lang.onLoaded(() => {
            const ar = salla.lang.messages?.['ar.trans'];
            const en = salla.lang.messages?.['en.trans'];
            if (ar && en && salla.helpers?.setNested) {
                for (const [key, arText, enText] of LANG_PAIRS) {
                    salla.helpers.setNested(ar, key, arText);
                    salla.helpers.setNested(en, key, enText);
                }
            }
            this.refreshLabels();
        });
    }
    onCancellationReasonsChange() {
        void this.load();
    }
    async componentWillLoad() {
        await salla.onReady();
        this.refreshLabels();
        await this.load();
    }
    refreshLabels() {
        const t = (key, fb) => salla.lang.getWithDefault(key, fb);
        this.modalTitleLabel = t('pages.orders.cancel_modal_title', 'إلغاء الطلب');
        this.questionLabel = t('pages.orders.cancel_reason_title', 'لماذا ترغب في إلغاء الطلب؟');
        this.subtitleLabel = t('pages.orders.cancel_reason_subtitle', 'ساعدنا نعرف السبب لتحسين تجربتك.');
        this.confirmLabel = t('pages.orders.cancel_reason_confirm', 'تأكيد إلغاء الطلب');
        this.backLabel = t('pages.orders.cancel_reason_back', 'تراجع');
        this.notePlaceholder = t('pages.orders.cancel_reason_note_placeholder', 'اذكر السبب');
        this.charLimitLabel = t('pages.orders.cancel_reason_char_limit', 'يتجاوز النص حد الأحرف المسموح');
    }
    get resolvedOrderId() {
        const id = this.orderId ?? this.pageOrder?.id ?? salla.config.get('page.id');
        const n = Number(id);
        return Number.isFinite(n) ? n : id;
    }
    readPageOrder() {
        const el = document.querySelector('salla-order-totals-card');
        return el ? parseJson(el.order ?? el.getAttribute('order')) : null;
    }
    waitForPageOrder() {
        const order = this.readPageOrder();
        if (order)
            return Promise.resolve(order);
        if (typeof MutationObserver === 'undefined') {
            // ponytail: jsdom has no MutationObserver; one microtask retry for spec DOM
            return Promise.resolve().then(() => this.readPageOrder());
        }
        return new Promise(resolve => {
            let settled = false;
            const finish = () => {
                if (settled)
                    return;
                settled = true;
                observer.disconnect();
                clearTimeout(timer);
                resolve(this.readPageOrder());
            };
            const card = document.querySelector('salla-order-totals-card');
            const observer = new MutationObserver(finish);
            observer.observe(card ?? document.documentElement, card
                ? { attributes: true, attributeFilter: ['order'] }
                : { childList: true, subtree: true, attributes: true, attributeFilter: ['order'] });
            const timer = setTimeout(finish, ORDER_WAIT_MS);
        });
    }
    cancelledByLabel(by) {
        return salla.lang.getWithDefault(`pages.orders.cancelled_by_${by}`, CANCELLED_BY_LABELS[by]);
    }
    mountCancelledHeader(attempt = 0) {
        const cancelled = this.orderCancelledReason;
        const reason = cancelled?.reasons[0];
        if (!reason || document.querySelector(CANCELLED_MARKER))
            return;
        const anchor = document.querySelector(DATE_HEADER);
        if (!anchor) {
            // ponytail: hook mounts before header table in single.twig; retry until anchor exists
            if (attempt < 20)
                requestAnimationFrame(() => this.mountCancelledHeader(attempt + 1));
            return;
        }
        const cell = document.createElement('td');
        cell.className = 's-order-cancel-cancelled-header-item';
        cell.setAttribute('data-salla-order-cancelled-reason', '');
        const label = document.createElement('div');
        label.className = 's-order-cancel-cancelled-header-label';
        label.textContent = this.cancelledByLabel(cancelled.cancelled_by);
        const value = document.createElement('b');
        value.className = 's-order-cancel-cancelled-header-reason';
        value.textContent = reason;
        value.title = reason;
        cell.append(label, value);
        anchor.insertAdjacentElement('afterend', cell);
    }
    purgeLegacyModal() {
        if (!this.reasons.length)
            return;
        for (const el of document.querySelectorAll('salla-modal#modal-order-cancel:not([data-salla-order-cancel])')) {
            el.remove();
        }
    }
    applyPageOrder(order) {
        this.pageOrder = order;
        this.orderCancelledReason = parseCancelledReason(order.order_cancelled_reason) ?? undefined;
    }
    async load() {
        this.loading = true;
        try {
            const fromProp = parseReasonsProp(this.cancellationReasons);
            if (!this.pageOrder) {
                const order = this.readPageOrder();
                if (order)
                    this.applyPageOrder(order);
            }
            if (!this.pageOrder && !fromProp) {
                const order = await this.waitForPageOrder();
                if (order)
                    this.applyPageOrder(order);
            }
            else if (!this.pageOrder) {
                void this.waitForPageOrder().then(order => {
                    if (order) {
                        this.applyPageOrder(order);
                        this.mountCancelledHeader();
                    }
                });
            }
            const order = this.pageOrder;
            this.reasons = fromProp
                ? normalizeReasons(fromProp)
                : order
                    ? normalizeReasons(Array.isArray(order.cancellation_reasons) ? order.cancellation_reasons : [])
                    : [];
        }
        finally {
            this.loading = false;
            this.purgeLegacyModal();
            this.mountCancelledHeader();
        }
    }
    reasonLabel(reason) {
        const locale = salla.lang.getLocale?.() || 'ar';
        return reason.translations?.[locale]?.name || reason.name;
    }
    get hasOtherSelected() {
        return this.selectedReasonId !== null && this.reasons.some(r => r.id === this.selectedReasonId && r.slug === 'other');
    }
    get canConfirm() {
        if (this.selectedReasonId === null)
            return false;
        if (!this.hasOtherSelected)
            return true;
        const len = this.reasonNote.trim().length;
        return len > 0 && len <= MAX_NOTE;
    }
    selectReason(id) {
        if (this.selectedReasonId === id) {
            if (this.reasons.find(r => r.id === id)?.slug === 'other')
                this.reasonNote = '';
            this.selectedReasonId = null;
            return;
        }
        if (this.reasons.find(r => r.id === this.selectedReasonId)?.slug === 'other') {
            this.reasonNote = '';
        }
        this.selectedReasonId = id;
        if (this.reasons.length && this.reasons[this.reasons.length - 1].id === id) {
            const timer = setTimeout(() => {
                requestAnimationFrame(() => {
                    document.getElementById(`s-order-cancel-reason-${id}`)?.scrollIntoView({ block: 'start', behavior: 'smooth' });
                    const textarea = document.querySelector('.s-order-cancel-textarea');
                    textarea?.focus();
                    clearTimeout(timer);
                });
            }, 200);
        }
    }
    resetForm() {
        this.selectedReasonId = null;
        this.reasonNote = '';
        this.confirmLoading = false;
    }
    setReasonNote(event) {
        this.reasonNote = event.target.value;
    }
    onKeyActivate(event, action) {
        if (event.key === 'Enter' || event.key === ' ')
            action();
    }
    async confirmCancel() {
        if (!this.canConfirm || this.confirmLoading)
            return;
        this.confirmLoading = true;
        try {
            const note = this.hasOtherSelected ? this.reasonNote.trim() : '';
            const payload = {
                reasons: [String(this.selectedReasonId)],
                ...(note && { reason_note: note }),
            };
            await salla.order.api.cancel(this.resolvedOrderId, payload);
            window.location.reload();
        }
        catch {
            this.confirmLoading = false;
        }
    }
    render() {
        if (this.loading || !this.reasons.length)
            return index.h(index.Host, { class: "s-order-cancel-wrapper" });
        return (index.h(index.Host, { class: "s-order-cancel-wrapper" }, index.h("salla-modal", { id: "modal-order-cancel", class: "s-order-cancel-modal", "data-salla-order-cancel": "", ref: el => {
                this.modal = el;
                void el?.setTitle?.(this.modalTitleLabel);
            }, width: "sm", onModalVisibilityChanged: e => !e.detail && this.resetForm() }, index.h("div", { class: "s-order-cancel-content" }, index.h("div", { class: "s-order-cancel-intro" }, index.h("p", { class: "s-order-cancel-heading" }, this.questionLabel), index.h("p", { class: "s-order-cancel-subtitle" }, this.subtitleLabel)), index.h("div", { class: "s-order-cancel-reasons s-scrollbar" }, this.reasons.map(reason => {
            const id = `s-order-cancel-reason-${reason.id}`;
            return (index.h("label", { key: reason.id, class: "s-order-cancel-reason", htmlFor: id }, index.h("input", { type: "radio", name: "cancellation_reason", id: id, class: "s-order-cancel-radio s-radio", checked: this.selectedReasonId === reason.id, onChange: () => this.selectReason(reason.id) }), index.h("span", { class: "s-order-cancel-reason-label" }, this.reasonLabel(reason))));
        })), this.hasOtherSelected && (index.h("div", { class: "s-order-cancel-other-field" }, index.h("textarea", { class: "s-order-cancel-textarea", placeholder: this.notePlaceholder, value: this.reasonNote, onInput: e => this.setReasonNote(e) }), index.h("p", { class: {
                's-order-cancel-counter': true,
                's-order-cancel-counter--limit': this.reasonNote.length >= MAX_NOTE,
            } }, this.reasonNote.length >= MAX_NOTE && `${this.charLimitLabel} `, MAX_NOTE, "/", this.reasonNote.length))), index.h("div", { class: "s-order-cancel-actions" }, index.h("salla-button", { color: "primary", class: "w-full", width: "wide", disabled: !this.canConfirm, loading: this.confirmLoading, onClick: () => this.confirmCancel(), onKeyDown: e => this.onKeyActivate(e, () => this.confirmCancel()) }, this.confirmLabel), index.h("salla-button", { class: "w-full", width: "wide", fill: "outline", color: "gray", onClick: () => this.modal?.close(), onKeyDown: e => this.onKeyActivate(e, () => this.modal?.close()) }, this.backLabel))))));
    }
    static get watchers() { return {
        "cancellationReasons": ["onCancellationReasonsChange"]
    }; }
};
SallaOrderCancel.style = sallaOrderCancelCss;

exports.salla_order_cancel = SallaOrderCancel;
