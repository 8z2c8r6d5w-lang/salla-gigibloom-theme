/*!
 * Crafted with ❤ by Salla
 */
import { h, Host } from "@stencil/core";
import { isVideoMimeType, isImageMimeType } from "../salla-file-upload/upload-limits";
import PlayIcon from "../../assets/svg/play-circle.svg";
/**
 * Renders a horizontal strip of image/video attachment thumbnails with an
 * optional "+N" overflow indicator on the last visible item. When `clickable`
 * is true, opens `salla-attachment-view` when a thumbnail is clicked.
 *
 * Media can be supplied as:
 * - `attachments` — full `{ id, url, mimeType?/type? }` objects
 * - `images` / `videos` — URL string arrays
 * - `rating` — object with `images` / `videos` arrays
 * - `productRatings` + `productId` — match a product rating, else fall back to `rating`
 */
export class SallaAttachments {
    constructor() {
        /**
         * Attachments to display. Accepts an array of `{ id, url, mimeType? }` or a
         * JSON string of the same shape.
         */
        this.attachments = [];
        /**
         * Image URLs to display. Accepts a string array or a JSON string.
         */
        this.images = [];
        /**
         * Video URLs to display. Accepts a string array or a JSON string.
         */
        this.videos = [];
        /**
         * Rating-like object with `images` / `videos` URL arrays. Used when
         * `images`/`videos` are empty, and as a fallback when resolving
         * `productRatings`.
         */
        this.rating = null;
        /**
         * Product ratings list. When set with `productId`, the matching rating's
         * media is preferred over `rating`.
         */
        this.productRatings = [];
        /**
         * Product id used to match an entry in `productRatings`.
         */
        this.productId = null;
        /**
         * Maximum number of thumbnails to show before displaying a "+N" overlay on
         * the last visible item.
         */
        this.maxVisible = 4;
        /**
         * When true, clicking a thumbnail opens the attachment lightbox.
         */
        this.clickable = false;
    }
    parseJson(value, fallback) {
        if (typeof value !== 'string') {
            return (value ?? fallback);
        }
        try {
            return JSON.parse(value);
        }
        catch {
            return fallback;
        }
    }
    parseStringList(value) {
        const parsed = this.parseJson(value, []);
        return Array.isArray(parsed) ? parsed.filter(url => typeof url === 'string' && url) : [];
    }
    parseMediaSource(value) {
        if (!value)
            return null;
        const parsed = this.parseJson(value, null);
        return parsed && typeof parsed === 'object' && !Array.isArray(parsed) ? parsed : null;
    }
    parseMediaSourceList(value) {
        const parsed = this.parseJson(value, []);
        return Array.isArray(parsed) ? parsed : [];
    }
    resolveMediaSource() {
        const images = this.parseStringList(this.images);
        const videos = this.parseStringList(this.videos);
        if (images.length || videos.length) {
            return { images, videos };
        }
        const rating = this.parseMediaSource(this.rating);
        const productRatings = this.parseMediaSourceList(this.productRatings);
        const productId = this.productId == null || this.productId === ''
            ? null
            : String(this.productId);
        if (productId && productRatings.length) {
            const matched = productRatings.find(entry => entry?.product?.id != null && String(entry.product.id) === productId);
            if (matched)
                return matched;
        }
        return rating || { images: [], videos: [] };
    }
    mediaToAttachments(source) {
        const images = (source.images || []).map(url => ({ type: 'image', url }));
        const videos = (source.videos || []).map(url => ({ type: 'video', url }));
        return [...images, ...videos];
    }
    getAttachments() {
        let items = [];
        if (typeof this.attachments === 'string') {
            try {
                const parsed = JSON.parse(this.attachments);
                items = Array.isArray(parsed) ? parsed : [];
            }
            catch {
                items = [];
            }
        }
        else if (Array.isArray(this.attachments)) {
            items = this.attachments;
        }
        if (!items.length) {
            items = this.mediaToAttachments(this.resolveMediaSource());
        }
        return items
            .map((attachment, index) => this.normalizeAttachment(attachment, index))
            .filter(attachment => this.isRenderableAttachment(attachment));
    }
    normalizeAttachment(attachment, index) {
        const mimeType = attachment.mimeType || this.resolveMimeType(attachment.type, attachment.url);
        return {
            id: attachment.id || `attachment-${index}`,
            url: attachment.url,
            mimeType,
        };
    }
    resolveMimeType(type, url) {
        if (type === 'image')
            return 'image/jpeg';
        if (type === 'video')
            return 'video/mp4';
        if (type?.includes('/'))
            return type;
        if (url && /\.(png|jpe?g|gif|webp|heic|heif)$/i.test(url))
            return 'image/jpeg';
        if (url && /\.(mp4|webm|mov|avi|mkv)$/i.test(url))
            return 'video/mp4';
        return type;
    }
    isRenderableAttachment(attachment) {
        return isImageMimeType(attachment.mimeType, attachment.url)
            || isVideoMimeType(attachment.mimeType, attachment.url);
    }
    isVideo(attachment) {
        return isVideoMimeType(attachment.mimeType, attachment.url);
    }
    openAt(index) {
        if (!this.clickable)
            return;
        this.attachmentView?.open(index);
    }
    render() {
        const items = this.getAttachments();
        if (!items.length)
            return null;
        const visible = items.slice(0, this.maxVisible);
        const remaining = items.length - this.maxVisible;
        return (h(Host, null, h("div", { class: {
                's-attachments': true,
                'is-clickable': this.clickable,
            } }, visible.map((attachment, index) => {
            const isLastVisible = index === this.maxVisible - 1;
            const showMoreOverlay = isLastVisible && remaining > 0;
            const thumbProps = this.clickable
                ? { onClick: () => this.openAt(index) }
                : {};
            return this.isVideo(attachment)
                ? h("div", { key: attachment.id, class: "s-attachments-video-thumb", ...thumbProps }, h("video", { src: attachment.url, muted: true, playsInline: true, preload: "metadata" }), showMoreOverlay
                    ? h("span", { class: "s-attachments-more-overlay" }, "+", remaining)
                    : h("span", { class: "s-attachments-video-thumb-play", innerHTML: PlayIcon }))
                : h("div", { key: attachment.id, class: "s-attachments-image-thumb", ...thumbProps }, h("img", { src: attachment.url, alt: "" }), showMoreOverlay
                    ? h("span", { class: "s-attachments-more-overlay" }, "+", remaining)
                    : null);
        }), this.clickable && (h("salla-attachment-view", { ref: el => (this.attachmentView = el), attachments: items })))));
    }
    static get is() { return "salla-attachments"; }
    static get originalStyleUrls() {
        return {
            "$": ["salla-attachments.scss"]
        };
    }
    static get styleUrls() {
        return {
            "$": ["salla-attachments.css"]
        };
    }
    static get properties() {
        return {
            "attachments": {
                "type": "string",
                "attribute": "attachments",
                "mutable": false,
                "complexType": {
                    "original": "Attachment[] | string",
                    "resolved": "Attachment[] | string",
                    "references": {
                        "Attachment": {
                            "location": "import",
                            "path": "../salla-attachment-view/salla-attachment-view",
                            "id": "src/components/salla-attachment-view/salla-attachment-view.tsx::Attachment"
                        }
                    }
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Attachments to display. Accepts an array of `{ id, url, mimeType? }` or a\nJSON string of the same shape."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "[]"
            },
            "images": {
                "type": "string",
                "attribute": "images",
                "mutable": false,
                "complexType": {
                    "original": "string[] | string",
                    "resolved": "string | string[]",
                    "references": {}
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Image URLs to display. Accepts a string array or a JSON string."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "[]"
            },
            "videos": {
                "type": "string",
                "attribute": "videos",
                "mutable": false,
                "complexType": {
                    "original": "string[] | string",
                    "resolved": "string | string[]",
                    "references": {}
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Video URLs to display. Accepts a string array or a JSON string."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "[]"
            },
            "rating": {
                "type": "string",
                "attribute": "rating",
                "mutable": false,
                "complexType": {
                    "original": "MediaSource | string",
                    "resolved": "string | { images?: string[]; videos?: string[]; product?: { id?: string | number; }; }",
                    "references": {
                        "MediaSource": {
                            "location": "global",
                            "id": "global::MediaSource"
                        }
                    }
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Rating-like object with `images` / `videos` URL arrays. Used when\n`images`/`videos` are empty, and as a fallback when resolving\n`productRatings`."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "null"
            },
            "productRatings": {
                "type": "string",
                "attribute": "product-ratings",
                "mutable": false,
                "complexType": {
                    "original": "MediaSource[] | string",
                    "resolved": "MediaSource[] | string",
                    "references": {
                        "MediaSource": {
                            "location": "global",
                            "id": "global::MediaSource"
                        }
                    }
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Product ratings list. When set with `productId`, the matching rating's\nmedia is preferred over `rating`."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "[]"
            },
            "productId": {
                "type": "any",
                "attribute": "product-id",
                "mutable": false,
                "complexType": {
                    "original": "number | string",
                    "resolved": "number | string",
                    "references": {}
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Product id used to match an entry in `productRatings`."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "null"
            },
            "maxVisible": {
                "type": "number",
                "attribute": "max-visible",
                "mutable": false,
                "complexType": {
                    "original": "number",
                    "resolved": "number",
                    "references": {}
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "Maximum number of thumbnails to show before displaying a \"+N\" overlay on\nthe last visible item."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "4"
            },
            "clickable": {
                "type": "boolean",
                "attribute": "clickable",
                "mutable": false,
                "complexType": {
                    "original": "boolean",
                    "resolved": "boolean",
                    "references": {}
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": "When true, clicking a thumbnail opens the attachment lightbox."
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "false"
            }
        };
    }
}
