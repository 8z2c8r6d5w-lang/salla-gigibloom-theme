/*!
 * Crafted with ❤ by Salla
 */
import { h } from "@stencil/core";
export class SallaCommentFactors {
    constructor() {
        this.factors = [];
    }
    getReviewFactorLabel(factor) {
        if (factor.label) {
            return factor.label;
        }
        // Fall back to defaults matching the storefront locale when the
        // translation keys are missing, instead of always showing Arabic.
        const isArabic = (salla.lang.locale || 'ar').startsWith('ar');
        const defaultLabels = isArabic
            ? ['ضعيف', 'مقبول', 'جيد', 'جيد جدا', 'ممتاز']
            : ['Poor', 'Average', 'Good', 'Very good', 'Excellent'];
        const ratingLabels = [
            salla.lang.getWithDefault('pages.rating.poor', defaultLabels[0]),
            salla.lang.getWithDefault('pages.rating.average', defaultLabels[1]),
            salla.lang.getWithDefault('pages.rating.good', defaultLabels[2]),
            salla.lang.getWithDefault('pages.rating.very_good', defaultLabels[3]),
            salla.lang.getWithDefault('pages.rating.excellent', defaultLabels[4]),
        ];
        const rating = Number(factor.rating || 0);
        const clampedRating = Math.max(1, Math.min(5, Math.round(rating)));
        return ratingLabels[clampedRating - 1] || '';
    }
    render() {
        if (!this.factors?.length) {
            return null;
        }
        return (h("div", { class: "s-comments-item-review-factors" }, this.factors.map((factor) => (h("div", { class: "s-comments-item-review-factor-row" }, h("div", { class: "s-comments-item-review-factor-value" }, h("span", { class: "s-comments-item-review-factor-label text-left rtl:text-right" }, this.getReviewFactorLabel(factor)), h("salla-rating-stars", { size: "mini", class: "s-comments-item-review-factor-stars", value: factor.rating })), h("span", { class: "s-comments-item-review-factor-name" }, factor.name))))));
    }
    static get is() { return "salla-comment-factors"; }
    static get originalStyleUrls() {
        return {
            "$": ["salla-comments.scss"]
        };
    }
    static get styleUrls() {
        return {
            "$": ["salla-comments.css"]
        };
    }
    static get properties() {
        return {
            "factors": {
                "type": "unknown",
                "attribute": "factors",
                "mutable": false,
                "complexType": {
                    "original": "ReviewFactor[]",
                    "resolved": "ReviewFactor[]",
                    "references": {
                        "ReviewFactor": {
                            "location": "import",
                            "path": "./interfaces",
                            "id": "src/components/salla-comments/interfaces.ts::ReviewFactor"
                        }
                    }
                },
                "required": false,
                "optional": false,
                "docs": {
                    "tags": [],
                    "text": ""
                },
                "getter": false,
                "setter": false,
                "defaultValue": "[]"
            }
        };
    }
}
