/*!
 * Crafted with ❤ by Salla
 */
export const IMAGE_UPLOAD_ACCEPT = 'image/png, image/jpeg, image/jpg';
/** Valid MIME types + extensions for native file pickers (macOS Finder requires real MIME types). */
export const VIDEO_UPLOAD_ACCEPT = 'video/mp4, video/quicktime, video/x-msvideo, video/x-matroska, video/webm, .mp4, .mov, .avi, .mkv, .webm';
export const MEDIA_UPLOAD_ACCEPT = `${IMAGE_UPLOAD_ACCEPT}, ${VIDEO_UPLOAD_ACCEPT}`;
export function buildMediaUploadAccept(allowImages, allowVideos) {
    const types = [];
    if (allowImages)
        types.push(IMAGE_UPLOAD_ACCEPT);
    if (allowVideos)
        types.push(VIDEO_UPLOAD_ACCEPT);
    return types.join(', ');
}
export function usesSplitCountLimits(maxImagesCount, maxVideosCount) {
    return maxImagesCount != null || maxVideosCount != null;
}
export function isConfiguredLimit(limit) {
    return limit != null;
}
export function getMaxImagesCount(maxImagesCount) {
    return maxImagesCount ?? 0;
}
export function getMaxVideosCount(maxVideosCount) {
    return maxVideosCount ?? 0;
}
export function getTotalMaxFiles(maxFilesCount, maxImagesCount, maxVideosCount) {
    if (usesSplitCountLimits(maxImagesCount, maxVideosCount)) {
        return getMaxImagesCount(maxImagesCount) + getMaxVideosCount(maxVideosCount);
    }
    return maxFilesCount;
}
export function isVideoMimeType(mimeType, urlOrName) {
    if (mimeType?.startsWith('video/'))
        return true;
    if (urlOrName && /\.(mp4|webm|mov|avi|mkv)$/i.test(urlOrName))
        return true;
    return false;
}
export function isImageMimeType(mimeType, urlOrName) {
    if (mimeType?.startsWith('image/'))
        return true;
    if (urlOrName && /\.(png|jpe?g|gif|webp|heic|heif)$/i.test(urlOrName))
        return true;
    return false;
}
export function parseSizeToBytes(size) {
    const match = size.match(/^(\d+(?:\.\d+)?)\s*(KB|MB)$/i);
    if (!match)
        return 0;
    const value = parseFloat(match[1]);
    return match[2].toUpperCase() === 'MB' ? value * 1024 * 1024 : value * 1024;
}
export function formatBytesAsSize(bytes) {
    if (bytes >= 1024 * 1024) {
        return `${Math.round(bytes / (1024 * 1024))}MB`;
    }
    return `${Math.round(bytes / 1024)}KB`;
}
export function formatDuration(seconds) {
    const mins = Math.floor(seconds / 60);
    const secs = Math.floor(seconds % 60);
    if (mins > 0) {
        return `${mins}:${secs.toString().padStart(2, '0')}`;
    }
    return `${secs}s`;
}
export function getFilePondMaxFileSize(maxFileSize, storeUploadSizeKb, maxVideoFileSize) {
    const imageLimit = storeUploadSizeKb ? `${storeUploadSizeKb}KB` : maxFileSize;
    const limits = [imageLimit, maxVideoFileSize].filter(Boolean).map(s => parseSizeToBytes(s));
    if (!limits.length) {
        return null;
    }
    return formatBytesAsSize(Math.max(...limits));
}
export function isUploadSizeError(error) {
    const body = (error.body || '').toLowerCase();
    return (error.type === 'size' ||
        body.includes('too large') ||
        body.includes('max file size') ||
        body.includes('file is too big') ||
        body.includes('exceeds'));
}
export function isUploadTypeError(error) {
    const body = (error.body || '').toLowerCase();
    return (error.type === 'type' ||
        body.includes('invalid type') ||
        body.includes('not allowed') ||
        body.includes('file type'));
}
export function getUploadErrorMessage(error, options) {
    if (isUploadSizeError(error)) {
        if (options.isVideo) {
            const sizeLabel = options.maxVideoFileSizeLabel ||
                (options.maxVideoBytes ? formatBytesAsSize(options.maxVideoBytes) : '');
            return options.videoTooLarge(sizeLabel);
        }
        return options.tooLargeLabel;
    }
    if (isUploadTypeError(error)) {
        return options.invalidTypeLabel;
    }
    if (error.body) {
        return error.body;
    }
    return options.genericErrorLabel;
}
/** FilePond FileStatus values used for gallery counting. */
export const FILE_STATUS_INIT = 1;
export const FILE_STATUS_IDLE = 2;
export const FILE_STATUS_PROCESSING = 3;
export const FILE_STATUS_PROCESSING_COMPLETE = 5;
export const FILE_STATUS_PROCESSING_ERROR = 6;
export const FILE_STATUS_LOADING = 7;
export const FILE_STATUS_LOAD_ERROR = 8;
export const FILE_STATUS_PROCESSING_QUEUED = 9;
export function shouldCountGalleryFile(status) {
    return status !== FILE_STATUS_LOAD_ERROR && status !== FILE_STATUS_PROCESSING_ERROR;
}
/** Exclude in-flight add validation so limit counters don't spike on rejected picks. */
export function shouldCountGalleryLimit(status) {
    if (!shouldCountGalleryFile(status)) {
        return false;
    }
    return status !== FILE_STATUS_LOADING && status !== FILE_STATUS_INIT;
}
