import type { City } from './interfaces';
/** Filters cities by Arabic/English name (used inside the city-change modal). */
export declare function filterCitiesForModal(cities: City[], query: string): City[];
