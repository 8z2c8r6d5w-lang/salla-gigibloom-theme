interface EditOrderButtonData {
    id: string | number;
    is_editable?: boolean;
    edit_url?: string;
    editable_until?: string;
    customer_editing?: {
        enabled?: boolean;
        ends_at?: string;
        edit_url?: string;
        payment_url?: string;
    };
}
export declare class SallaEditOrderButton {
    /** The order ID to fetch details for. Falls back to the current page order ID. */
    orderId?: string | number;
    order?: EditOrderButtonData | null;
    loading: boolean;
    componentWillLoad(): Promise<void>;
    onOrderIdChange(): Promise<void>;
    private get paymentUrl();
    private loadOrder;
    private renderEditButton;
    render(): any;
}
export {};
