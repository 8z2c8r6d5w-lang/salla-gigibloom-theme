import { GiftResponse, GiftImage, Region, Cities, Country } from './gift-schema';
/**
 * @slot widget-btn-content - Used to customize widget button content.
 */
export declare class SallaGifting {
    constructor();
    private modal;
    private uploader?;
    private stepsWrapper?;
    private textSelect?;
    private customTextArea?;
    private textArea?;
    private calendarFormGroup?;
    private step1Elems?;
    private step2Elems?;
    private defaultEvent;
    /** Order options picked in the pre-gift modal, sent as `order_options` with the gift payload. */
    private collectedOrderOptions;
    /** Live PDP price, kept in sync by `onPriceUpdated`, used to seed the order-options total. */
    private livePrice;
    sectionTitle: string;
    sectionSubtitle: string;
    sectionBtnText: string;
    giftDetails: string;
    selectImageForYourGift: string;
    selectImageOrUpload: string;
    selectGiftMessage: string;
    giftText: string;
    giftCustomText: string;
    textId: string;
    incorrectGiftText: string;
    nextStep: string;
    senderNameLabel: string;
    receiverNameFieldLabel: string;
    receiverMobileFieldLabel: string;
    receiverCountryFieldLabel: string;
    ksa: string;
    selectCountry: string;
    selectCity: string;
    selectCityInfo: string;
    cityChangeNotice: string;
    receiverCityFieldLabel: string;
    receiverRegionFieldLabel: string;
    selectRegion: string;
    receiverEmailFieldLabel: string;
    emailPlaceholder: string;
    sendLater: string;
    selectSendDateAndTime: string;
    canNotEditOrderAfterSelectDate: string;
    sendGift: string;
    donationRequired: string;
    currentStep: number;
    showCalendar: boolean;
    showGiftText: boolean;
    currentLang: string;
    parentClass: string;
    errors: object;
    gift: GiftResponse;
    selectedGiftTextOption: string;
    selectedCountryId: number;
    selectedCountryCode: string;
    loadingCities: boolean;
    loadingRegions: boolean;
    regions: Region[];
    selectedRegionId: number;
    isKSA: boolean;
    selectedCountry: Country | null;
    selectedRegion: Region | null;
    displayedCities: Cities[];
    selectedCity: Cities | null;
    searchingCities: boolean;
    enableCityAjaxSearch: boolean;
    citySearchQuery: string;
    private step2Animated;
    private static readonly CITY_AJAX_THRESHOLD;
    private static readonly CITY_SEARCH_DEBOUNCE_MS;
    private citySearchTimer;
    private citySearchCounter;
    private locationRequestCounter;
    parsedFormData?: Record<string, any>;
    showTextArea: boolean;
    selectedImage: string;
    uploadedImage: string;
    selectedText: string;
    senderName: string;
    errorMessage: string;
    hasError: boolean;
    quantity: number;
    deliveryDate: string;
    timeZone?: string;
    receiverName: string;
    receiverMobile: string;
    receiverCountryCode: string;
    receiverEmail: string;
    hostId: string;
    disableClass: string;
    receiverCity: string;
    /**
     * The product id for which the gifting system is required.
    */
    productId: number;
    /**
     * Widget title
     */
    widgetTitle: string;
    /**
     * Widget subtitle
     */
    widgetSubtitle: string;
    /**
     * Is Physical products
     */
    physicalProducts: boolean;
    /**
     * Is Physical products (alias of `physicalProducts` for themes passing `physical`)
     */
    physical: boolean;
    private isPhysical;
    /**
     * Resolves the city notice text shown in the physical-product gift step:
     * - recipient can't change address           -> default city info notice
     * - recipient can change, fixed shipping cost -> city-change notice
     * - recipient can change, any shipping cost   -> no notice
     */
    private cityNoticeText;
    /**
     * Is Vertical widget
     */
    vertical: boolean;
    /**
     * The form selector to be used to get the form data
     */
    formSelector: string;
    componentDidLoad(): void;
    private getFormDataFromForm;
    private mergeObjectToFormData;
    /**
     * Order-options gate. Two shapes, because the two gifting entry points already differ:
     *
     * - **Cart page**: the order-option forms are rendered inline and synced to the cart on change,
     *   so gifting reuses the cart-submit check — block on invalid, never re-ask in a modal.
     * - **PDP**: nothing renders the options, so they're collected in the shared
     *   <salla-order-options-modal> *before* the gifting modal, the same modal and sequence the PDP
     *   uses for Quick Purchase and Apple Pay.
     *
     * Returns false when the buyer must fix something first (invalid inline form, or a dismissed
     * modal); the gifting modal then stays closed and they retry from the widget button.
     */
    private collectOrderOptions;
    /**
     * Same check the theme runs on cart submit: every `form[id^="item-"]` (cart items *and* the
     * inline order-option forms) must be valid. Values are already on the cart — each form syncs
     * through `cart.updateItem` on change — so nothing is collected here, gifting is only blocked.
     */
    private validateInlineCartOptions;
    /** What to ask order options for on the PDP. `basePrice` seeds the modal's live total. */
    private getOrderOptionsTarget;
    /** `quantity` state is only seeded on price updates, so fall back to the product form. */
    private getGiftQuantity;
    /** Get-or-create the singleton order-options modal on <body>. */
    private ensureOrderOptionsModal;
    /**
     * Show / Open the gifting modal window
    */
    open(): Promise<any>;
    skipStep1(): boolean;
    /**
     * anime.js writes inline transform/opacity onto the step containers and their
     * `.anime-item` children while transitioning between steps. The modal is only
     * hidden on close (the component is never destroyed), and Stencil never set
     * those inline styles so it won't clear them on re-render. Without this reset,
     * reopening keeps step 2 painted on top (leftover opacity/translate) while the
     * markup is back on step 1 — clicks then fall through to step 1's uploader.
     */
    private resetStepsAnimation;
    /**
     *
     * Hide / close the gifting modal window
    */
    close(): Promise<HTMLElement>;
    /**
     * Update the modal height based on the changes on the inner elements height for a specific step OR just a pass a new fixed height
    */
    private setWrapperHeight;
    private toggleCalendar;
    private toggleGiftText;
    /**
     *
     * Go to the step 2
     */
    goToStep2(withValidation?: boolean): Promise<void>;
    validateStep1(): boolean;
    /**
     *
     * Go to the step 1
     */
    goToStep1(e: any): void;
    private getFilepondPlaceholder;
    setPreview(image: GiftImage): void;
    removePreview(): void;
    private handleTextAreaChange;
    private clearError;
    private handleSenderName;
    private handleReceiverName;
    private resetCityState;
    private setInitialCities;
    private fetchCities;
    private handleCitySearch;
    private handleCitySelected;
    private handleCityDropdownClosed;
    private handleCountryDropdownClosed;
    private handleRegionDropdownClosed;
    private handleCountrySelected;
    private handleRegionSelected;
    private handleUploadImage;
    private handleRemoveImage;
    private handlePhoneInputChange;
    private handleDateTimePicker;
    private getCalendarClasses;
    private submitForm;
    private step2ItemClass;
    private generateClass;
    private handleGiftButtonClick;
    render(): any[];
}
