import { BundleSection } from '../interfaces';
export declare class SallaMultipleBundleProductDetails {
    /** The list of sections belonging to a bundle product. */
    sections: BundleSection[];
    selectedProducts: {
        [sectionId: string]: Set<string | number>;
    };
    private productSelectedHandler;
    host: HTMLElement;
    private isSingleProductSection;
    private getObligatoryMin;
    private isProductSelectionLocked;
    /** Empty max → cap is the number of products in the section. */
    private getEffectiveMax;
    private getSelectedCount;
    private isAtSelectionLimit;
    private canSelectProductInSection;
    canSelectBundleProduct(sectionId: string | number, productId: string | number): Promise<boolean>;
    private queryProductCheckbox;
    private dispatchBubblingChange;
    /** Uncheck checkbox, remove form inputs, and reset modal state for one product slot. */
    private clearSectionProductSlot;
    /** max=1: replace section selection and keep DOM/form in sync. */
    private syncSectionSelection;
    private autoSelectSingleProductSections;
    private clearProductFormData;
    private clearProductModalOptions;
    onSelectProduct: (sectionId: string | number, product: any) => void;
    ensureProductSelected: (sectionId: string | number, product: any) => void;
    onSelectProductOptions: (product: any, sectionId: any) => void;
    private handleBundleSliderProductSelected;
    private handleBundleSliderProductOptionsSelected;
    private getProgressStatus;
    private getSectionSelectionNote;
    handleSectionsChange(): void;
    componentWillLoad(): void;
    private renderAccordionHeader;
    componentDidLoad(): void;
    disconnectedCallback(): void;
    render(): any;
}
