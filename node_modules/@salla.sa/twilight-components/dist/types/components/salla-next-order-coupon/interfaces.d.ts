export interface CouponAmount {
    amount: string;
    currency: string;
}
export interface NextOrderCoupon {
    code: string;
    type: 'P' | 'fixed' | 'percentage';
    amount: number | string | CouponAmount;
    minimum_amount?: string | CouponAmount;
    expiry_date?: string;
}
