import { OrderBranch } from './interfaces';
/**
 * @salla/ui-components
 * The `salla-order-branch` component renders pickup branch details for thank-you pages.
 */
export declare class SallaOrderBranch {
    /** Stack the identity and contact cards vertically instead of side-by-side. */
    stackCards: boolean;
    branchData?: OrderBranch;
    isLoading: boolean;
    componentWillLoad(): Promise<void>;
    private getBranchData;
    private parseBranchResponse;
    private getApiErrorMessage;
    private notifyApiError;
    private label;
    private get preparationText();
    private unitLabel;
    private get workingHoursItems();
    private get branchAddress();
    private get mapUrl();
    private get phone();
    private renderDetailsColumn;
    private renderWorkingHoursCard;
    private renderBranchIdentityCard;
    render(): any;
}
