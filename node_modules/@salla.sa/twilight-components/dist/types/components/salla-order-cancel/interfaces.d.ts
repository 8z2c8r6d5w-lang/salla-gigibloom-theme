export interface CancellationReason {
    id: number;
    name: string;
    type: 'system' | 'custom';
    slug: string | null;
    sort?: number;
    is_active?: boolean;
    translations?: Record<string, {
        name: string;
    }>;
}
export interface CancelOrderPayload {
    reasons: string[];
    reason_note?: string;
}
export type CancelledBy = 'customer' | 'merchant';
export interface OrderCancelledReason {
    cancelled_by: CancelledBy;
    reasons: string[];
}
