import { EventEmitter } from '../../stencil-public-runtime';
import { OrderEditItem, OrderEditProductOption, OrderEditSelectedOptions } from './interfaces';
/**
 * @salla/ui-components
 * The `salla-order-edit-item` component renders a single editable order item card
 * with quantity controls, editable product options, and a remove button.
 */
export declare class SallaOrderEditItem {
    /** Item data object */
    item: OrderEditItem;
    /** Parent order ID (for API calls) */
    orderId: string | number;
    quantity: number;
    /** Emitted when an item quantity or options are updated */
    orderItemUpdated: EventEmitter<{
        itemId: string | number;
        quantity: number;
        options?: OrderEditSelectedOptions;
        optionDefinitions?: OrderEditProductOption[];
    }>;
    /** Emitted when an item is successfully removed */
    orderItemRemoved: EventEmitter<{
        itemId: string | number;
    }>;
    private quantityInput;
    private productOptions;
    componentWillLoad(): void;
    handleItemChange(newItem: OrderEditItem): void;
    private handleQuantityChange;
    private get hasOptions();
    private getSelectedOptions;
    private getOptionsData;
    private emitUpdate;
    private handleOptionsChange;
    private removeItem;
    render(): any;
}
