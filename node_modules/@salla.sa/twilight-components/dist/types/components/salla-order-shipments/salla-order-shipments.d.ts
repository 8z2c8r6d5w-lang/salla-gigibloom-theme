import { Shipment } from './interfaces';
/**
 * @salla/ui-components
 * The `salla-order-shipments` component renders a per-shipment tracking breakdown
 * on the thank-you page for orders fulfilled as split shipments (2+ shipments).
 * It renders nothing for single-shipment orders.
 */
export declare class SallaOrderShipments {
    /** Override the order id; defaults to the current page's order id. */
    orderId?: string | number;
    shipments: Shipment[];
    private request;
    componentWillLoad(): Promise<void>;
    onOrderIdChange(): Promise<void>;
    disconnectedCallback(): void;
    private fetchShipments;
    private label;
    private copyTracking;
    private renderTracking;
    private renderShipment;
    render(): any;
}
