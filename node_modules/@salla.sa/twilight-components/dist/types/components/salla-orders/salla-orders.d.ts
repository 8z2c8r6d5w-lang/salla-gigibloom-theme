import type { Order, OrderQueryParameters } from './interface';
import type { Pagination } from '../../interfaces';
type OrdersFilter = 'all' | 'archived' | 'waiting';
export declare class SallaOrder {
    constructor();
    /**
     * Query Parameter to send along with the fetch request
     */
    params: OrderQueryParameters;
    /**
     * Load more text
     */
    loadMoreText: string;
    orderNumberText: string;
    totalOrderText: string;
    orderDateText: string;
    orderStatusText: string;
    noOrderText: string;
    load_more_text_trans: any;
    languageCode: string;
    filterAllText: string;
    filterArchivedText: string;
    filterAwaitingReviewText: string;
    leaveReviewText: string;
    noUnratedOrdersText: string;
    noArchivedOrdersText: string;
    orders: Array<Order>;
    pagination: Pagination;
    hasInfiniteScroll: boolean;
    total: number;
    nextPage: string | null;
    showPlaceholder: boolean;
    activeFilter: OrdersFilter;
    pendingReviewCount: number;
    emptyFilterResult: boolean;
    isFetching: boolean;
    private status;
    private btnLoader;
    private infiniteScroll;
    private ordersTableBody;
    private ratingModal;
    private reviewPoints;
    private filterGeneration;
    private showExternalOrders;
    private setLoadMoreLoading;
    private refreshReviewLabels;
    private setupLoyalty;
    private formatNumber;
    private getBannerText;
    private isEligibleForReview;
    private openReviewModal;
    private switchFilter;
    private syncFilterToSearchParams;
    private initiateInfiniteScroll;
    loadMore(): Promise<void>;
    private createOrderRows;
    /**
     * Count of eligible, unrated orders. Kept in sync with the active tab's
     * response when it's the "waiting" tab, otherwise fetched separately so the
     * banner/tab count is accurate on the "All" tab too.
     */
    private loadPendingReviewCount;
    private populateOrdersTableIfEmpty;
    private fetchOrders;
    private getSingleOrderItem;
    private renderTableHead;
    private renderSkeletonRow;
    private renderSkeletonRows;
    private getEmptyFilterText;
    private renderFilters;
    private renderBanner;
    render(): any;
    componentDidRender(): void;
    componentWillLoad(): any;
}
export {};
