/**
 * @slot payment - Replaces payment item, has replaceable props `{image}`, `{payment}`.
 * @slot sbc - Replaces SBC certificate item, has replaceable props `{image}`.
 * @slot cod - Replaces COD item, has replaceable props `{image}`.
 */
export declare class SallaPayments {
    host: HTMLElement;
    private paymentSlot;
    private sbcSlot;
    private codSlot;
    private payments;
    /**
     * Array of the payments/items that are not supported, ex: ["sbc", "made-in-ksa", "cod", "mada", ""].
     */
    exclude: string[];
    componentWillLoad(): any;
    /** Slugs whose mini logo lives on the new assets CDN; add one when its logo is re-uploaded there. */
    private static NEW_CDN_LOGOS;
    /** Mini-logo URL: migrated slugs use the new assets CDN, the rest stay on the legacy host. */
    private paymentLogo;
    render(): any;
    componentDidLoad(): void;
    private static audited;
    /** Reports KSA stores whose commercial-register / freelance badge is gone or hidden. */
    private auditTrustBadge;
    componentDidRender(): void;
}
