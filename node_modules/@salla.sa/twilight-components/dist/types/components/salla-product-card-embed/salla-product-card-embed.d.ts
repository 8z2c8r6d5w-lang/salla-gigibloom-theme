import type { Product } from '@salla.sa/twilight/types/common';
export declare class SallaProductCardEmbed {
    hostEl: HTMLElement;
    /** Product identifier used to fetch product details. */
    productId: string | number;
    /** Optional alignment for the embedded card. */
    align?: 'left' | 'right' | 'center';
    /** Tracking source for the embed; defaults to the global `embed-card` channel, overridable per surface. */
    source?: string;
    loading: boolean;
    hasError: boolean;
    productData: Product | null;
    private cardContainer?;
    componentWillLoad(): Promise<void>;
    componentDidLoad(): void;
    componentDidRender(): void;
    /**
     * The host floats for `align="left"|"right"`. If the parent is a plain
     * block element with visible overflow it won't contain those floats,
     * letting them spill into following siblings (e.g. a comments section).
     * Promote such parents to a block formatting context.
     */
    private ensureParentContainsFloats;
    private fetchProduct;
    private renderCardNode;
    private withViewedSource;
    render(): any;
}
