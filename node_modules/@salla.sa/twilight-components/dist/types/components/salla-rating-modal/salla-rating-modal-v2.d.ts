import type OrderFeedbackResponse from './order-feedback-response';
import type { ReviewFactor } from './order-feedback-response';
export declare class SallaRatingModalV2 {
    order: OrderFeedbackResponse | undefined;
    hasError: boolean;
    showContactWidget: boolean;
    errorMessage: string;
    images: any[];
    productImages: Record<number, Array<any>>;
    editItemImages: Array<any>;
    productReviewFactors: Record<number, ReviewFactor[]>;
    editFactorRatings: Record<string, number>;
    productMainRatings: Record<number, number>;
    productFactorRatings: Record<number, Record<string, number>>;
    editMainRating: number | undefined;
    contact_body: (store: string, id: number, customer: string) => string;
    editMode: boolean;
    defaultMode: boolean;
    deleteMode: boolean;
    shouldOpenDeleteModal: boolean;
    editType?: 'product' | 'store' | 'shipping';
    editFeedbackId?: number;
    deleteFeedbackId?: number;
    editItem: any;
    allowAttachImages: boolean;
    allowAttachVideos: boolean;
    allowContactSupport: boolean;
    uploadCounts: Record<number, number>;
    uploadImageCounts: Record<number, number>;
    uploadVideoCounts: Record<number, number>;
    hasUploadsInProgress: boolean;
    loyaltyImageRatingPoints: number;
    private dragAndDropFilesLabel;
    private contactSubjectLabel;
    private editReviewLabel;
    private allowedImagesTypesLabel;
    private threeImagesMax;
    private areYouSureLabel;
    private confirmDeletionLabel;
    private cancelLabel;
    private confirmDeleteBtn;
    private updatedSuccessfullyLabel;
    private deletedSuccessfullyLabel;
    private nothingToRateLabel;
    private contactUsLabel;
    private requiredFactorsLabel;
    private rateTheFollowingLabel;
    private loyaltyPointsForImagesLabel;
    private contentRefs;
    private hiddenInputs;
    private hiddenVideoInputs;
    private editHiddenVideosInput?;
    private uploadingByProductId;
    private userFilledFactorKeys;
    private boundRatingRows;
    private stepsCount;
    private nextBtn;
    private backBtn;
    private modal;
    private editModal;
    private deleteModal;
    private currentIndex;
    private currentTab;
    private thanksTab;
    private body;
    private thanksTime;
    private steps;
    private dots;
    private submitted;
    private thanksCountdownInterval;
    private MAX_IMAGE_UPLOAD_LIMIT;
    private MAX_VIDEO_UPLOAD_LIMIT;
    private MAX_VIDEO_FILE_SIZE;
    private MAX_VIDEO_DURATION;
    private MAX_PARALLEL_UPLOADS;
    /**
     * The order id, to rate on its products & shipping.
     * Explicitly `null` means an unverified (no order) product review.
     */
    orderId?: number | string | null;
    productId?: number | string;
    host: HTMLElement | undefined;
    /**
     * Show the rating modal
     */
    private syncStoreSettings;
    private canAttachMedia;
    private getMediaUploadAccept;
    private refreshMediaLabels;
    private getAllowedMediaTypesLabel;
    private getMaxUploadHintLabel;
    private setUploadCount;
    private setUploaderProcessing;
    private scheduleUploaderHeightRefresh;
    private handleGalleryCountUpdate;
    /** An eligible review with no order behind it — product-only, no store/shipping steps. */
    private isUnverifiedReview;
    /** Encrypted order id from page config / prop — used to fetch order details. */
    private getRatingOrderId;
    /** Numeric order id from API response — used when submitting ratings. Null when unverified. */
    private getSubmitOrderId;
    private isCustomerOrderShape;
    private getBootstrapOrder;
    private loadCustomerOrderForEdit;
    private getOrderProductRatings;
    private getMatchedProductRating;
    private hasRatingFeedback;
    private isTestimonialsEnabled;
    private isProductsEnabled;
    private isShippingEnabled;
    private getOrderProducts;
    private getRateableProducts;
    /**
     * True when the order never had anything ratable to begin with (as opposed to
     * everything already being rated). Distinguishes a genuinely broken/empty
     * order-feedback response from the legitimate "all done, thank you" state.
     */
    private hasNothingToRate;
    private getStepsCount;
    private scheduleWizardSetup;
    private afterOrderLoaded;
    /**
     * Point the modal at a different order/product and invalidate any cached
     * order so the next open() re-fetches instead of reusing stale data.
     * Pass `orderId` as `null` for an eligible-but-unverified product review.
     */
    setOrder(orderId: number | string | null, productId?: number | string): Promise<void>;
    /** Builds a single-product, order-less feedback target for unverified reviews. */
    private loadUnverifiedProductOrder;
    private escapeHtml;
    private getModalTitle;
    open(): Promise<unknown>;
    openEditModal(params: {
        type: 'product' | 'store' | 'shipping';
        feedback_id: number;
    }): Promise<void>;
    fetchEditItem(feedbackId: number): Promise<void>;
    private editReview;
    openDeleteModal(params: {
        feedback_id: number;
    }): Promise<unknown>;
    deleteReview(): Promise<void>;
    /**
     * Show the rating modal
     */
    close(): Promise<HTMLElement>;
    private handleWizard;
    private refreshWizardRefs;
    private showActiveStep;
    private updateBodyHeight;
    private previousTab;
    private submit;
    private submittedBefore;
    private getRatingHiddenInput;
    private collectNamedInputs;
    private normalizeFactorsList;
    private validate;
    private collectFormData;
    private buildProductRatingPayload;
    private sendFeedback;
    private clearThanksCountdown;
    private showThankYou;
    private getFilepondPlaceholder;
    private getCustomerName;
    private readyUpLoyaltyPoints;
    private loyaltyPointsNote;
    private normalizeReviewFactorsPayload;
    private fetchProductReviewFactors;
    private fetchAllProductReviewFactors;
    private ensureProductFactorsLoaded;
    private getFactorFieldName;
    private normalizeToArray;
    private isFactorReviewsList;
    private getFactorReviewsFromRecord;
    private getEditFactorReviewsList;
    private getFactorKey;
    private getFactorRatingsForProduct;
    private setFactorRatingsForProduct;
    private markExistingFactorRatingsAsUserFilled;
    private shouldAutofillFactors;
    private autofillUnfilledFactors;
    private handleMainRatingChange;
    private handleFactorRatingChange;
    private setupProductRatingAutofill;
    private buildEditFactorRatings;
    private renderProductStep;
    private renderProductMainRating;
    private renderProductReviewFactors;
    private handleSendEmail;
    private handleSendWhatsApp;
    private resetData;
    private getDeleteModal;
    private renderUploadedImagesCount;
    private getEditModal;
    render(): any;
    componentWillLoad(): Promise<void>;
    private registerTranslations;
    componentDidRender(): void;
    disconnectedCallback(): void;
}
