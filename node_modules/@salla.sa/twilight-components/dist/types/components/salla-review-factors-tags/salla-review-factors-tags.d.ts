import type { ReviewFactor } from './interfaces';
export declare class SallaReviewFactorsTags {
    /**
     * Preloaded review factors. When provided, skips the order fetch.
     */
    factors?: ReviewFactor[];
    /**
     * Order ID used to fetch rating factors from `GET /orders/{order_id}`.
     */
    orderId?: number;
    /**
     * Product rating feedback ID.
     */
    ratingId?: number;
    /**
     * Product ID used to match the rated line item.
     */
    productId?: number;
    /**
     * Order item ID used to match the rated line item.
     */
    itemId?: number;
    loadedFactors: ReviewFactor[];
    componentWillLoad(): Promise<void>;
    protected onPropsChange(): Promise<void>;
    private getDisplayFactors;
    private loadFactors;
    private renderFactorTags;
    render(): any;
}
