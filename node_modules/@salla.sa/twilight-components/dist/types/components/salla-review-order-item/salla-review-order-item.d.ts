import type { OrderItem, ProductRatingMediaSource } from './interfaces';
export declare class SallaReviewOrderItem {
    /**
     * Product id used to match `items[].product.id` and `rating.product[].product.id`.
     * The order itself is fetched via `page.id`.
     */
    productId?: string | number;
    orderItem?: OrderItem;
    fetchOrderId?: string | number;
    productRatings: ProductRatingMediaSource[];
    allowUpdate: boolean;
    componentWillLoad(): Promise<void>;
    onProductIdChange(): Promise<void>;
    /** Real order id used for `GET /orders/{id}`. */
    private resolveFetchOrderId;
    private sameId;
    private label;
    private toOrderItemRating;
    /**
     * Build the reviewed line item from:
     * - `order.items[]` matched by `items[].product.id`
     * - `order.rating.product[]` matched by `rating.product[].product.id`
     */
    private resolveOrderItem;
    private loadOrderItem;
    private renderActions;
    private serializeRatingMedia;
    private serializeProductRatings;
    render(): any;
}
