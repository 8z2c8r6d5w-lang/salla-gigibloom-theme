import { HTMLStencilElement } from '../../stencil-public-runtime';
import SearchProductsResponse from './search-response';
import { SearchLabels, SearchTrendingCategory, SearchTrendingProduct } from './interfaces';
export declare class SallaSearch {
    host: HTMLStencilElement | undefined;
    constructor();
    private modal;
    private searchInput;
    private hasTrackedFocus;
    private hasLoadedPopularSearches;
    translationLoaded: boolean;
    labels: SearchLabels;
    results: SearchProductsResponse | undefined;
    loading: boolean;
    typing: boolean;
    debounce: ReturnType<typeof setTimeout>;
    search_term: string;
    hasInputValue: boolean;
    searchHistory: string[];
    /**
     * Most-searched categories to surface before the customer types a query.
     * Populated from GET /search/popular on the modal's first open; stays empty (and hidden) if
     * the request fails.
     */
    trendingCategories: SearchTrendingCategory[];
    /**
     * Most-searched products, reused both as the pre-search "most searched" list
     * and as the results-view "search suggestions" sidebar. Each links to the product's
     * own page. Same /search/popular source as trendingCategories.
     */
    mostSearchedProducts: SearchTrendingProduct[];
    /**
     * Set the component to display as a placeholder (button) that will open the modal. Defaults to `false`
     */
    placeholder: boolean;
    /**
     * Set the component display without modal window. Defaults to `false`
     */
    inline: boolean;
    /**
     * Adds a border radius to the input. Half of the height.
     */
    oval: boolean;
    /**
     * Sets the height of the input
     */
    height: number;
    /**
     * Maximum number of words allowed in the input
     */
    maxWords: number;
    /**
     * Show submit action buttons at the end.
     */
    showAction: boolean;
    open(): Promise<void>;
    handleModalVisibilityChanged(ev: CustomEvent<boolean>): void;
    onModalClose(): void;
    handleKeyDown(ev: KeyboardEvent): void;
    private commitSearchHistory;
    private tracking;
    private removeFromSearchHistory;
    componentWillLoad(): Promise<void>;
    private debounceSearch;
    private limitWordsAndSearch;
    private getQueryParam;
    private handleActionClick;
    handleSearch(val: string): void;
    search(val: any): void;
    private afterSearching;
    private renderCategoriesSection;
    private renderMostSearchedProductsSection;
    private renderSearchHistorySection;
    private renderIdlePanel;
    private renderResultsBody;
    private renderViewAllLink;
    private renderDropdown;
    private renderInputWrapper;
    private renderPlaceholderTrigger;
    private renderInlineTrigger;
    private renderModal;
    render(): any;
    /**
     * Run it one time after load
     */
    componentDidLoad(): void;
}
